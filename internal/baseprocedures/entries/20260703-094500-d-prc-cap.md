---
type: decision
layer: process
kind: procedure
canonical: capture
confidence: medium
summary: >-
    The capture procedure is the shared spine for recording signals and
    decisions: assemble a graph-grounded draft, play it back for explicit user
    confirmation, write through the pre-flight gate whose override only the
    user can choose, then verify the generated summary for fidelity.
    Supersede, close, and anchored captures enter the same procedure through
    params.
params:
    anchor: {type: entry-id, optional: true, desc: entry this capture is anchored on}
    supersedes: {type: entry-id, optional: true, desc: chain head this capture replaces}
    closes: {type: list<entry-id>, optional: true, desc: entries this capture resolves}
    kind: {type: entry-kind, optional: true, desc: pre-selected target kind}
state:
    captureBranch: {type: text, optional: true, desc: concrete branch authority explicitly seeded by the dispatching workflow}
    body: {type: text, desc: entry description; the first sentence must work as a standalone summary}
    entryKind: {type: entry-kind, desc: "signal or decision kind, from the distinguishing tests"}
    layer: {type: layer, desc: strategic | conceptual | tactical | operational | process}
    refs: {type: list<ref>, desc: "each {id, kind, desc?}; kind chosen from what the body asserts"}
    topics: {type: list<label>, desc: reuse existing labels; a new label deliberately starts a cluster}
    confidence: {type: confidence, desc: honest — high / medium / low}
    intent: {type: intent, optional: true, desc: required when entryKind is directive}
    attachments: {type: list<attachment-handle>, optional: true, desc: staged handles carrying the dialogue's full record}
    widenReport: {type: text, desc: searches run and entries inspected before drafting}
    canonical: {type: text, optional: true, desc: "an actor's canonical name — the stable identity used across the graph"}
    aliases: {type: list<text>, optional: true, desc: "other names the actor appears under — git, chat, external handles"}
    roleActor: {type: text, optional: true, desc: "for a role, the canonical of the actor this role binds to"}
    recognitionMode: {type: bool, optional: true, desc: "seeded true when this capture's words were already approved upstream (bootstrap cluster) — softens playback framing to recognition without weakening the confirm"}
    fidelityNote: {type: text, optional: true, desc: one-line note on why the summary is faithful}
    correctedSummary: {type: text, optional: true, desc: replacement summary when the generated one drifted}
steps:
    - id: assemble
      collect: [body, entryKind, layer, "refs?", "topics?", confidence, "intent?", "attachments?", widenReport, "canonical?", "aliases?", "roleActor?"]
      inject:
          - {fn: viewLayout, args: {layout: "active:as-counts"}}
      transitions:
          - when: entryKindIsActor and hasBody and hasCanonical and hasWidenReport
                  and aliasesWellFormed
                  and refsResolve and refKindsValid and refsInspected
                  and participantsCanonical
            to: playback
          - when: entryKindIsRole and hasBody and hasRoleActor and roleActorResolves
                  and hasWidenReport
                  and refsResolve and refKindsValid and refsInspected
                  and participantsCanonical
            to: playback
          - when: not entryKindIsActor and not entryKindIsRole
                  and hasBody and hasRefs and hasTopics and hasWidenReport
                  and refsResolve and refKindsValid and refsInspected
                  and participantsCanonical and intentPresentIfDirective
            to: playback
    - id: playback
      chooser: user
      options:
          - {choice: confirm, call: confirmPlayback, to: write}
          - {choice: adjust, collect: ["body?", "refs?", "topics?", "confidence?", "intent?"], to: assemble}
          - {choice: abort, to: end(abandoned)}
    - id: write
      guard: playbackConfirmed
      op: newEntry
      transitions:
          - when: noHighFindings
            to: verifySummary
          - otherwise: reviseOrOverride
    - id: reviseOrOverride
      chooser: user
      render: findings
      options:
          - {choice: revise, collect: ["body?", "refs?", "topics?"], to: assemble}
          - {choice: override, call: recordOverride, to: write}
          - {choice: abort, to: end(abandoned)}
    - id: verifySummary
      chooser: agent
      inject:
          - {fn: generatedSummary}
      options:
          - {choice: faithful, collect: [fidelityNote], to: end(completed)}
          - {choice: drifted, collect: [correctedSummary], call: replaceSummary, to: end(completed)}
---

The capture procedure is the shared spine for recording signals and decisions through dialogue: assemble a draft grounded in the graph, play it back for the user's explicit confirmation, write through the pre-flight gate — whose override is reachable only through the user — and verify the generated summary for fidelity. Supersede, close, and anchored captures are not separate moves; they are this procedure entered with params (`supersedes`, `closes`, `anchor`, a pre-selected `kind`). The instruction units below are the authoritative capture guidance, served step by step against the running draft.

## unit: assemble

Draft one entry — one idea, dialogue first. Report the fields below (batching them all in one report is fine); the step re-serves naming exactly what is missing until the draft is complete and mechanically valid.

**Ground before you draft.**{{if .widenReport}} Grounding was inherited from the dispatching move — the searches and entries already inspected:

{{.widenReport}}

Extend it only if your draft reaches beyond what was already inspected — a new ref, a claim the inherited reads don't cover; append those searches to `widenReport`. If the draft stays within the inherited grounding, leave it as is.{{else}} Search the graph from several different angles — different phrasings, plus any known term or ID — and inspect the promising candidates in full before asserting what the graph holds. An existing entry may already cover this (ref it, or enter a supersede instead), and the right refs come from reading chains, not titles. Record the searches you ran and the entries you inspected in `widenReport`.{{end}} Inspection is checked mechanically, not on trust: every ID in `refs`, `supersedes`, and `closes` must have been served to this session in full — a show call, an injected chain's primary, or reads made by the move that dispatched this capture. A rejection names the un-inspected IDs; read them (reads stay free), then report again.
{{if .anchor}}
This capture is anchored on {{.anchor}} — read its chain before drafting.{{end}}{{if .supersedes}}
This capture replaces {{.supersedes}}. The new body must carry the full current understanding on its own — the old entry retires — and state why it is replaced.{{end}}{{if .closes}}
This capture retires{{range .closes}} {{.}}{{end}}. The body must state the retirement rationale, not just point at the target.{{end}}

**Body** (`body`). The first sentence must work as a standalone summary — overview surfaces lead with it, so "Plan for X" tells a reader nothing; lead with what the entry is about. Fold the dialogue's substance into the description — trade-offs weighed, alternatives rejected, the why — so future readers get the reasoning, not just the conclusion. Every entry ID typed in the body must appear in `refs` (or be the supersede/close target), and every ref must be visible in the body's narrative.

**Kind** (`entryKind`). Signals record something noticed: `done` records completed work and must point at what it completes; `fact`, `question`, or `insight` when the narrative is unambiguously observational, an open unknown, or a synthesis; `gap` is the default — something needs attention. Decisions record something committed, tested in order: a constraint that must always hold → `contract`; perpetual pull with no completion criterion → `aspiration`; a justified choice against alternatives → `directive`; verifiable outcomes worth decomposing → `plan` (the body then requires an `## Acceptance criteria` section with `- [ ]` items); dispatching work whose shape is already known → `activity`.{{if .kind}} The target kind was pre-selected: **{{.kind}}**.{{end}}

**Layer** (`layer`): strategic — why, direction · conceptual — approach, shape · tactical — structure, trade-offs · operational — individual steps · process — how we work.

**Refs** (`refs`, each `{id, kind, desc?}`, full entry IDs). The kind names why the pointer exists, chosen from what the body asserts — never a default: `grounded-in` (a basis reasoned from), `builds-on` (extending a closed line, or the next step after a finished chain), `refines` (sharpening an active target in place), `addresses` (responding to a gap/question/insight, or realizing a decision's commitment), `surfaces` / `surfaced-by` (this work produced the target / was produced by it), `depends-on` / `required-by` (prerequisite, from either side), `related` (the floor — a genuine sibling, only when nothing sharper fits). A terminal done signal is never "addressed" — reach for `builds-on`, `grounded-in`, or `surfaced-by`. The optional `desc` says why this ref exists in this entry's narrative.

A ref may point across a repo boundary, written as `<repo-id>:<entry-id>`. Resolve-or-block applies equally there: a cross-repo ref must resolve to a declared dependency of this graph, or capture blocks — so confirm the target resolves (via `show`) before drafting, and a bare foreign ID is stored in full prefixed form. The lifecycle fields `closes` and `supersedes` never cross the boundary — a decision retires only entries in its own graph.

**Topics** (`topics`). Labels are stable identifiers — reuse beats reinvention. Labels in use across active entries:

{{.viewLayout}}

Pick an existing label when one fits; a new label deliberately starts a cluster. Prefer hierarchical paths (`family/member`) for label families.

**Confidence** (`confidence`) is honest: high — strong conviction · medium — reasonable but unvalidated · low — hypothesis.

**Intent** (`intent`) is required when the kind is directive and is never derivable: `pending` — demands follow-up (the default posture) · `guiding` — standing context that shapes later decisions without ever completing · `settled` — born terminal; the body must justify why no follow-up is needed.

**Attachments** (`attachments`). The description is the summary; the attachment is the record. When the dialogue produced design trade-offs, evaluation findings, exploration synthesis, or research, stage the full record (`stage_attachment`) and list the handles here — a compressed summary without the record loses the reasoning permanently. Skip only when the description alone carries the full substance.
{{if eq .entryKind "actor"}}
**This is an actor** — a participant identity, recorded at the process layer. It has no natural refs or topics, so the gate does not ask for them; what it needs instead:

- **Canonical** (`canonical`) — the stable name this person is known by across the graph, written once and reused in every `participants` field. Pick the form they'd recognize as theirs.
- **Aliases** (`aliases`, optional) — the other names they appear under: a git author name, a chat handle, an external identity. List each once; they are read-side conveniences for mining and comprehension, never a second canonical.
- **Body** — introduce the identity they bring from *outside* this project: affiliation, background, expertise — who they are independent of the work here. What they *do here* is a separate **role** entry, not this one. When a person came up in mixed identity-and-contribution terms, that split already happened upstream: draft the actor for who they are, and let the role capture carry the contribution.
- **Layer** (`layer`) — set it to `process`; actors live there.
{{end}}{{if eq .entryKind "role"}}
**This is a role** — how a participant contributes *here*, bound to their actor identity, recorded at the process layer. No natural refs or topics; what it needs instead:

- **Bound actor** (`roleActor`) — the canonical of the actor this role belongs to. It must already resolve to a known actor, so capture that actor first; the role binds to its canonical, never mints a new name.
- **Body** — state the contribution this person carries on this project: authority, domain weight, authorship, the decisions they hold. Keep it to their stable part here, not this week's task — what someone *does here* is their standing contribution, distinct from their current activity.
- **Layer** (`layer`) — set it to `process`; roles live there.
{{end}}
## unit: playback

{{if .recognitionMode}}These are the words you and the user settled on a moment ago, upstream — so this play-back is a recognition, not a fresh field-by-field review. Show the description verbatim, name in a line what it is, and ask them to confirm it goes in as settled. Keep it light; only slow down and open the full contract if something changed since they last saw it. The confirm is still explicit and still binds to the shown body — the framing softens, the invariant does not.{{else}}Present the draft to the user for verification. This is the verification contract, not a summary: the user checks the exact entry against its fields, so show the description verbatim and the fields labeled. Narrate around it in the user's language, but do not paraphrase the description.{{end}}

{{.body}}

- kind: {{.entryKind}} · layer: {{.layer}}{{if .intent}} · intent: {{.intent}}{{end}} · confidence: {{.confidence}}
- refs:{{range .refs}} {{.ID}} ({{.Kind}}){{if .Desc}} — {{.Desc}}{{end}};{{end}}
- topics:{{range .topics}} {{.}}{{end}}{{if .canonical}}
- canonical: {{.canonical}}{{if .aliases}} · aliases:{{range .aliases}} {{.}}{{end}}{{end}}{{end}}{{if .roleActor}}
- role of: {{.roleActor}}{{end}}
- target branch: {{if .captureBranch}}{{.captureBranch}}{{else}}the project's configured default branch{{end}}{{if .supersedes}}
- supersedes: {{.supersedes}}{{end}}{{if .closes}}
- closes:{{range .closes}} {{.}}{{end}}{{end}}{{if .attachments}}
- attachments:{{range .attachments}} {{.}}{{end}}{{end}}

This play-back recurs after every adjust. On the first presentation, show everything. After an adjust, a delta is enough — show the changed fields in full, name what is unchanged, and offer the complete entry on request. But a confirm must never bind to an unseen description: whenever the body changed since the user last saw it in full, show the full description verbatim again before asking — the user confirms what they see, not a memory of it.

Wait for the user's explicit answer — progress in the dialogue (answered questions, affirmations) is not confirmation; if unclear, ask. Relay the user's words verbatim with the answer. Confirm writes the entry; adjust carries revised fields back into the draft (any edit after a confirm reopens this play-back); abort abandons the capture.

## unit: findings

Pre-flight findings blocked the write:

{{range .findings}}- [{{.Severity}}] {{.Category}}: {{.Observation}}
{{end}}
Read each high finding as a prompt: what reasoning from the dialogue is missing from the entry text? Prefer **revise** — fold the missing context into the body rather than tweaking wording, adjust refs or topics, and the draft returns to play-back. Choose **override** only when a finding is genuinely wrong (for example, it argues against rationale the dialogue already settled). The override is the user's decision alone: present the findings to the user and relay their words verbatim with the choice.

## unit: verifySummary

The entry is written. Its stored summary is what overview surfaces and future briefings render — drift here is what other readers consume, and it is cheap to fix now.

Generated summary:

{{.generatedSummary}}

Check it against the body: does it stay true to the meaning, or has it introduced a participant not in the body, shifted the commitment framing, dropped the core idea, or "corrected" an identifier that was intentional? If faithful, answer with a one-line fidelity note. If drifted, answer with a corrected summary — it replaces the stored one verbatim.
