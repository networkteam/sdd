# Carried into every Claude Code worktree. Only gitignored files are copied.

# Run sdd immediately without waiting for a rebuild
bin/sdd

# Participant identity — captures need it
.sdd/config.local.yaml

# Devbox env cache — may speed shell startup; regenerates if stale
.devbox/
