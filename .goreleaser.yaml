version: 2

project_name: sdd

before:
  hooks:
    - go mod tidy

builds:
  - id: sdd
    main: ./cmd/sdd
    binary: sdd
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{ .Version }}

archives:
  - ids:
      - sdd

homebrew_casks:
  - name: sdd
    # A prerelease tag (v1.2.3-alpha.1) must not reach the tap — brew users
    # track stable, and a cask bump would serve them the soak build.
    skip_upload: "auto"
    repository:
      owner: networkteam
      name: homebrew-tap
      branch: main
      token: "{{ .Env.HOMEBREW_TAP_TOKEN }}"
    homepage: https://github.com/networkteam/sdd
    description: Signal-Dialogue-Decision graph tool
    hooks:
      post:
        install: |
          if OS.mac?
            system_command "/usr/bin/xattr", args: ["-dr", "com.apple.quarantine", "#{staged_path}/sdd"]
          end

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  github:
    owner: networkteam
    name: sdd
  extra_files:
    - glob: install.sh
