---
type: signal
layer: process
kind: done
closes:
    - 20260625-162643-d-prc-x1q
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - collaboration/concurrent-work
    - reliability/discipline
summary: This done-signal records the successful delivery of the worktree-isolation instruction fix (d-prc-x1q). The bundled worktree recipe now includes a re-anchor step after `EnterWorktree`, resolves the stale-absolute-paths habit, and the repo's `CLAUDE.md` clarifies that devbox and `PATH` remain base-scoped with explicit invocation patterns. Changes were committed at ca75c4e and aaa365c, with the Codex render validating clean.
summary_hash: 7e3d3cb4b2c0ec5efbb97b53151a404c
---

Ships the worktree-isolation instruction fix (d-prc-x1q). The bundled worktree recipe gained a re-anchor step — after `EnterWorktree`, pre-entry grounding stays graph-only, each file is re-Read at its worktree path before editing, and the "prefer absolute paths" habit is flagged as pointing at the base checkout. This repo's `CLAUDE.md` "## Worktrees" note was rewritten to state that devbox and `PATH` stay base inside a worktree (run `devbox run -c "$PWD" <script>` and `./bin/sdd`; Claude Code has no supported auto-activation on entry). Committed at ca75c4e (template source + `CLAUDE.md`); the installed Claude and Codex renders were refreshed via `sdd init` (commit aaa365c), and the Codex render validates clean.
