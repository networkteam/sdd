---
type: signal
layer: tactical
kind: done
closes:
    - 20260612-122618-d-tac-53l
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - type-system/refs
summary: This done-signal records the completed shipment of `surfaced-by` as the ninth ref-kind — the backward-class inverse of `surfaces` — making the surfaces pair order-independent and filling the terminal-`done` surfacer case where `addresses` is mechanically blocked. All six acceptance criteria are met, plus all closed-set enumerations (CLI help, MCP schema, SKILL.md, cli-reference) updated beyond the ACs; the full test and lint suite is clean. This closes the implementation plan (20260612-122618-d-tac-53l) that committed to adding the kind over documentation-only alternatives.
summary_hash: c43357f884a0c43e8a2dd7f21bfc2599
---

Shipped `surfaced-by` as the ninth ref-kind — the backward-class inverse of `surfaces` — completing plan d-tac-53l. The kind lets a surfaced entry captured after its surfacer point back with "raised/produced by", making the surfaces pair order-independent the way depends-on/required-by already is, and fills the terminal-`done` surfacer case where `addresses` is mechanically blocked. Implementation in commit a9cd517; installed skill renders refreshed in 0f9b0ce.

All six acceptance criteria met:

1. Capturable kind — `RefKind` constant, `refKinds` set, and `RefKindValues` carry the ninth kind (model + parse/capturable unit tests); a CLI dry-run accepts a `surfaced-by` ref end-to-end.
2. Applicability matrix declares `surfaced-by` applicable across all four target classes, including terminal-`done` (the surfacer case `addresses` cannot serve), each cell carrying its reading; a matrix test pins the terminal-`done` applicability.
3. Pre-flight renders it admissible (computed from the matrix) and the rubric carries the boundary — raised/produced by the target's work to `surfaced-by`; merely reasons from it to `grounded-in` — injected from the ref-kinds reference.
4. The ref-kinds reference and its framework-concepts mirror (via the template include) document the row and reading, record `surfaced-by` as the backward partner that makes the surfaces pair order-independent, and re-point the reverse-of-`surfaces` guidance from `grounded-in` to `surfaced-by`.
5. The growth/completeness note is corrected: the reverse of `surfaces` was found inadequate (lossy fallbacks plus mis-targeting on a terminal-done surfacer), so its inverse is added — the growth principle stands, the one "no inverse" judgment fixed.
6. A calibration eval case pins a "surfaced entry captured after a terminal-done surfacer" scenario to `surfaced-by`; it passes the live validator 3/3 with zero findings, so the boundary against grounded-in/addresses/related holds behaviorally, not only structurally.

Beyond the ACs (approved in dialogue): the four other closed-set enumerations — the CLI `--refs` help, the MCP tool schema, the SKILL.md closed-set line, and the cli-reference verb list — were updated so the ninth kind is discoverable wherever the set is listed, not only in the AC-named reference. Display order places `surfaced-by` adjacent to `surfaces`, mirroring depends-on/required-by. go vet (incl. -tags=eval), full go test, golangci-lint, and Codex skill validation are all clean.
