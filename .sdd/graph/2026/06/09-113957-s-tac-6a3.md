---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260609-105101-d-cpt-n0f
      kind: addresses
      desc: realizes the directive's bring-existing-surfaces-into-line commitment for the stats surface
closes:
    - 20260609-112301-s-tac-84t
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - cli/ux
summary: This done signal records that `sdd stats` styling has been aligned to the CLI color-scheme standard, replacing three bespoke styles with the shared palette and extracting `clr*` styles into a new `palette.go` to prevent future drift; committed as 46ccc13, vet/test/lint clean. It realizes the bring-existing-surfaces-into-line commitment of the color-scheme directive (d-cpt-n0f) and closes the gap signal that flagged the stats palette divergence (s-tac-84t).
summary_hash: 4315be87615bc5948b7465ef2a3f1414
---

Brought `sdd stats` styling into line with the CLI color-scheme standard (d-cpt-n0f), replacing its three bespoke styles with the shared palette: section headers now render white-bold (matching `sdd show`), secondary text in body grey 252, the table header rule faint 240, and column heads plain cyan — the bold dropped so they read like field labels do in `sdd show`. A blank line was added below each section header for separation. The shared `clr*` styles were lifted out of `show_styled.go` into a new `palette.go` so the scheme has one CLI-wide home and no command silently re-diverges. Committed as 46ccc13; vet/test/lint clean and verified against the live stats sink under a TTY.
