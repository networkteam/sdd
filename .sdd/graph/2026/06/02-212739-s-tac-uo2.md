---
type: signal
layer: tactical
kind: done
supersedes:
    - 20260601-214718-s-tac-mxi
closes:
    - 20260601-164056-d-cpt-lgs
    - 20260602-211125-d-prc-84e
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - dev/ux
summary: Shipped dev-binary auto-rebuild via git hooks under `.githooks/`, self-configured through devbox's `init_hook` and `core.hooksPath` on `direnv allow`, with CLAUDE.md updated for `devbox run build` (commit feadcf1). Closes both the tooling-coupling directive (d-cpt-lgs) and dev-vs-consumer contexts directive (d-prc-84e) by implementing the auto-rebuild mechanism that serves as the dev-repo currency for both. Supersedes the prior signal (s-tac-mxi) to consolidate execution coverage under both closed directives rather than duplicate the work.
summary_hash: 7dc0e4a5c0ec0433828495bf5ee87b3c
---

Shipped dev-binary auto-rebuild for the sdd repo via git hooks (post-merge, post-rewrite, post-checkout) under `.githooks/`, wired through devbox's `init_hook` and `core.hooksPath` to self-configure on `direnv allow`, with post-checkout guarding to skip file checkouts and CLAUDE.md updated to build via `devbox run build` (commit feadcf1). Fulfills both the tooling-coupling directive (d-cpt-lgs) and the dev-vs-consumer contexts directive (d-prc-84e), since the auto-rebuild is the dev-repo currency mechanism both name. Supersedes the prior done signal (s-tac-mxi) to widen its execution record to cover both decisions rather than duplicate it.
