---
type: decision
layer: tactical
kind: plan
refs:
    - id: 20260610-231642-s-ops-nfn
      kind: grounded-in
      desc: the recorded production-auth obligation that motivates this plan
    - id: 20260527-210616-d-tac-0v4
      kind: builds-on
      desc: picks up the HTTP-auth work that plan deferred as a non-goal
    - id: 20260531-161006-s-cpt-80v
      kind: grounded-in
      desc: the remote-hosting/auth model informing the OAuth-vs-allowlist choice
    - id: 20260609-234656-d-cpt-afn
      kind: depends-on
      desc: sequenced after the evaluation verdict authorizes the server to proceed
participants:
    - Christopher
confidence: medium
topics:
    - portability/mcp
summary: This plan commits to adding proper authentication to the workflow MCP server's HTTP transport — either OAuth 2.0/OIDC or a Host/Origin allowlist — before the server moves beyond ngrok-tunnelled evaluation. It fulfills the production-auth obligation incurred when DNS-rebinding protection was disabled (s-ops-nfn), picks up HTTP-auth work the CLI-parity plan explicitly deferred (d-tac-0v4), and uses the remote-hosting/auth model from feasibility research (s-cpt-80v) to inform the OAuth-vs-allowlist choice. Execution is sequenced after the evaluation verdict (d-cpt-afn) confirms the server proceeds.
summary_hash: 24e6d3039683e70832cd05ddbdb982e6
---

Plan to add proper authentication to the workflow MCP server's HTTP transport — either OAuth 2.0/OIDC or a Host/Origin allowlist — as a gate before the server is used beyond ngrok-tunnelled evaluation. This closes the production-auth obligation recorded when DNS-rebinding protection was disabled and the bearer-token guard was accepted as evaluation-only (s-ops-nfn), and picks up the HTTP-auth work that the CLI-parity plan explicitly deferred as a non-goal (d-tac-0v4). The choice between OAuth and an origin allowlist is left open pending the deployment shape: an allowlist suffices if hosting stays single-origin, whereas the multi-user remote-hosting model from feasibility research (s-cpt-80v) calls for OAuth with session state bound to the authenticated principal. Execution is sequenced after the two-client evaluation verdict (d-cpt-afn) confirms the server proceeds, so this plan stays open as the committed next step rather than starting before that decision.

## Acceptance criteria

- [ ] An auth mechanism (OAuth 2.0/OIDC or Host/Origin allowlist) is chosen and the rationale recorded against the deployment shape
- [ ] The chosen mechanism is implemented on the HTTP (Streamable HTTP) transport
- [ ] DNS-rebinding protection is re-enabled or explicitly superseded by the new auth, resolving the evaluation-only disablement in s-ops-nfn
- [ ] The bearer-token-only path cannot be used for non-local deployment
- [ ] Auth behavior is covered by tests and verified end-to-end
