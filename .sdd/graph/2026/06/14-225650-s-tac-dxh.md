---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260530-001431-s-cpt-7ix
      kind: builds-on
      desc: the prose-refs divergence gap b7f closed; its committed fix was never built, so the concern is live again
    - id: 20260611-113656-d-tac-b7f
      kind: grounded-in
      desc: the directive whose body-to-edge enforcement commitment this gap rests on — promised here, never built in the validator
    - id: 20260530-002329-s-prc-hbi
      kind: related
      desc: a concrete instance of the premature-closure pattern — a gap closed before its framing was fulfilled
    - id: 20260410-151529-s-stg-3vr
      kind: grounded-in
      desc: the finding that text-instruction discipline is unreliable — why guidance-only coverage is insufficient
participants:
    - Christopher
    - Claude
confidence: medium
topics:
    - reliability/pre-flight
    - type-system/refs
summary: 'This gap signals that the body-to-edge ID consistency enforcement committed to by directive d-tac-b7f was never implemented: `preflight_mechanical.go` contains no check that extracts IDs from entry bodies and diffs them against structured edges, leaving only unreliable authoring guidance in place (grounded in s-stg-3vr). Because b7f nonetheless closed the prose-refs divergence gap (s-cpt-7ix), that gap reads resolved while its fix is absent — a direct instance of the premature-closure pattern (s-prc-hbi). The open question is whether to build the missing check or formally retire b7f and honestly reassess whether s-cpt-7ix remains closed.'
summary_hash: 1cf27cdad7c0d33e84155d946ba41bd7
---

The body-to-edge ID consistency enforcement that directive b7f committed to was never built: the mechanical pre-flight layer (`preflight_mechanical.go`) runs participant, ref-kind, supersede-fork, and actor/role checks, but nothing extracts IDs from an entry body to diff against its edges. What's actually live is authoring guidance in the skill — text-instruction discipline, which the project has repeatedly found unreliable (s-stg-3vr) — not the mechanical guarantee b7f promised. Yet b7f closed the prose-refs divergence gap (s-cpt-7ix), so that gap reads resolved while its fix is vapor: a concrete instance of the premature-closure pattern (s-prc-hbi). Open question this gap carries: build the body-to-edge check, or formally accept that the skill guidance is sufficient and retire b7f — and if the latter, reconsider whether s-cpt-7ix is honestly closed.
