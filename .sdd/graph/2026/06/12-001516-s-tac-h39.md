---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260611-153513-d-tac-4ln
      kind: addresses
      desc: realizes AC 3 (supported_agents list + multi-select + render-each-listed-agent)
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - skill/architecture
    - portability/runtime
    - cli/init
summary: 'This signal records the completed delivery of slice 2 of the multi-agent render plan: `sdd init` now reads a `supported_agents` list from config and renders each listed agent into its own scope directory per run, with agent selection via a `--agents` flag or interactive multi-select persisted on fresh init. It realizes AC 3 of the multi-agent render plan (20260611-153513-d-tac-4ln), covering the supported-agents list, multi-select UX, and per-agent render loop; slice 3 items — this repo''s own Codex adoption, Codex frontmatter standard, and upgrade-path persistence — remain deferred.'
summary_hash: 50378b8755ff3443cf80aa06aa0f9324
---

Shipped slice 2 of the multi-agent render plan: `sdd init` now reads a committed `supported_agents` list from `.sdd/config.yaml` and renders every listed agent into its own scope dir each run (claude → `.claude/skills/`, codex → `.agents/skills/`). Adds the `AgentCodex` target, `KnownAgentTargets`/`ParseAgentTarget`, a per-agent `SkillInstallDir` subpath, and a `Config.SupportedAgents` field; `InitCmd.Targets` replaces the single target and the handler loops the install per agent, committing each rendered dir. Selection comes from a `--agents` flag or an interactive multi-select on first run (Claude pre-selected), persisted on fresh init via `FormatConfig`. Verified: `sdd init --agents claude,codex` renders both trees with the Codex profile carrying its own deviations (instructed `run sdd …` injection + else-branch sub-skill phrasing), config records `supported_agents: [claude, codex]`, and unit tests cover the install-dir subpath, target parsing, the multi-agent install, and the multi-select toggle/confirm logic. Realizes AC 3 of plan d-tac-4ln. Deferred to slice 3: this repo's own codex adoption, the Agent-Skills-standard Codex frontmatter, and `supported_agents` persistence on the upgrade path. Code in commit f28fc84.
