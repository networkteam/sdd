---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260622-095321-d-tac-8u5
      kind: addresses
      desc: supplies the --type d|s view parity prerequisite this directive named (partial; --missing-kind remains)
    - id: 20260506-151345-d-tac-uww
      kind: builds-on
      desc: adds the type() filter to the view pipeline this closed plan delivered
    - id: 20260607-125045-s-prc-guf
      kind: related
      desc: the cli-reference doc lag fixed here was a fresh instance of this untraced-impact pattern (participant filter shipped without updating the skill doc)
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - cli/view
    - reliability/discipline
summary: This signal records the completed addition of a `type()` filter to the `sdd view` pipeline — accepting `d`/`s` abbreviations and full names — along with a catch-up of the `cli-reference` skill doc to cover `participant()` and `group(by(participant))` that had silently drifted. It partially addresses the `sdd list` retirement directive (20260622-095321-d-tac-8u5), supplying the `--type d|s` parity prerequisite while leaving `--missing-kind` as the sole remaining gap, and extends the view pipeline delivered by (20260506-151345-d-tac-uww). The doc lag caught here was a fresh instance of the untraced-impact pattern (20260607-125045-s-prc-guf), surfaced manually rather than by any systematic check.
summary_hash: 4cb6890be2d3a122374e88919c3cd4b2
---

Added a `type()` filter to the `sdd view` pipeline — `type(d)`/`type(s)`, with full names `decision`/`signal` accepted — shipped in commit 25d0dc9. It resolves the abbrev and sets the existing `GraphFilter.Type` that `g.Filter` already applies, so no apply-stage wiring was needed; it mirrors the `layer()` filter. This supplies the `--type d|s` parity that `sdd view` lacked — a prerequisite the `sdd list` retirement directive (20260622-095321-d-tac-8u5) named — leaving only the `--missing-kind` migration helper, which is likely droppable, as the remaining parity gap before removal. The same change caught up the `cli-reference` skill doc, which had drifted: it never documented the `participant()` filter or `group(by(participant))` added earlier this session, so both are now documented alongside `type()` and the installed renders were refreshed. That doc lag was itself a fresh instance of the untraced-impact pattern (20260607-125045-s-prc-guf) — a capability shipped without updating its connected surface — caught during this follow-up rather than by any systematic check. Verified against the live graph (decisions and signals partition the active set exactly; full-name form and filter composition work) with new unit tests, plus vet and lint clean.
