---
type: decision
layer: tactical
kind: activity
refs:
    - id: 20260614-133644-d-tac-dcn
      kind: addresses
      desc: builds the Go-native spike this evaluation scores
    - id: 20260413-142300-d-cpt-l3s
      kind: grounded-in
      desc: the in-process integration calls the existing finders and handlers directly
participants:
    - Christopher
    - Claude
confidence: medium
topics:
    - portability/runtime
    - strategy/adoption
summary: 'Commits to building the Go-native spike for the two-spike runtime evaluation (20260614-133644-d-tac-dcn): a throw-away hosted SDD agent on agent-sdk-go that integrates sdd in-process, calling existing finders and handlers directly without an MCP bridge, then wires an HTTP/SSE web UI path to reach the shared evaluation bar. The in-process integration design is grounded in the established CQRS layering (20260413-142300-d-cpt-l3s), which makes direct handler and finder calls architecturally natural. The UI path and single-source instruction rendering are identified as the make-or-break unknowns; findings feed the runtime verdict without merging into the main codebase.'
summary_hash: 728a23b754cdc9b03a80b8ba622dff99
---

Spike: build a throw-away hosted SDD agent on agent-sdk-go (Go), integrating sdd in-process — the agent calls the existing finders and handlers directly, no MCP bridge (grounded in the CQRS layering, 20260413-142300-d-cpt-l3s) — wiring the agent loop and proving a web UI path (HTTP/SSE to a React chat, or AG-UI-shaped events). Reach the shared evaluation bar (20260614-133644-d-tac-dcn): a non-technical user gets a plain-language catch-up and captures one entry through guided dialogue with confirmation, end to end against a real sdd repo. The UI path is the make-or-break unknown for this candidate; process instructions must render from the single template source, not a copy. Built in its own worktree, not merged; findings feed the runtime evaluation.
