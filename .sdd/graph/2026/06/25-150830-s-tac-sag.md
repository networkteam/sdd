---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260506-151345-d-tac-uww
      kind: builds-on
      desc: the sdd view pipeline that absorbed both overview surfaces, making the hand-built status/list presenters redundant
closes:
    - 20260610-000311-d-tac-oei
    - 20260622-095321-d-tac-8u5
    - 20260622-123745-d-tac-8p6
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - cli/ux
    - skill/architecture
summary: This done-signal records the completed retirement of `sdd status` and `sdd list`, folding both overview surfaces onto `sdd info` and `sdd view` across code, skills, documentation, and installed references. It closes the `sdd status` removal directive (d-tac-oei), the `sdd list` retirement directive (d-tac-8u5), and the `--missing-kind` waiver (d-tac-8p6), executing all three mandates in full. The work builds on the `sdd view` pipeline (d-tac-uww) that made the hand-built presenters redundant, with build, vet, test, and lint all passing clean.
summary_hash: 6d03388a4ead8f5444818b2b430dc580
---

Retired the `sdd status` and `sdd list` commands, folding both overview surfaces onto `sdd info` (the session header) and `sdd view` (the overview pipeline). Removed both CQRS command stacks — the `status` and `list` commands in `cmd/sdd`, their `query` / `finders` / `presenters` layers, and the status presenter test — while preserving the shared `EntryLine` row format `sdd view` and `sdd search` reuse; `searchCapability()` relocated to its sole remaining consumer in `finders/info.go`. Rewrote every skill template, reference doc, README / AGENTS line, and stale Go and pre-flight-rubric comment that named the removed commands onto `sdd info` / `sdd view` / `sdd search` / `sdd show`, editing shared `sdd status, sdd list` lines once; the Participants-block and status-notation explanations were relocated to their `sdd view` homes in `framework-concepts.md` rather than deleted. Two judgment calls beyond the directives' literal worklist: the bare-`sdd` default command was dropped entirely, so `sdd status` / `sdd list` now exit nonzero as unknown commands and bare `sdd` prints help (chosen over a default that would mask the removed command); and the now-orphaned `GraphFilter.MissingKind` field plus its model test were removed to complete the `--missing-kind` waiver rather than leave it half-applied. Also removed an orphaned installed reference (`playbook-catchup.md`) that still cited `sdd status` though no template produced it. Shipped in commits 401a34b (command removal + reference rewrite), 3d219e5 (dropped default command), and 2578758 (orphan removal); installed skill renders refreshed via `sdd init` (d5a66bf). Build, vet, test, and lint all clean; the five Codex skill renders validate. Built on the `sdd view` pipeline (d-tac-uww) that made the hand-built presenters redundant.
