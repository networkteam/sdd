---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260605-182704-d-cpt-mvb
      kind: grounded-in
      desc: styled renderer conforms to the dual-audience terminal architecture (TTY detection, colorprofile)
    - id: 20260609-105101-d-cpt-n0f
      kind: related
      desc: the standing CLI color-scheme directive this styling work produced
closes:
    - 20260604-162244-d-tac-z3k
    - 20260609-110815-d-tac-vht
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - cli/ux
summary: 'This signal records the completed delivery of the `sdd show` restructure: the bespoke label/arrow block is replaced by a YAML frontmatter envelope plus markdown body, compact upstream/downstream trees, and a lipgloss-styled renderer (via glamour) selected against a plain renderer by TTY detection, `--format`, and `NO_COLOR`, conforming to the dual-audience terminal architecture (20260605-182704-d-cpt-mvb). All eight acceptance criteria are met, closing both the restructure plan (20260604-162244-d-tac-z3k) and its depth-flag augmentation (20260609-110815-d-tac-vht). The color scheme validated here was lifted into a standing CLI palette directive (20260609-105101-d-cpt-n0f) for consistency across future rendering work.'
summary_hash: 5b08f365280a998fee5f312a38de26c3
---

Ships the `sdd show` restructure: the bespoke label/arrow block is replaced by a YAML frontmatter envelope plus raw markdown body for the primary entry and compact upstream/downstream markdown trees for the neighborhood, with a lipgloss-styled terminal renderer (glamour-rendered body) selected against a plain markdown renderer by TTY detection, `--format`, and `NO_COLOR`, conforming to the dual-audience terminal architecture (20260605-182704-d-cpt-mvb). Closes the plan (20260604-162244-d-tac-z3k) and its augmenting directive (20260609-110815-d-tac-vht).

All eight plan acceptance criteria are met: the envelope + raw body (AC1), the stored summary omitted by default and shown with `--with-summary` (AC2), the compact tree line shape `<verb> <id> (<kind>, <status>) — <first-sentence>` with `↳` why sub-lines (AC3), first-sentence micro-summaries derived at render time for depth nodes (AC4), the child-level `+N more refs truncated` line at the depth boundary (AC5), renderer selection by TTY / `--format` / `NO_COLOR` (AC6), status and topic derivation moved to the read side so both renderers stay pure (AC7), and tests across both renderers including truncation and a deduped node (AC8). The augmenting directive's commitment — independent `--up` / `--down` depths with downstream shown by default — is met, with the defaults (up 2, down 1) verified empirically against the live graph.

Deviations, all dialogued: the styled body renders through a new dependency, `charm.land/glamour/v2` (its document margin zeroed for flush-left alignment), folded in here rather than captured as a separate entry; the styled color scheme was lifted into a standing directive (20260609-105101-d-cpt-n0f) so other CLI rendering work stays consistent; and the depth surface moved from the acceptance criteria's `--max-depth` wording to `--up` / `--down` (the augmenting directive above), with the child-level truncation behavior the AC specified unchanged.

Code landed across commits 98d825b (read-side status derivation), 3c95e1d (envelope + markdown tree), 5905b70 (styled renderer + selection), 1707a67 / 2b92251 / 49cfa20 / 44a1631 / 76d7a6e (styling refinements from review), and 238d96d (independent `--up` / `--down` depths). Bundled skill references were swept and reinstalled. The full vet / test / lint gate is clean and the output was verified end to end on the live graph — styled and plain, active and superseded entries.
