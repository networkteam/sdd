# Example intent classification — reliability + CLI loose-ends sweep

Illustrative starting point, NOT the authoritative set. At execution, sweep
every active directive whose intent is still `unknown` and classify each.
Outcomes seen in this example:

## intent: guiding  (standing — keeps constraining future work)
- d-cpt-mvb  — terminal-experience architecture
- d-cpt-rkj  — CLI output policy
- d-cpt-5f4  — three-renderer split
- d-cpt-n0f  — colour palette
- d-prc-nfz  — ref-kind sharpness calibration

## promote-to-standalone, then intent: guiding
- d-prc-v0h  — applicable-never-high calibration; currently `refines` the
  superseded d-prc-2is, so supersede into a standalone directive carrying
  the rule (its lever isn't covered by the successor d-prc-nfz).

## close, not stamp  (realized one-shot, retired by a done signal)
- d-tac-w30  — closed by s-tac-wts (sdd stats shipped)
- d-prc-17a  — closed by s-prc-ufl (derived-status wiring shipped)

## Note
- d-cpt-5f4 refines d-cpt-rkj — handle that pair together (fold or rebase) per d-prc-u61.
