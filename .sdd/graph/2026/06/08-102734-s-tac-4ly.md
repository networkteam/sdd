---
type: signal
layer: tactical
kind: done
closes:
    - 20260608-095950-d-tac-m9b
participants:
    - Christopher
    - Claude
confidence: high
summary: This done-signal reports the successful shipment of supersede-chain resolution, where refs and derived status now walk `SupersededBy` transitively to the live head rather than halting at a superseded intermediate. It closes the resolution plan (d-tac-m9b), satisfying all six acceptance criteria via a new `ResolvedRef` type, updated `DerivedStatus`, render trail support in `sdd show`/`sdd view`, and a fork-detection lint rule — all covered by a clean vet/test/lint gate and end-to-end verification on the live graph.
summary_hash: 89242c31bb3a4c5fcb7294320977019b
---

Shipped supersede-chain resolution: refs and reported status now resolve to the live head of a supersession chain rather than stopping one hop short or reporting a superseded intermediate. Closes the resolution plan (d-tac-m9b), realizing all six acceptance criteria.

Added a `ResolvedRef` value type and `Graph.ResolveRef` that walks `SupersededBy` transitively to the head, retaining the ordered path behind `Head()`/`Origin()`/`IsStale()`/`Path()` with the slice unexported so an intermediate can't be mistaken for the head. `Graph.DerivedStatus` resolves its chosen closer/superseder to that head — fixing the wrong-closer attribution and the transitive single-hop gap as one primitive. `sdd show` and `sdd view` `expand(refs)` render the supersede trail through to the head for stale ref targets; pre-flight keeps the literal superseded target (its status drives the ref-meta check) and surfaces the live head's content alongside; `sdd lint` flags a fork — an entry with more than one superseder. Resolver, status, render, and lint are covered by tests, the full vet/test/lint gate is clean (golangci 0 issues), and the ground-zero case verified end-to-end on the live graph (a reference into a twice-superseded chain now renders its trail through to the live head). Running the new lint over the live graph surfaced one pre-existing fork — a question superseded by two successors — captured as a separate grooming finding.
