---
type: signal
layer: process
kind: done
closes:
    - 20260608-102113-s-prc-ba5
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - reliability/discipline
summary: This done signal records the addition of a commit-hash citation rule to the `/sdd` implementation playbook's implement-then-close step, committed as b355cf0 and reinstalled via `sdd init`. It closes the playbook gap (20260608-102113-s-prc-ba5) by making explicit commit-hash citation mandatory in closing done signals, ensuring the artifact pointer is always present and pre-flight's artifact-durability check does not fire. The entry dogfoods the rule it introduces by citing its own fix commit.
summary_hash: 6b93cdfaf3ecc1fefc425538eb18e360
---

Added the commit-hash citation rule to the `/sdd` implementation playbook's implement-then-close step: the closing done signal must cite the commit hash(es) of the code it records, so the artifact pointer is explicit and pre-flight's artifact-durability check does not fire. A one-line capture-discipline addition with no design choice — short-loop closing the gap directly. Committed to the bundled skill source as b355cf0 and reinstalled via `sdd init` (installed copy refreshed in its own auto-commit). This done signal cites its own fix commit, dogfooding the rule it adds.
