---
type: signal
layer: tactical
kind: done
closes:
    - 20260610-000354-d-tac-n9k
    - 20260604-150714-d-cpt-cyx
participants:
    - Christopher
    - Claude
confidence: high
summary: This done signal records the full end-to-end shipment of the directive `intent` attribute (`pending | guiding | settled`) across the CLI, model, view, pre-flight, and skills, with all acceptance criteria met and tests clean. It closes both the conceptual directive that defined the intent attribute and its terminality semantics (d-cpt-cyx) and the tactical plan that specified the implementation work (d-tac-n9k). The one-time backfill of existing directives' intent remains a separate, now-unblocked activity.
summary_hash: e2f49b8ec677bc82d9f142398f56a6d0
---

Shipped the directive `intent` attribute (`pending | guiding | settled`) end to end across the CLI, model, view, pre-flight, and skills, realizing the acceptance criteria set by the conceptual directive (d-cpt-cyx) and completing the implementation plan (d-tac-n9k) — both closed here.

By AC: capture — a `--intent` flag plus the MCP capture arg, with `NewEntryCmd.Validate` enforcing directive-only, the closed value set, and required-on-directive at every write path; the model stays permissive so legacy directives read unchanged (commit 915e43e). Terminality — a new `settled` derived status, born terminal with no closing edge; supersession beats settled; closing a settled directive is rejected (ce7f6d3). Rendering — `{status: settled}` across view, chains, and `sdd show`; `[intent: guiding]` on entry lines while pending and unspecified stay quiet; settled directives drop out of active listings (be1b583, 015eda9). Read split — an `intent()` view filter composable within `not()`; catch-up action lanes now carry directives but exclude guiding; `/sdd` surfaces active guiding directives as a standing-context block (015eda9). Pre-flight — a `settled-justification` rubric emits a medium finding when a settled directive's body does not justify its terminality, validated live with both eval fixtures passing (7f65e47). Docs, skill capture-discipline guidance, bootstrap examples, view help text, and the German vocabulary gloss refreshed; installed renders re-stamped and the Codex render validated (7ef411a, 916c39a, 5e01640, df533c3).

`go vet`, `go test ./...`, and `golangci-lint` are clean. AC 4's reference to the retired `sdd list` was dropped, and AC 5's guiding-exclusion was wired into the open-loops catch-up lane that shipped after the plan was drafted. The one-time backfill of existing directives' intent stays a separate, now-unblocked activity.
