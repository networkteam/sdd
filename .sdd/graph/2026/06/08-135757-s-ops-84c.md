---
type: signal
layer: operational
kind: done
closes:
    - 20260608-135744-s-ops-qbl
participants:
    - Christopher
    - Claude
confidence: high
summary: This done-signal records that `.sdd/stats/` was added to the gitignore set written by `sdd init`, with both init gitignore tests extended to assert the fourth entry and all checks passing at commit 24c26a4. It closes the gap flagged by the missing stats-sink gitignore entry (20260608-135744-s-ops-qbl), ensuring re-running `sdd init` on upgraded projects now excludes the machine-local stats sink. The fix is scoped to this single instance; the broader untraced-dependent-surface discipline remains open and is being addressed separately.
summary_hash: fa2d925c8d388beb959a78f6c3fa772d
---

Added `.sdd/stats/` to the gitignore set `sdd init` writes (alongside the other machine-local `.sdd/` paths) and extended both init gitignore tests to assert the fourth entry; `go vet ./...` and the handlers tests pass (commit 24c26a4). Re-running `sdd init` on an upgraded project now excludes the machine-local stats sink. Scoped to this single instance — the broader untraced-dependent-surface discipline it belongs to stays open and is being taken up separately.
