---
type: signal
layer: operational
kind: fact
refs:
    - id: 20260610-225912-s-tac-n9a
      kind: related
      desc: the workflow MCP server slice whose bearer-token-guarded HTTP transport this configuration sits on
    - id: 20260531-161006-s-cpt-80v
      kind: related
      desc: feasibility research's auth/remote-hosting model that the production caveat bears on
participants:
    - Christopher
confidence: high
topics:
    - portability/mcp
summary: This signal records that the MCP SDK's DNS-rebinding protection was disabled on the HTTP transport of the workflow MCP server during ngrok-tunnelled evaluation, because the tunnel rewrites Host/Origin headers in a way that triggers blanket 403 rejections. The trade-off is acceptable for the current PoC given the bearer-token guard already on that transport (20260610-225912-s-tac-n9a), but is explicitly scoped to evaluation only — production deployment will require an origin allowlist or OAuth-based auth, as the remote-hosting model identified in feasibility research (20260531-161006-s-cpt-80v) demands.
summary_hash: f0937b4e5eab79efe68ec3857ec51ad2
---

The MCP SDK's built-in DNS-rebinding protection rejected every request reaching the workflow MCP server through the ngrok tunnel with a 403, so it was disabled on the HTTP (Streamable HTTP) transport during evaluation. The protection validates the `Host`/`Origin` headers against an allowlist of expected local hosts; an ngrok tunnel rewrites those headers to the public tunnel domain, which never matches, producing a blanket 403. Disabling it is judged acceptable for the current PoC/evaluation setup because the bearer-token guard on the HTTP transport already authenticates every caller, making the rebinding defense redundant there. This trade-off is explicitly scoped to tunnelled remote evaluation and must not carry into the production version; a real multi-user deployment needs either an allowlist that includes the true public origin or OAuth-based auth rather than relying on the bearer token alone.
