---
type: signal
layer: tactical
kind: done
closes:
    - 20260601-152219-s-tac-koz
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - type-system/refs
summary: Fixed the summary-prompt hash drift by rendering on-disk ref kinds in summaries while preserving canonical output in pre-flight and user-facing contexts (commit fec9284). The parsed Ref now maintains raw on-disk kinds, FormatEntryForPrompt splits into summary and canonical variants, and all verifications pass across the test suite and linting. Closes the tactical gap (20260601-152219-s-tac-koz) and restores 23 legacy entries to their original byte form without history rewrite, honoring the design intent (d-cpt-s9j).
summary_hash: 440428d2939c84bbdc523ecc31878127
---

Stopped the summary-prompt hash drift from the grounds/evidence to grounded-in alias by rendering each ref's on-disk kind in the summary prompt while keeping pre-flight and all user-facing output canonical (commit fec9284). The parsed Ref now preserves its raw on-disk kind — set in UnmarshalYAML only when an alias actually resolves, so non-alias refs stay byte-equal to constructed literals — and FormatEntryForPrompt splits into a summary variant (on-disk kind, via Ref.OnDiskKind) and the canonical variant pre-flight uses. Verified by building the pre-fix binary, which flags the legacy entries stale (grounds-hash vs grounded-in-hash), against the fixed binary, which reports them clean; go vet, the full test suite, and golangci-lint all pass. Also restored the 23 entries the pre-fix re-summarize treadmill had already canonicalized on disk back to their original byte form from git — all pure ref-kind plus hash re-stamps with summary prose preserved — so their original stored hashes are valid again under the fix (sdd lint clean across the graph), completing the no-history-rewrite intent of d-cpt-s9j.
