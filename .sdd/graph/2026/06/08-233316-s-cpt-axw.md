---
type: signal
layer: conceptual
kind: annotation
refs:
    - id: 20260608-190808-d-cpt-30v
      kind: related
      desc: the rule directive being joined to the type-system/rules cluster
participants:
    - Christopher
    - Claude
confidence: medium
topics:
    - type-system/rules
summary: This annotation signal retroactively tags the rule directive (20260608-190808-d-cpt-30v) into the `type-system/rules` cluster, consolidating the rule family — kind, discovery, capture, and consolidation work — under one topic. Because the directive predates the `type-system/rules` label and carries different inline tags, this entry adds cluster membership externally rather than modifying the immutable original.
summary_hash: 69abf1be85a6abd2e55147b2ab51947a
---

Tags the rule directive (d-cpt-30v) into the `type-system/rules` cluster so the rule family — the kind, discovery, capture, and consolidation work — gathers under one topic. The directive predates the `type-system/rules` label (it carries `type-system/kinds`, `reliability/discipline`, and `skill/architecture` inline), so this annotation adds the membership rather than re-tagging the immutable entry.
