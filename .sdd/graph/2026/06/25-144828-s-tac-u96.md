---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260625-115201-s-cpt-6h6
      kind: grounded-in
      desc: realizes the inverse-heat (coldness) model this insight proposed
closes:
    - 20260625-122343-d-tac-cvl
    - 20260625-115123-s-prc-6cw
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - skill/catch-up
    - cli/view
summary: This done signal records the completed shipment of the catch-up "Open loops" lane, implementing the `coldness(e) = decay(entry-age) / (1 + in-degree)` algorithm in `sdd view` with an exp-30d default and weave-don't-list sub-skill guidance. It realizes the inverse-heat model proposed by the conceptual insight (20260625-115201-s-cpt-6h6), completes the implementation plan (20260625-122343-d-tac-cvl), and closes both dimensions of the catch-up blind-spot gap (20260625-115123-s-prc-6cw) — dimension two via coldness ranking, dimension one by including the directive kind in the lane filter.
summary_hash: c25af8808a5dcdf42349b04b7ab6ec12
---

Shipped the catch-up open-loops lane, completing plan 20260625-122343-d-tac-cvl and resolving both dimensions of the catch-up blind-spot gap 20260625-115123-s-prc-6cw. Added a `coldness` rank algorithm to `sdd view` — `coldness(e) = decay(entry-age) / (1 + in-degree)`, the inverse of heat: fresh, un-referenced entries rank highest, catching the just-captured commitment that heat scores near zero because nothing refs it yet. The catch-up template gained an "Open loops" data lane — `kind(plan,activity,directive,gap,question):active:rank(coldness(exp-30d)):n(8):expand(refs)` — that resolves dimension two (fresh commitments dropped by cold-start heat) directly and dimension one (fresh directives, previously in no catch-up lane) by including the directive kind; the sub-skill weaves open loops into the existing story-arc threads by their upstream refs rather than rendering a flat block, and treats a genuinely disconnected one as its own new-direction beat. All six acceptance criteria met: the algorithm (bare `coldness` defaulting to exp-30d, slower than heat's exp-14d), the lane rendering in both the Claude injection and Codex instruction forms, the weave-don't-list guidance, documentation in both rank-vocabulary surfaces (`sdd view` help text and the `cli-reference` skill doc), and the installed-skill refresh (Codex render validates; `go vet`, `go test`, and `golangci-lint` clean). The exp-30d half-life was sanity-checked against the live graph: it surfaced a clean set of genuinely-open recent commitments including fresh directives, with no flooding and no over-fading at current ages, so exp-30d is kept as the v1 default — exp-60d is the lever if recency-bias proves too strong as the graph ages. Code committed in 1475369; installed skill renders refreshed in b5fdf39.
