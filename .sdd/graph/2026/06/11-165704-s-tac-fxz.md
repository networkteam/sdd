---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260611-153513-d-tac-4ln
      kind: addresses
      desc: realizes ACs 1, 2, and the Claude side of 5 (partial; plan stays open)
    - id: 20260611-162553-d-tac-dtl
      kind: addresses
      desc: implements the skill-render application of the Go-templates mechanism (broader migration + bridge remain)
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - skill/architecture
    - portability/runtime
summary: 'This signal records the completion of slice 1 of the multi-agent render plan: skill sources are now a single agent-neutral Go-template tree under `internal/bundledskills/templates/`, rendered per agent by `Load(target)`, with bespoke parsers deleted (net −40 lines) and Claude output verified byte-equivalent by `TestLoadRendersClaudeProfile` (commit 889fa19). It partially addresses the multi-agent plan (20260611-153513-d-tac-4ln), realizing ACs 1, 2, and the Claude side of AC 5, while delivering the skill-render application of the Go-templates directive (20260611-162553-d-tac-dtl); both refs remain open as config migration, the AGENTS.md/CLAUDE.md bridge, Codex rendering, and the Codex eval are still outstanding.'
summary_hash: 600a0d7a166ad7388b4ebd0e4a92e78a
---

Shipped slice 1 of the multi-agent render plan: SDD's skill source is now one agent-neutral Go-template tree under `internal/bundledskills/templates/` (`*.md.tmpl`), rendered per agent by `Load(target)`, with "claude" reduced to a render profile. Per-agent prose deviations are `{{ if eq .Agent "claude" }}` conditionals and an `inject` helper (Claude's `!`cmd`` injection vs an instructed run); a `{{ template }}` include and an escaped attachments placeholder replace the bespoke `ResolveSkillIncludes` and comment-directive parser, both deleted (net −40 lines). The Claude render is byte-equivalent to the prior bundle — verified by `sdd init` leaving `.claude/skills/` unchanged and guarded by `TestLoadRendersClaudeProfile`; the one whitespace drift (an extra blank line from the include's trailing newline) was caught by that check and fixed. Realizes ACs 1, 2, and the Claude side of AC 5 of plan d-tac-4ln and the skill-render application of directive d-tac-dtl; both stay open — config/`.gitignore`/pre-flight migration, the `AGENTS.md`/`CLAUDE.md` bridge, Codex rendering, and the Codex eval remain. Code in commit 889fa19.
