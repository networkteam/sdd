---
type: signal
layer: tactical
kind: done
closes:
    - 20260608-000750-d-tac-g1j
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - cli/ux
summary: Records the completed upgrade of the charm TUI stack to v2 (bubbletea 2.0.7, bubbles 2.1.0, lipgloss 2.0.3), migrating `sdd init`'s five interactive prompt models in `cmd/sdd/main.go` to the new API surface — `KeyPressMsg`, `msg.String()` key matching, `View() tea.View`, and `SetWidth`. This closes the committed upgrade decision (20260608-000750-d-tac-g1j); static analysis and automated tests pass, but the interactive run-loop remains untested and a manual `sdd init` smoke test in a scratch directory is noted as warranted.
summary_hash: 3099ab728ee7626815c928fa42311208
---

Upgraded the charm TUI stack to v2 (bubbletea 2.0.7, bubbles 2.1.0, lipgloss 2.0.3, `charm.land/*/v2`), migrating the sole consumer — `sdd init`'s five interactive prompt models in `cmd/sdd/main.go` — to the v2 API: `tea.KeyMsg` → `tea.KeyPressMsg` with `key.Type` switches replaced by `msg.String()` ("enter", "ctrl+c", "esc", "up"/"k", "down"/"j"); `View() string` → `View() tea.View` via `tea.NewView`; and the `textinput.Model.Width` field → `SetWidth`. `go vet`, full `golangci-lint`, and `go test ./...` are green, and the rebuilt binary runs. The interactive run-loop (live keypress handling, cursor render) is not covered by automated tests and warrants a manual `sdd init` smoke in a scratch dir. Commit ac680ae.
