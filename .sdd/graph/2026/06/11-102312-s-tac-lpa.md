---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260611-092529-d-prc-nfz
      kind: surfaces
      desc: the calibration directive this work produced, captured through the new pipeline
closes:
    - 20260610-233200-d-tac-tph
    - 20260423-200039-s-prc-vvd
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - reliability/pre-flight
    - type-system/refs
    - reliability/testing
summary: 'This signal records the completed delivery of the pre-flight ref-kind reliability rework, shipping all nine acceptance criteria of the plan (d-tac-tph): applicability judgments moved to a deterministic Go matrix, the finding schema reordered to emit reasoning before verdict, and six pinned leak cases added to the eval suite, which now stands at 29/29 clean. The work surfaces the evidence-gated medium calibration as its own standing directive (d-prc-nfz), captured through the new pipeline itself. It closes the supersede-eagerness gap (s-prc-vvd) as structurally resolved: the severity-before-reasoning mechanism driving cross-run oscillation is removed, its evidence pinned as a passing eval case.'
summary_hash: a736a17b46ab2fa8f7d42960959cf692
---

Shipped the pre-flight ref-kind reliability rework: applicability decisions moved out of the LLM into a Go-declared matrix with deterministic mechanical enforcement, the finding schema now emits reasoning before its verdict, and the calibration eval gained six pinned leak cases with N-run pass-rate assertions — the full suite stands at 29/29 with the vet/test(-race)/lint gate clean. All nine acceptance criteria of the plan are met; commits 8559bbf, 398ea12, 62ba68a, b25ab18, 5dd5a80, 6276712, cbb07b5, and the eval-helper fix concluding verification carry the work, with full per-AC evidence in the attached [verification report](./11-102312-s-tac-lpa/verification.md).

Two deviations from the plan's framing, both documented in the report. First, pre-fix reproduction was partial: the five pinned ref-meta cases passed 3/3 on Sonnet before the fix — the captured leaks are model- and context-dependent, so those cases pin the contract going forward rather than demonstrating a before/after flip; the supersede-oscillation case did reproduce the cross-run severity instability (high, absent, medium on identical input), albeit on a true positive. Second, two pre-existing eval cases that expected kind questions at high were recalibrated to evidence-backed mediums, since the high tier for kind questions is now mechanical by design.

Verification itself earned its cost by catching two regressions before they shipped: models on both Haiku (live capture) and Sonnet (eval) dropped the leading category field after the initial reorder — fixed by ordering observation, category, severity so every field follows the reasoning — and the matrix notes initially funneled a replacement-shaped directive into the ref-kind channel, displacing a genuine supersession concern; both notes now name supersession territory explicitly, and the displaced case passes. The work surfaced the evidence-gated medium calibration now standing as its own directive (d-prc-nfz), captured through the new pipeline itself.

This closes the supersede-eagerness gap (s-prc-vvd) as resolved: the severity-before-reasoning mechanism behind its cross-run oscillation is structurally removed, its evidence is pinned as the supersede-restructure eval case (3/3 green post-fix), and the misread-class highs it documented did not reproduce in sampling — the pinned case remains the regression sentinel should they return.
