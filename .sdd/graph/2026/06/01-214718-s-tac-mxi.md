---
type: signal
layer: tactical
kind: done
closes:
    - 20260601-164056-d-cpt-lgs
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - dev/ux
summary: Shipped dev-binary auto-rebuild for the sdd repo via git hooks (post-merge, post-rewrite, post-checkout) under `.githooks/`, wired through devbox's `init_hook` and `core.hooksPath` to self-configure on `direnv allow`, with post-checkout guards to skip file checkouts and CLAUDE.md updated with build documentation. Closes the tooling-coupling directive (d-cpt-lgs) by implementing the automatic rebuild mechanism it specified; the version-discipline gate it required already exists in the minimum_version write gate.
summary_hash: 9fd4cecdffc0e7e0ed6b5db6e72acadc
---

Shipped the dev-binary auto-rebuild for the sdd repo (commit feadcf1): committed git hooks (post-merge, post-rewrite, post-checkout) under `.githooks/` that rebuild `bin/sdd` via a new `devbox run build` script, with `core.hooksPath` wired by the devbox `init_hook` so `direnv allow` self-configures; post-checkout guards on the branch-checkout flag to skip file checkouts. CLAUDE.md updated to build via `devbox run build` and to document the auto-rebuild. Verified that the build script produces the binary, post-merge rebuilds a missing one, and the post-checkout guard builds on a branch switch but skips file checkouts. Closes the tooling-coupling directive (d-cpt-lgs); the version-discipline mechanisms it decided already exist in the minimum_version gate.
