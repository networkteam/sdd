---
type: signal
layer: process
kind: fact
refs:
    - id: 20260604-154612-d-prc-v0h
      kind: grounded-in
      desc: the applicable-never-high rule and terminal-done tie-break the finding violates
    - id: 20260605-141122-s-prc-lsg
      kind: grounded-in
      desc: the shipped recalibration pinned this shape against addresses only; this instance evades the pin
    - id: 20260610-000139-s-prc-xr9
      kind: related
      desc: fourth instance for the non-convergence pattern it names
    - id: 20260608-225505-s-prc-lbv
      kind: related
      desc: 'contrast: that instance self-retracted in prose; this one is internally coherent'
    - id: 20260608-003047-s-prc-pex
      kind: related
      desc: 'contrast: that instance oscillated across runs; this one fired confidently in a single run'
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - reliability/pre-flight
    - type-system/refs
summary: This signal observes that pre-flight emitted a confidently wrong single-run `[high]` ref-kind-inapplicable finding on a `builds-on` ref to a terminal-done target, correctly naming the applicable kinds in prose while simultaneously categorizing one as inapplicable — a new leak axis distinct from intra-run self-retraction (s-prc-lbv) and cross-run oscillation (s-prc-pex). The finding violates the applicable-never-high rule (d-prc-v0h) and evades the eval pin shipped by the recalibration (s-prc-lsg), which guards only the `addresses` direction. It adds a fourth instance to the non-convergence pattern (s-prc-xr9) and supplies a pinnable eval case for the deferred rubric work (d-prc-v0h).
summary_hash: f8b7b5df0b2c2e6c2c2a1368d474404b
---

Pre-flight emitted a single-run `[high]` ref-kind-inapplicable finding on a `builds-on` ref to a terminal done signal, recommending `grounded-in` — escalating the documented builds-on/grounded-in defensible choice on the terminal-done shape into a precondition violation. The finding's own prose performed the tie-break correctly (naming `grounded-in` and `surfaces` as the applicable kinds) yet categorized the defensible kind as inapplicable, so unlike the self-retracting (s-prc-lbv) and oscillating (s-prc-pex) instances, this finding is internally coherent and confidently wrong — a new leak axis that a retraction-detection fix would not catch. The shape is exactly the terminal-done case the recalibration pinned an eval fixture for (s-prc-lsg), but the pin guards the `addresses` direction only; the inapplicable-categorization of one of the two canonical kinds evades it. First instance observed in an external project graph (non-English entry body), showing the leak is content- and language-independent. Adds a fourth instance to the non-convergence pattern (s-prc-xr9) and a pinnable eval case for the deferred rubric work (d-prc-v0h). Redacted transcript with the verbatim finding attached: [transcript](./10-222058-s-prc-2lm/preflight-builds-on-inapplicable-transcript.md).
