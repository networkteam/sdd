---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260604-195225-s-tac-jxr
      kind: grounded-in
      desc: the prior cache measurements the before-picture rests on
    - id: 20260604-162749-s-tac-osb
      kind: addresses
      desc: caching-prefix gap; structural fix shipped, left open pending the realized-reuse measurement
    - id: 20260605-141122-s-prc-lsg
      kind: builds-on
      desc: corrects two augmenting eval fixtures the ref-meta recalibration left on builds-on
closes:
    - 20260604-164359-d-tac-yt1
    - 20260604-164527-d-tac-fah
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - reliability/pre-flight
    - llm/caching
    - reliability/grounding
summary: 'This signal reports the completed shipment of the pre-flight cache restructure: the system prompt is now a byte-stable, type-independent universal preamble with per-type rubrics and proposed entries moved to the user prompt, and the open-signal completeness scan has been dropped from decision_refs in favour of skill-side coverage. The restructure is grounded in prior cache measurements showing poor reuse (s-tac-jxr), directly addresses the caching-prefix gap (s-tac-osb) — left open pending realized-reuse confirmation — and corrects two eval fixtures made stale by the ref-meta recalibration (s-prc-lsg). It closes both the open-signals removal directive (d-tac-yt1) and the byte-stable preamble directive (d-tac-fah).'
summary_hash: 7fd446ece756e64b60bd62420891725e
---

Shipped the pre-flight cache restructure. Pre-flight's system prompt is now a byte-stable, type-independent universal preamble shared by the 10 substantive checks (role, universal partials, ref-kind vocabulary, sorted active contracts, output format), with per-type rubrics and the proposed entry moved to the user prompt, so sequential captures within a session reuse Anthropic's prompt cache (commit f7dfada). Also dropped the open-signal completeness scan from the decision_refs check — the largest volatile chunk in the prefix — now that skill-side Widen->Inspect covers that job; ref-completeness judges against referenced entries only. Active contracts live in the cached preamble, sorted by full ID so the prefix stays byte-identical when the contract set is unchanged — a byte-stability invariant guarded by a render test.

The prior measurement (s-tac-jxr) showed cache reuse was rare (~1 in 9 pre-flight calls) and the half-stable prefix was a net cost: the cache-write premium fell on ~130k tokens created against only ~6k read. Byte-stability is now proven by test; the realized cache-read win is measurable on future sequential captures, which will close the caching gap (s-tac-osb).

annotation/focus keep a lighter bespoke system block to avoid over-flagging their structural rubrics. Eval validation surfaced and resolved three issues: durability gained salience in the shared preamble and correctly fired on a fixture missing its artifact (fixture corrected); two augmenting fixtures used builds-on on active plans and moved to refines, matching the ref-meta recalibration (s-prc-lsg) that left them stale; and a transient unescaped-quote JSON-parse failure prompted a safe-quote instruction in the verdict output format — a mitigation, with parse-retry and tool-use structured output noted as the durable guards.

Closes d-tac-yt1 and d-tac-fah. Full before/after analysis and case-by-case eval validation in the attached record (./06-125155-s-tac-sk1/record.md).
