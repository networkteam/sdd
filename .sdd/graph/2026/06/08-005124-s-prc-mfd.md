---
type: signal
layer: process
kind: done
refs:
    - id: 20260410-151529-s-stg-3vr
      kind: grounded-in
      desc: text-instructions-fail → make the gate procedural, not more prose
    - id: 20260603-154135-s-prc-lud
      kind: grounded-in
      desc: de-dup discipline — one evaluation definition, referenced
    - id: 20260607-022834-d-cpt-927
      kind: related
      desc: corrects the worktree recipe set up under it
closes:
    - 20260608-005046-s-prc-czq
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - reliability/discipline
    - skill/architecture
summary: This signal records the hardening of the `/sdd` implementation gate and consolidation of evaluation definitions, closing the mode+marker-skip gap (s-prc-czq). The fix is structural rather than prose-based—grounded in the text-instructions-fail insight (s-stg-3vr)—enforcing a fixed mode menu and mandatory WIP marker at the engage→implement transition, with evaluation consolidated into a single referenced definition per the de-dup discipline (s-prc-lud). The worktree recipe was also corrected under the scope established by (d-cpt-927), and a fresh-session re-eval confirmed both gates held end-to-end.
summary_hash: 863c5928aac2e06530259c9214afd99a
---

Hardened the `/sdd` implementation gate and consolidated evaluation, closing the mode+marker-skip gap. The engage→implement transition is now a prescriptive gate: a fixed mode menu (agent recommends, user picks) and a mandatory WIP marker — skippable only via an explicit quick mode — which removes the "don't branch for solo work" contradiction that had licensed skipping. Evaluation is defined once in `references/evaluation.md` (inner/outer lenses; three points — during implementation, the pre-merge landing briefing, and after landing) and referenced from the engage, implementation, and meta-process playbooks per the de-dup discipline (s-prc-lud), reconciling the divergent technical/product/brand framing. The fix is procedural rather than more principle-style prose, grounded in the text-instructions-fail insight (s-stg-3vr): a "remember to" reminder demonstrably failed the first eval, so the gate enforces the move structurally. The worktree recipe was also corrected (marker-on-base-first, `--force` teardown). Validated by a fresh-session re-eval exercising both gates and the conclude: the agent presented the mode menu and recommended one, created the marker, ran autonomously, then gave a landing evaluation (inner + outer, surfaced implicit decisions, its merge-ready read) before the user confirmed the merge. The marker-on-base and `--force` corrections landed post-eval, not yet re-exercised. Commits b3948f8, 699ab05, e2f3d58 plus installed re-stamps.
