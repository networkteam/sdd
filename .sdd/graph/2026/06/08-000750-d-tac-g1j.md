---
type: decision
layer: tactical
kind: activity
refs:
    - id: 20260416-150852-d-tac-s2g
      kind: builds-on
      desc: the bubbles-based sdd init TUI this migrates to v2
participants:
    - Christopher
    - Claude
confidence: medium
topics:
    - cli/ux
summary: Commits to upgrading the charm TUI stack (bubbletea, bubbles, lipgloss) to v2, migrating the sole existing consumer — the `sdd init` interactive setup in `cmd/sdd/main.go` — to the new API surface including `View() tea.View`, `KeyPressMsg`, and named keys. This builds on the bubbles-based TUI foundation established for `sdd init` (20260416-150852-d-tac-s2g), advancing it to v2 while the codebase footprint is still a single file, so future surfaces like `sdd stats` start on current rather than legacy v1 code.
summary_hash: efc86774f44e9ac76025d5f5810fc5f1
---

Upgrade the charm TUI stack — bubbletea, bubbles, lipgloss — to v2 (`charm.land/*/v2`), migrating the single existing consumer (`sdd init`'s interactive setup in `cmd/sdd/main.go`) to the new API: `View() tea.View`, `KeyPressMsg` with renamed fields, named keys (space). Done now while the footprint is one file, so new TUI surfaces (the planned `sdd stats`) build on the latest rather than accreting v1 code. Verify `sdd init` still runs and the test suite stays green.
