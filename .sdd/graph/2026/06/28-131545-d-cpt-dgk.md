---
type: decision
layer: conceptual
kind: directive
refs:
    - id: 20260616-234140-d-tac-9lv
      kind: addresses
      desc: supplies the intent-backfill stamping for this directive
    - id: 20260628-131525-d-cpt-owo
      kind: refines
      desc: refines the output policy by replacing naive logging-to-stderr with TUI-aware leveled logging on the human path
    - id: 20260628-131533-d-cpt-qhr
      kind: grounded-in
      desc: extends the three-renderer audience separation to the progress/feedback/logging surface
supersedes:
    - 20260605-182704-d-cpt-mvb
participants:
    - Christopher
    - Claude
confidence: medium
intent: guiding
topics:
    - cli/ux
    - cli/logging
summary: This guiding directive establishes the terminal-experience architecture for the sdd CLI, separating interactive TTY humans — served by a single output coordinator managing transient progress, leveled slog, and styled feedback — from non-TTY consumers receiving clean structured stdout and slog on stderr. It refines the naive logging-to-stderr output policy with TUI-aware leveled logging on the human path (d-cpt-owo) and extends the three-renderer audience separation across the full progress/feedback/logging surface (d-cpt-qhr). It supersedes the prior identical directive (d-cpt-mvb), re-stated here under the one-time intent-backfill stamping activity (d-tac-9lv).
summary_hash: 70be7788e53ab040c3ace8eba575f26e
---

Establish the terminal-experience architecture for the sdd CLI, serving two consumer modes that stay distinct: interactive TTY humans and non-TTY consumers (agents, pipes, scripts). For TTY humans a single output coordinator owns the terminal so transient progress, leveled observability logging, and styled feedback never corrupt each other or the TUI; non-TTY mode emits clean structured results on stdout and structured slog on stderr, with no progress animation or decoration. It commits to four capabilities — transient progress distinct from log lines, leveled slog routed through the coordinator, reusable styled feedback components, and a per-command UX design — and honors the CQRS contract by exposing progress/feedback reporter callbacks on command and query structs, wired to a renderer only at the CLI layer. This is the standing terminal-experience architecture every command's output wiring conforms to — hence guiding. Re-stated from the prior identical directive (d-cpt-mvb) under the one-time intent backfill (d-tac-9lv); the superseded entry carries the full rationale and refs.
