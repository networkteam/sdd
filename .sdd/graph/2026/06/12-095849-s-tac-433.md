---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260611-153513-d-tac-4ln
      kind: addresses
      desc: realizes AC 4 (conforming Codex render, skills-ref validated) and the Codex side of AC 5 (per-agent deviations as conditionals); plan stays open
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - skill/architecture
    - portability/runtime
summary: This signal records the completed implementation of conforming Codex skill renders, validated end to end via the skills-ref tool with all five Codex skill dirs reporting "Valid skill" (commit e5071d8, vet + test + lint clean). It realizes AC 4 and the Codex side of AC 5 of the multi-agent render plan (20260611-153513-d-tac-4ln), with RenderSkillFile now target-aware and per-agent frontmatter deviations expressed as template conditionals from a single source. The plan remains open as this repo's own Codex adoption and the AGENTS.md/CLAUDE.md bridge are deferred to the next slice.
summary_hash: a395261e57a8e8ea28d816e558752152
---

Codex skills now render conforming to the Agent Skills standard (agentskills.io), validated end to end by the skills-ref reference tool: all five rendered Codex skill dirs report "Valid skill". RenderSkillFile became target-aware — Codex nests the sdd-version/sdd-content-hash stamps under metadata: (the standard rejects unknown top-level keys) while Claude keeps them top-level, with the read and hash paths accepting either placement so drift detection stays target-agnostic (an emptied metadata map is dropped on strip so a stamped Codex file hashes identically to its unstamped source). Per-agent frontmatter deviations are expressed as trim-marked template conditionals from the single source: Codex gains compatibility: and drops the Claude-only context/model/user-invocable keys, while Claude output stays byte-identical (verified: sdd init leaves .claude/skills/ untouched). The validator is managed in the project devbox (a uv package) and run via a validate-skills script over .agents/skills/. Committed as e5071d8, vet + test(-race) + lint clean. Realizes AC 4 and the Codex side of AC 5 of the multi-agent render plan; this repo's own Codex adoption and the AGENTS.md/CLAUDE.md bridge remain for the next slice, so the plan stays open.
