---
type: signal
layer: tactical
kind: done
closes:
    - 20260628-123059-d-tac-ck9
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - cli/architecture
summary: This done-signal confirms the completed refactor of the five per-kind active-decision helpers — Directives, Activities, Plans, Contracts, Aspirations — into thin Graph.Filter wrappers, consolidating active-set logic (closed, superseded, settled, role-cascade) into a single location. It closes the directive that mandated this change (20260628-123059-d-tac-ck9), including its requirement to retain named accessors for call-site readability. The refactor is verified behavior-preserving via go vet, the full test suite, and golangci-lint, with the commit landed at 6e21ba3.
summary_hash: e4804dfa7b6c165065d1adee2a668cee
---

Collapsed the five per-kind active-decision helpers — Directives, Activities, Plans, Contracts, Aspirations — into thin Graph.Filter(GraphFilter{Type: TypeDecision, Kind: K, OpenOnly: true}) wrappers, so the active-set definition (closed, superseded, settled, role-cascade) now lives in Graph.Filter alone rather than being duplicated across six places. Named accessors were kept over a generic ActiveDecisions(kind) for call-site readability, as the directive specified. The refactor is behavior-preserving: go vet, the full test suite, and golangci-lint all pass clean, and the settled-directive exclusion that prompted the directive is no longer maintained in two spots. Committed at 6e21ba3.
