---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260420-170144-s-prc-p6q
      kind: related
      desc: the entry the observation was made on
    - id: 20260826-003829-s-prc-1s1
      kind: surfaced-by
      desc: drafting that gap is what led to this check
participants:
    - Christopher
confidence: high
topics:
    - search/index
    - reliability/grounding
summary: 'A vector search built from near-verbatim wording in (20260420-170144-s-prc-p6q)''s own summary returns that entry nowhere in twenty-five results, while two other entries from its topic cluster are returned and a text search on the same terms returns it first; the index manifest confirms the entry is recorded as indexed with four chunks under the current embedding fingerprint. Scope and cause remain open: only one entry was checked, and the vector store''s files expose no readable identifiers to verify whether its vectors are actually present. The check was made while drafting (20260826-003829-s-prc-1s1), whose prior art the earlier searches had missed.'
---

A vector search using wording taken from 20260420-170144-s-prc-p6q's own summary does not return that entry at any rank, while a text search on the same terms returns it first.

The query was "mine playbook move transcripts URLs documents load the source identify touchpoints". That entry's stored summary opens "The SDD skill lacks a named 'mine' playbook move for extracting insights from external sources through guided dialogue" and goes on to name podcast transcripts, loading sources, and source URLs. The overlap is close to verbatim, so the entry was expected to rank at or near the top. It was absent from twenty-five results, with superseded entries included, while two entries from the same topic cluster were returned. The same terms in text mode returned it first. Its index manifest records it as indexed with four chunks under the current embedding fingerprint, and it carries a summary.

Expected: an entry the index holds ranks for a query built out of its own words.

Extent unknown. One entry was checked, so whether this is a single case or a class stays open for investigation. The cause is unestablished too: the vector store's files carry no readable entry identifiers, so whether this entry's vectors are in the store could not be checked from outside it.
