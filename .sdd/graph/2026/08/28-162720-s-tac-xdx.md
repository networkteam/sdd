---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260820-162456-d-tac-o2v
      kind: addresses
      desc: delivers its v0.17.0 half — note, log line, README lead; the v0.18.0 half stays open
participants:
    - Christopher
confidence: high
topics:
    - implementation/skill-text
    - strategy/adoption
summary: 'The v0.17.0 transition messaging is live across all three surfaces named by the retirement directive: the `/sdd` skill now opens with a deprecation note, `sdd init` logs it at info level, and the README leads with `/sdd-engine` while demoting the legacy invocation to a collapsed deprecated section. This delivers the v0.17.0 half of the two-release retirement plan (20260820-162456-d-tac-o2v), which stays open as the v0.18.0 removal, rename, and orphan cleanup remain ahead.'
---

The v0.17.0 transition messaging is live on all three surfaces the retirement directive names (20260820-162456-d-tac-o2v): commit 17ccd010 carries the source changes, with the refreshed skill renders committed by the reinstall.

The `/sdd` skill now opens with a deprecation note the agent relays to the user before anything else — work in `/sdd-engine`, v0.18.0 will remove this skill and rename `/sdd-engine` to `/sdd` — as the directive's one named exception to the frozen prose. `sdd init` logs the same message at info level, reaching users who upgrade without invoking the skill. The README leads with engine use: the how-you-use-it section and the quickstart start at `/sdd-engine`, the legacy invocation moved into a collapsed section marked deprecated, and the session walkthrough no longer names the legacy skill's mechanics. The Codex render still validates against the Agent Skills standard.

The directive stays open: this delivers its v0.17.0 half only, while the removal, rename, and orphan cleanup remain committed to its v0.18.0 half.
