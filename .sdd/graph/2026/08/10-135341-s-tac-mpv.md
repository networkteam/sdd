---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260730-093011-d-tac-k4q
      kind: grounded-in
      desc: the active plan commits to deleting the relocation subsystem, making removal of this obsolete race test proportionate
closes:
    - 20260810-123434-s-tac-di7
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - reliability/testing
    - dev/ci
    - portability/runtime
summary: Commit `4014eca4` deletes `TestTargetRollbackFinalUnlinkPreservesSameContentForeignInode` from `main` and restores CI to green with no production behavior change, confirmed by passing macOS, Ubuntu, and lint jobs on GitHub Actions. The removal is proportionate because the relocation subsystem the test exercised is already committed for deletion (20260730-093011-d-tac-k4q), and neighboring same-content foreign-inode tests remain, keeping live coverage intact. This closes the red-CI gap (20260810-123434-s-tac-di7); the broader session-subsystem rebuild proceeds separately.
---

Commit `4014eca4` removes the obsolete `TestTargetRollbackFinalUnlinkPreservesSameContentForeignInode` race case from `main`, and GitHub Actions CI run https://github.com/networkteam/sdd/actions/runs/31385383406 passed without any production behavior change. The deleted test exercised final-unlink identity handling inside relocation machinery already committed for removal by 20260730-093011-d-tac-k4q; repairing its cancellation ordering would have deepened maintenance on that retired path, while skipping would have kept dead coverage visible but inactive.

The removal is deliberately narrow: the neighboring publication-sync replacement-identity test and dual-name rollback reconciliation test remain, so same-content foreign-inode protection still has coverage in the relocation suite. Before push, `go test ./...`, `go vet ./...`, and `golangci-lint run ./...` all passed locally. After push, the GitHub Actions macOS test, Ubuntu test, and lint jobs all completed successfully, providing the main-branch evidence that was missing from the first draft. This closes 20260810-123434-s-tac-di7; the broader four-slice session-subsystem rebuild remains separate.
