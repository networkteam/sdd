---
type: decision
layer: tactical
kind: directive
refs:
    - id: 20260824-093559-d-tac-onl
      kind: refines
      desc: grows the focus's v0.17.0 list in place; the focus stays the period's frame and both retire together
    - id: 20260827-224853-s-tac-giv
      kind: addresses
      desc: commits its repair into v0.17.0 as a third direct fix
    - id: 20260824-165218-s-tac-7w6
      kind: addresses
      desc: commits its resolution — both candidate fixes together — into v0.17.0
    - id: 20260826-090801-d-tac-got
      kind: depends-on
      desc: the fact work the pre-flight rewire needs first; pulled into v0.17.0 with it
participants:
    - Christopher
confidence: high
intent: pending
topics:
    - strategy/adoption
    - reliability/pre-flight
    - engine/base-facts
summary: 'This directive expands the v0.17.0 scope by three additions within the engine-first focus (20260824-093559-d-tac-onl): the writing guide''s JSON-escaping repair as a third direct fix (20260827-224853-s-tac-giv), the ref-kind vocabulary base fact (20260826-090801-d-tac-got), and pre-flight''s migration onto that fact with a loud-failing load (20260824-165218-s-tac-7w6). Pulling the ref-kind cluster into v0.17.0 ensures the legacy-render removal never ships against a binary still reading the old path; the fact work was a prerequisite for the pre-flight rewire, so both land together rather than a loud-fail patch alone.'
---

The v0.17.0 release scope grows by three committed additions, sharpening the engine-first focus (20260824-093559-d-tac-onl) in place: the writing guide's JSON-output repair (20260827-224853-s-tac-giv), the ref-kind vocabulary base fact (20260826-090801-d-tac-got), and pre-flight's move onto that fact with a loud-failing load (20260824-165218-s-tac-7w6).

The writing-guide defect joins as a third direct fix because it sits in the release's own trust class: an advisory check rejecting capture reports, observed three times in one session on alpha.3, on the exact write path this release recommends to everyone. The remedy stays as the gap records it — JSON escaping stated once in a static shared partial.

The ref-kind cluster moves up a release. The fact activity carried no release assignment, only the constraint of landing before v0.18.0's render removal; the pre-flight gap left both its candidate fixes unsettled. This directive settles that gap with both candidates together in v0.17.0 — pre-flight renders the vocabulary from the graph fact, and a failed load blocks instead of degrading — so the removal that would make the silent break reachable never ships against a binary still reading the legacy render. A loud-fail patch alone was rejected: it would only convert silent degradation at v0.18.0 into a noisy failure there, while the fact work has to land anyway.

The focus's v0.17.0 list otherwise stands as it names it: bounded automatic serves, the transition messaging, and its two direct fixes — MCP search citations and the config-messaging sweep — remain in scope; its loud-capture-failures plan is already delivered. No other recent finding joins this release.
