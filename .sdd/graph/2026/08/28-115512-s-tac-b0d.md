---
type: signal
layer: tactical
kind: done
closes:
    - 20260827-234805-s-tac-03e
participants:
    - Christopher
confidence: high
topics:
    - engine/base-procedures
    - agent/ux
preflight: skipped
summary: The assemble lane now teaches the full attachment link syntax — the placeholder form, its resolution to the entry's attachment directory at write, the plain-prose requirement (a link inside a code span, fence, or indented block reads as documentation, not a live link), and the reverse rule that any other file path belongs in code formatting or draws write-gate warnings. This closes the served-text gap (20260827-234805-s-tac-03e), taking the assemble-lane route over the base-fact alternative; the lane is now the syntax's single authority home, shipped in commits 19e19d99 and cf71ea4f. In the doing, the write path turned out to blindly resolve placeholder examples — it had already mangled the gap's own body at capture — surfaced and recorded as its own gap.
---

The capture procedure's assemble lane now teaches the attachment link syntax, closing the served-text gap 20260827-234805-s-tac-03e. The lane's attachments guidance ends with the link form `[label]({{attachments}}/filename.md)` and its resolution to the entry's attachment directory at write, the plain-prose requirement — inside a code span, fence, or indented block a link reads as documentation of the syntax, not a live link — and the reverse rule that any other file path belongs in code formatting, or it reads as a claim to an attachment the entry does not carry and draws warnings at the write gate. Shipped in commits 19e19d99 and cf71ea4f; the exact wording lives there.

Of the gap's two candidates the fix took the assemble-lane route — finishing the instruction that stopped one sentence short — settled in dialogue; the gap's body carries both candidates and the line they turn on. The lane is now the syntax's single authority home, and the staging tool, the reading tool, and the kind facts keep carrying why and when without it.

In the doing, the write path itself turned out to mangle documented examples of the placeholder — a blind resolution that had already rewritten this gap's own body at capture — surfaced here and recorded as its own gap.
