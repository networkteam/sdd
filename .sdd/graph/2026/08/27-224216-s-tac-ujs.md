---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260826-230218-s-tac-7yr
      kind: surfaced-by
      desc: the slice-two delivery whose extended link check produced the warnings
    - id: 20260409-113337-d-tac-04t
      kind: grounded-in
      desc: the flagged historical entry — read in full, both matches are documentation examples on an entry claiming no attachment
participants:
    - Christopher
confidence: high
topics:
    - reliability/pre-flight
    - implementation/engine
preflight: skipped
summary: The attachment-link check introduced in slice two's delivery fires on documentation examples of the attachment syntax, not only on entries that falsely assert an attachment — producing false warnings against immutable history and an unconditional write block with no override path. The check was surfaced by the slice-two delivery (20260826-230218-s-tac-7yr), and the falsely flagged entry is the decision that established the attachment mechanism itself (20260409-113337-d-tac-04t), which carries no attachments and claims none. A candidate fix is to skip matches inside inline code spans and fenced blocks across both prefix forms the scan uses.
---

The attachment-link check exists to block a body claiming an attachment its entry does not carry, but it also fires on bodies that merely document the ./27-224216-s-tac-ujs syntax — warning on a closed historical entry today, and set to block a future capture whose body cites the syntax as an example. Observed right after the check shipped (20260826-230218-s-tac-7yr): the session framing's graph-health block now reports two broken-attachment-link warnings on the decision that established the attachment mechanism (20260409-113337-d-tac-04t) — one for the generic filename placeholder form it documents, one for the design.md example inside its quoted command; the literal strings live in that entry — quoting them here re-triggers the scan, so this entry describes them instead. Read in full, that entry carries no attachments and claims none: both flagged strings are documentation.

Expected per the check's purpose as delivered: block an entry whose body asserts an attachment the entry does not carry — an entry mentioning the syntax as documentation makes no such claim. The scan cannot tell the two apart: it matches the placeholder prefix anywhere in the body, including inside inline code and fenced blocks, where documentation examples live. On the read surface this is lint noise against immutable history; on the write surface the finding blocks unconditionally — observed live when this entry's own first capture attempt was blocked on its quoted documentation strings and the user's override did not pass it, because the override covers the pre-flight judgment while this check sits in structural validation, leaving no recorded path past a wrong structural finding.

Recorded as reasoning rather than finding, one candidate: skip matches inside inline code spans and fenced code blocks, where example usage lives by convention — the check's other prefix — the on-disk directory form a link resolves to at write time — shares the exposure in principle, so the skip belongs to the scan as a whole, not to one prefix.
