---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260831-195807-s-tac-tje
      kind: builds-on
      desc: the boundary-rebuild done whose flagged live read-back this verification completes
closes:
    - 20260811-234304-s-tac-ual
participants:
    - Christopher
confidence: high
topics:
    - llm/observability
    - evaluation/outer/smoke-test
summary: 'A live verification on a real engine session confirms capture-time LLM completions now record: reconnecting the sdd MCP server on the rebuilt binary produced attributed rows in `.sdd/stats/llm.jsonl` for all three chat purposes, including the first ever writing-guide row. This closes the engine-recording gap (20260811-234304-s-tac-ual), settling its open why via the act the gap itself prescribed. It completes the flagged live read-back of the boundary-rebuild done (20260831-195807-s-tac-tje), though the cache-floor question stays unmeasured since Ollama reports no cache breakdown.'
---

Live verification on a real engine session confirms capture-time LLM completions now record, closing the engine-recording gap (20260811-234304-s-tac-ual): Christopher reconnected the sdd MCP server on the rebuilt binary and a done capture was driven through it end to end, after which `.sdd/stats/llm.jsonl` holds attributed rows for all three chat purposes of that capture — writing-guide (the first such row ever recorded), preflight, and summarize — each attributed to its provider, model, and variant with token counts and duration.

The act is exactly the resolution the gap named — record completions on this path, then read the numbers — and it settles the gap's open why: the same capture flow on the pre-rebuild binary, minutes earlier, recorded preflight and summarize but no writing-guide row, matching the mechanism analysis in the boundary-rebuild done (20260831-195807-s-tac-tje), whose flagged follow-up this read-back completes. The gap's second candidate, the prompt-prefix cache-floor question, stays unmeasured — the verified run used Ollama, which reports no cache breakdown.
