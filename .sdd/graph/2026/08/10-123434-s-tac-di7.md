---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260730-093011-d-tac-k4q
      kind: grounded-in
      desc: the active rebuild plan deletes the relocation engine and makes maintaining this rollback race test disproportionate
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - reliability/testing
    - dev/ci
    - portability/runtime
summary: A CI run on `main` is red because `TestTargetRollbackFinalUnlinkPreservesSameContentForeignInode` yields `context canceled` instead of the required `identity changed` error, exposing a cancellation-versus-identity race in the session-store relocation machinery. Because the active rebuild plan already commits to deleting the entire relocation subsystem (20260730-093011-d-tac-k4q), fixing the race would spend maintenance on behavior slated for removal; the recommended path to green CI is deleting the test outright, or skipping it only if removal would leave live coverage dark.
---

A GitHub Actions run on `main` is red because `TestTargetRollbackFinalUnlinkPreservesSameContentForeignInode` returned `context canceled` where the test requires an `identity changed` rollback error. The failure occurred on Ubuntu in run https://github.com/networkteam/sdd/actions/runs/31375521439/job/93413756716 after the test cancelled the relocation context immediately after publication, leaving the ordering-sensitive final-unlink assertion exposed.

The failing test protects same-content foreign-inode identity during rollback in the session-store relocation machinery. That entire relocation subsystem is already committed for deletion by 20260730-093011-d-tac-k4q, so repairing its cancellation-versus-identity race would add maintenance to behavior the active plan explicitly removes. The smallest immediate route to green CI is to delete this obsolete test, or skip it only if deletion would obscure still-live coverage, while leaving production relocation behavior untouched until the planned subsystem deletion lands.
