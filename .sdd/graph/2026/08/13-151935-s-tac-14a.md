---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260812-134549-d-tac-9be
      kind: addresses
      desc: the kind-knowledge plan whose completion check this executes; the plan stays open
    - id: 20260811-190704-s-tac-r7h
      kind: builds-on
      desc: the earlier delivery that stored the test cases and three-run protocol this re-runs, and recorded the wrong stranding finding as the before-picture
    - id: 20260813-095836-s-tac-rwd
      kind: grounded-in
      desc: the delivery that put the type-system facts into the writing guide's prompt — what this run measures the effect of
    - id: 20260811-183929-s-tac-fu8
      kind: grounded-in
      desc: 'the instability this result is compared against: the same input producing different findings on every run'
participants:
    - Christopher
confidence: high
topics:
    - graph/entry-craft
    - reliability/testing
    - type-system/kinds
summary: 'A three-run re-execution of the kind-knowledge smoke test confirms the writing guide now produces stable, correct findings across both repository test cases: expected findings appeared on every run, ruled-out findings did not return, and one run demonstrated the guide actively applying the type-system knowledge now in its prompt (20260813-095836-s-tac-rwd). The wrong commit-hash complaint eliminated in the earlier harness run (20260811-190704-s-tac-r7h) stayed gone, and the per-run instability that had made the same input yield different findings each time (20260811-183929-s-tac-fu8) is resolved at the core. Remaining edge noise — severity flips and one-off extras — falls under the per-kind checking work the kind-knowledge plan (20260812-134549-d-tac-9be) leaves for later; this run is the completion check that plan asked for, and it passes.'
---

The writing guide, now judging with the type-system overview fact and the done authoring fact included in its prompt, was re-run over the two real done entries kept in the repository as its test cases — and the result passes the kind-knowledge plan's completion check: on both entries, all three runs found what we expected them to find, and nothing we had ruled out came back.

The first test entry is a done describing a code delivery; on it, every run raised the expected conflation, on exactly the paragraph where dialogue had placed it. The second is a done that only retires a directive whose work was delivered elsewhere; on it, every run raised only the one finding we had agreed to tolerate — and the wrong complaint about missing commit hashes, which every run before the fix had produced (20260811-190704-s-tac-r7h), did not return. One run even reasoned from the settled-directive rules that only the served facts carry, showing the guide actually uses the type-system knowledge in its prompt (20260813-095836-s-tac-rwd) rather than ignoring it.

Before, the same input produced different findings on every run (20260811-183929-s-tac-fu8). Now the core is stable and matches what dialogue had settled as correct. What remains is noise at the edges: a finding's severity flips between runs, and single extras show up in one run and never again. The only consistent extra is a complaint about "wg5", a label the first test entry itself uses without explaining — a fair catch, just beyond what we had settled in advance. Sharpening those edges is the per-kind checking work the kind-knowledge plan (20260812-134549-d-tac-9be) deliberately leaves for later: this re-run is the smoke test its completion check asks for, nothing more. Full run logs are attached; the first run was cut off by a ten-minute test timeout after the first entry finished, so the second entry ran again separately under a longer limit.
