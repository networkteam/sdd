---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260812-134549-d-tac-9be
      kind: addresses
      desc: the plan this pass advances — reading surface, spec reference, workflow capture; it stays open for the remaining kinds, lane teasers, and reconciliation
    - id: 20260814-134331-s-tac-9kn
      kind: builds-on
      desc: the previous run whose attached cold-start probe supplied this pass's work list
    - id: 20260812-160448-d-tac-ymk
      kind: addresses
      desc: the binding-coverage directive the new workflow field lands under — one more capturable field reached through a declared binding
    - id: 20260814-165034-d-cpt-311
      kind: surfaces
      desc: the registry-fact direction this pass's dialogue produced
    - id: 20260814-171302-s-cpt-5zq
      kind: surfaces
      desc: the abilities-extension idea this pass's dialogue produced
    - id: 20260814-234048-d-cpt-zpz
      kind: surfaces
      desc: the no-structured-syntax-in-strings directive distilled from the carrier correction
    - id: 20260814-233547-s-tac-bqt
      kind: surfaces
      desc: the anchor-seed silent drop the re-probe uncovered, verified in code and recorded with its repair option space
closes:
    - 20260814-135103-s-tac-4hs
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - engine/base-facts
    - agent/ux
summary: 'This pass shipped fixes to three surfaces — the show reading surface now demotes embedded body headings (closing the double-heading defect (20260814-135103-s-tac-4hs)), the procedure spec reference gained gate-checkable field pairs, documented dispatch semantics, and a loader-verified worked example, and procedure workflow capture was rebuilt from a YAML-in-string carrier into a typed, schema-advertised value validated by the engine''s own loader on arrival. The workflow field lands under the declared-binding discipline (20260812-160448-d-tac-ymk), the no-structured-syntax-in-strings rule it prompted is recorded as a standing directive (20260814-234048-d-cpt-zpz), and a probe re-run — building on the previous run''s work list (20260814-134331-s-tac-9kn) — moved the cold-start verdict from "guesswork throughout" to "workflow half reliably authorable," while also surfacing the anchor-seed silent drop (20260814-233547-s-tac-bqt). The kind-knowledge plan (20260812-134549-d-tac-9be) stays open for remaining kinds, lane teasers, and reconciliation, and the session''s dialogue produced two further recorded outputs: the registry-fact direction (20260814-165034-d-cpt-311) and the abilities-extension idea (20260814-171302-s-cpt-5zq).'
---

This pass shipped the cold-start probe's findings across three surfaces — the show reading surface now serves procedure workflow frontmatter with embedded bodies demoted beneath its sections, the procedure spec reference now lists the field names a gate can check and documents dispatch semantics, per-type meanings, and a loader-verified worked example, and a procedure workflow is now capturable through the engine as one structured, schema-advertised value gated by the engine's own loader — and a probe re-run confirmed it: the fresh agent's drafted procedure loads.

**The reading surface** (commit 6675bad3). The show envelope carries a procedure's class and params/state/steps on both outlets, so the shipped specs became the worked-example corpus the previous probe found unreachable — and embedded bodies demote beneath the body section, closing the double-heading defect (20260814-135103-s-tac-4hs) on the CLI and the served tool alike.

**The spec reference** (commits 0bb70242, 74e8abec, e82cb41a, 76cba894). All three of the probe's top improvements landed: the field names a gate can check render as field-and-check pairs from the engine's presence map — the one irregular pair made regular by renaming the check itself — sub-procedure dispatch is documented as it actually works, and the skeleton gave way to a worked example authored beside the engine and loaded by its tests. Review-driven follow-ups in the same pass: variable types carry meanings rendered from a new engine description map with the served-schema contract stated, the worked example gained its entry envelope, the three resolve-check docs state that an absent field passes, the render field's description was corrected (it names a body unit), the same-canonical revision claim was cut back to base-procedure scope, unit prose was reworded to the mechanical fact (served verbatim as the step's instructions), and the procedure kind's headline became "Extending the process with workflows".

**Capture writes workflows — built twice** (commits 972c6b9e, aaebe460). The first build carried the workflow as YAML inside a string; Christopher rejected that carrier — every other reported field is a typed, schema-advertised value, and this was the lone exception — so it was rebuilt the same day: a new procedure-spec base type whose hand-written schema fragment advertises the real workflow grammar at the report boundary, converted to the entry's raw YAML nodes through one encode path, with the builtin specLoads gate running the draft through the engine's own loader against the live registry either way. A broken workflow now blocks at capture with the loader's diagnostics, and the syntax-error class inside strings ceased to exist at the boundary. The correction is distilled as its own guiding directive (20260814-234048-d-cpt-zpz), and the field lands under the declared-binding discipline (20260812-160448-d-tac-ymk).

**Checker and hygiene** (commits 28978247, 8c0d5b60, 0a7c2552). The writing guide's shaped-for-the-pull criterion now names the unbroken-block-of-prose anti-pattern — tuned after the guide passed a five-paragraph wall live in this session with the vague clause in front of it. Pre-flight finding keys normalized to lowercase JSON tags, removing the two-casing trap in capture's units. Procedures with broken specs list as broken in the served inventory instead of silently signature-less.

**Verification: probe re-run and editorial review** (both attached in full). A fresh agent under the original cold-start conditions moved the verdict from "No — syntax is guesswork at a dozen points" to "Partly — the workflow half is reliably authorable": the pointer trail held over four hops with zero ability-name guessing, and its drafted spec passed the engine's actual loader after three character-level YAML quoting fixes — exactly the error class the structured carrier removes. Its sharpest finding, verified in code and recorded as its own gap: the engage-to-capture anchor copy silently never lands (20260814-233547-s-tac-bqt). A separate editorial review of the three-fact reading path judged the chain "mostly yes, with three real breaks": the never-delivered registry-pull how-to — unwritable host-neutrally today, honestly answered only by the registry-fact direction the dialogue recorded (20260814-165034-d-cpt-311) — unshown template-placeholder syntax, and an overloaded "seed" story for how a run receives its initial values. The registry side-quest's other outcome, the abilities-extension idea (20260814-171302-s-cpt-5zq), is recorded on its own.

**Left open**: the plan (20260812-134549-d-tac-9be) continues — remaining kinds' authoring facts, lane teasers, reconciliation. The editorial review's text pass on the spec reference is not yet applied: reduce the registry-pull promise, show one placeholder, disentangle the three value channels (caller start, parent copy, default), define the type list's back half, and state that the live registry outranks the printed field-name list. Smaller observations from the doing are carried as mentions only, each becoming its own entry pointing back here if pursued: the unfiltered recent-movement lane, a wall-of-text specimen for the checker's sweep harness, the same-canonical wording in the procedure kind's fact, a search kind-filter returning 1 of 10 procedures, and completion serves echoing already-judged guide findings while being the only outlet for non-blocking pre-flight findings.
