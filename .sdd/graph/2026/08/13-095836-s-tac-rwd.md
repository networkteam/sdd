---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260812-134549-d-tac-9be
      kind: addresses
      desc: the kind-knowledge plan whose framework-floor criterion this realizes; the plan stays open
    - id: 20260811-184407-d-cpt-x7a
      kind: addresses
      desc: the two-facts-per-kind directive whose consumer wiring and framework floor this delivers; its per-kind checking facts stay follow-on
closes:
    - 20260811-183929-s-tac-fu8
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
summary: 'The capture writing guide now reads the type system from the graph: its prompt carries the type-system introduction and the drafted kind''s authoring fact, both resolved from live supersession heads at request time, closing the gap that left every kind-fit judgment to inference over bare tokens (20260811-183929-s-tac-fu8). This delivers the framework-floor criterion and the checker''s consumer wiring committed by the kind-knowledge plan and the two-facts-per-kind directive (20260812-134549-d-tac-9be, 20260811-184407-d-cpt-x7a), though per-kind checking facts and the remaining plan work — construction model, lane teasers, reconciliation — stay follow-on.'
---

The capture writing guide now reads the type system from the graph: its prompt carries the type-system introduction beneath every judgment and the drafted kind's authoring fact ahead of the draft — delivered in commits ba92d542, c47bc198, and 07ca11e8, realizing the framework-floor criterion of the kind-knowledge plan (20260812-134549-d-tac-9be) and closing the gap that left every kind-fit judgment to inference over bare tokens.

The prompt is structured rather than prepended: role and purpose, then the framework's own meaning — the overview fact inlined as calibration for the five axes, never additional checks — then the criterion and checks, the drafted kind's fact, and the draft last. Both facts render from the loaded graph at request time through a source that resolves to live supersession heads, so a project override wins and the served words track the graph with no manual sync; a missing fact fails the check loud rather than degrading to a silently absent section. The kind slot stays empty for kinds without an authoring fact, so the guide's kind knowledge grows as each fact ships — the checker is now a consumer of the kind-fact pool, the first prompt element the two-facts-per-kind directive commits (20260811-184407-d-cpt-x7a); its per-kind checking facts stay follow-on, so that directive stays open.

The doing folded in two review corrections: the heading demotion for inlined fact bodies reuses the existing markdown composition package instead of the duplicate first written, and review discussion produced test-convention work — the exported-surface testing rule and a severity-aware lint wrapper — recorded as repository guidance outside this entry. Still open on the plan: the construction model, the remaining kinds' facts and lane teasers, reconciliation — and the completion check this delivery unblocks, re-running the banked specimen harness against the recorded baselines.
