---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260812-134549-d-tac-9be
      kind: addresses
      desc: the kind-knowledge plan whose first slice's knowledge half this completes; the plan stays open
    - id: 20260812-180000-s-prc-typ
      kind: surfaces
      desc: the delivered fact itself — the indexed type-system overview this act produced
    - id: 20260728-084122-s-cpt-eff
      kind: addresses
      desc: the capture-side steer it left open, whose guiding-directive half the fact now delivers on the engine path; open for the frozen legacy copies
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
summary: The type-system overview fact (20260812-180000-s-prc-typ) is now graph-resident and indexed, with kind lists generated from the model enumeration and two discriminators corrected — aspiration versus directive by force, and contract as a guiding directive — completing the knowledge half of the first slice of the kind-knowledge plan (20260812-134549-d-tac-9be), which stays open for the construction model, writing-guide floor, authoring facts, and legacy copy reconciliation. The contract ruling delivers the guiding-directive half of the capture-side steer (20260728-084122-s-cpt-eff) on the engine path only, leaving the frozen legacy copies open; always-on priming is declined, so the plan's index-and-teaser default stands.
---

The type system's introduction is now graph-resident: the overview fact (20260812-180000-s-prc-typ) ships embedded and indexed, its kind lists generated from the model enumeration, and the capture procedure's kind step points at it instead of carrying its own discrimination text — delivered in commit 112c0da4 against the kind-knowledge plan (20260812-134549-d-tac-9be), completing the knowledge half of its first slice. It was consolidated positive-side first from the capture procedure's hand-carried text, the session shell's served kind list, and the concept reference's tables and tests, then audited against the capture-time validation rubrics that hold the same knowledge in reverse, and frozen only after review by Christopher.

Two taught discriminators were corrected rather than copied, following what the graph had already settled — the full reasoning lives in the fact itself: aspiration versus directive now discriminates by force rather than completion criterion, and the contract kind takes no new captures, a standing constraint being a guiding directive. The contract steer, ruled by Christopher in this run, delivers the decoupled guiding-directive half of the capture-side steer that 20260728-084122-s-cpt-eff left open — on the engine path only, the legacy skill copies staying frozen under the plan, so that gap stays open for reconciliation. The reach call the plan required is made and recorded here: always-on priming is declined — the fact cannot meet the tersest-of-all bar — so the plan's committed default stands, index-and-teaser.

The doing reshaped the work twice at review: the fact became the type system's introduction rather than a choosing aid alone, and the kind tables became the generated block — rendered from the declared enumeration with an authored question per kind, so a newly declared kind fails the build until its question is written. The default-kind column was dropped as capture mechanics rather than kind knowledge. Still open on the plan: the construction model, the writing-guide floor, the remaining kinds' authoring facts and their lane teasers, and the reconciliation of the inventoried copies.
