---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260826-172134-s-tac-3yc
      kind: builds-on
      desc: slice one's static-serve delivery; this act completes the plan's remaining criteria on top of it
closes:
    - 20260826-120330-d-tac-8f8
    - 20260821-105247-d-tac-q1x
    - 20260826-003953-d-tac-ktg
    - 20260814-233547-s-tac-bqt
    - 20260811-233331-s-tac-bjn
    - 20260811-232108-s-tac-kus
    - 20260826-000339-s-tac-fyi
    - 20260707-175502-s-prc-lgu
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - implementation/mcp
summary: 'Slice two of the capture-efficiency plan is shipped and completes it: the capture path now fails loudly at all four silent-drop boundaries, playback serves engine-computed deltas after the first full round, the correctable field set covers attachments, entryKind, and layer, and body edits travel as atomic search-replace patches. Building on slice one''s static-serve dedup (20260826-172134-s-tac-3yc), this delivery closes the capture-efficiency plan (20260826-120330-d-tac-8f8), the loud-boundaries plan (20260821-105247-d-tac-q1x), and the playback-economy directive (20260826-003953-d-tac-ktg), along with each individual gap they bundled: the anchor seed drop (20260814-233547-s-tac-bqt), the declared-field silent discard (20260811-233331-s-tac-bjn), playback''s uncorrectable field set (20260811-232108-s-tac-kus), the full-draft re-serve on every edit (20260826-000339-s-tac-fyi), and the orphaned-attachment write-gate gap (20260707-175502-s-prc-lgu).'
---

Slice two of the capture-efficiency plan is shipped and completes the plan: the capture path now fails loudly at every boundary that used to drop confirmed values, the correctable field set covers what a revision needs, playback serves engine-computed rounds instead of re-serving the draft whole, and small edits travel as atomic patches. Delivered on branch worktree-capture-slice2-8f8 in commits b3f941fe, a342c97a, d781c008, cc7f2ab4, 1e5e3df4, acdcd6bb, b0dfc73c, and 5093247c, each with regression tests at the layer owning what it asserts. Slice one delivered the static-serve dedup criteria (20260826-172134-s-tac-3yc); this act delivers every remaining criterion, so it closes the plan (20260826-120330-d-tac-8f8) together with the loud-capture-path plan answered inside it (20260821-105247-d-tac-q1x) and the play-back refinement both realized (20260826-003953-d-tac-ktg).

**The four loud boundaries, each closing its gap.**

- A dispatch seed that cannot land fails the child start naming the source field, the target field, and the target's actual declaration. Capture's anchor moved from params to state so the shipped seeds land, and the new guard immediately surfaced a second live instance — engage seeding explore, which declared no anchor at all — repaired in the same cut (closes 20260814-233547-s-tac-bqt).
- A declared state field riding a chooser answer at the top level is refused naming the field and where it should have gone, mirroring the undeclared-field refusal. The drop site was the answer-envelope routing, which forwarded only the nested fields object (closes 20260811-233331-s-tac-bjn).
- An entry whose body claims an attachment it does not carry is blocked at the write gate. The model-layer link check existed but was inert on the engine path: the draft-to-entry assembly neither populated the attachment list nor resolved the {{attachments}} placeholder. Both now happen before the gate — which also ends MCP-captured bodies landing with the literal placeholder — and the LLM pre-flight validation gains a check that reads the body against the entry's attachment filenames and blocks a prose claim of an attachment the entry does not carry. The staged-file reconciliation the gap names first stays unbuilt, per the scope settled in 20260821-105247-d-tac-q1x, closed here (closes 20260707-175502-s-prc-lgu).
- Playback's blindness is gone on both halves: the confirm round serves the entry whole on first presentation, and attachments, entryKind, and layer join every revision path's collectable set, so a fault found at playback costs an edit, not an abandon. A kind change re-enters assemble's ordinary gate, where state licensed only by the superseded kind is refused as a named finding and clears by a null report (closes 20260811-232108-s-tac-kus).

**The serving economy.** Steps declare the fields the engine renders as an owned draft block: whole on the first round — small fields complete, the body as a bounded head-and-tail excerpt of text the agent authored moments before — and after that only what changed: scalars whole, lists item-level, prose as a content-anchored unified-style diff without line numbers, based on what the engine last served this instance, with a round that changed nothing saying so in one line. The base is in-memory per instance, so resume and whole-position replay serve full state by construction and reset it (closes 20260826-000339-s-tac-fyi). Prose is a new declared engine type; declaring a field prose is what selects diff treatment, and capture's body and widenReport now declare it.

**Patch reports.** A body edit reports as ordered unique-match search-replace pairs applied atomically — a failing pair refuses the whole report naming itself, full re-report stays valid, and the report event logs the patched result so replay re-writes rather than re-applies. Staged attachments follow the same shape: edited in place by handle through stage_attachment's new patches mode and read back in bounded pages through read_attachment's session-plus-handle addressing, reachable before any entry carries them. The MCP tool contract snapshot was bumped deliberately for the two extended schemas.

**From the doing.** The first round's diff-only-where-the-engine-changed-it arm is empty today because the store never alters a reported body; the bounded acknowledgment carries the round until normalization exists to diff against. The resolved target-branch statement stayed in the playback unit rather than moving into the draft block, keeping the engine's answer to branch precedence explicit per round.
