---
type: decision
layer: tactical
kind: directive
refs:
    - id: 20260821-123832-d-tac-ip1
      kind: refines
      desc: adds the disconnect fix to the v0.18.0 scope this plan carries, leaving its own criteria untouched
    - id: 20260824-093559-d-tac-onl
      kind: grounded-in
      desc: the engine-first focus whose end this directive sequences the continuation after
    - id: 20260828-164140-s-tac-mss
      kind: addresses
      desc: the auto-conclude gap whose narrow trigger removal is pulled into v0.18.0
    - id: 20260828-165352-d-cpt-aen
      kind: grounded-in
      desc: the capability rebuild sequenced as the first arc after the focus
    - id: 20260827-232430-d-cpt-voa
      kind: related
      desc: the engine-side half of the delegation arc sequenced second
    - id: 20260829-122157-d-cpt-zpe
      kind: related
      desc: the harness-side half of the delegation arc sequenced second
participants:
    - Christopher
confidence: high
intent: pending
topics:
    - implementation/engine
    - strategy/adoption
    - portability/mcp
summary: 'This directive keeps the engine-first releases'' committed scope intact and adds exactly one item to v0.18.0: a connection close never concludes a session, the narrow trigger removal from the auto-conclude gap (20260828-164140-s-tac-mss). It refines the retirement plan''s v0.18.0 scope (20260821-123832-d-tac-ip1) without touching its criteria, and, after the engine-first focus ends (20260824-093559-d-tac-onl), sequences the capability rebuild (20260828-165352-d-cpt-aen) first — kept out of v0.18.0 since its implementation plan doesn''t exist — with the delegation pair, engine-side (20260827-232430-d-cpt-voa) and harness-side (20260829-122157-d-cpt-zpe), building on the cleaned surface. Fulfilled once v0.18.0 ships with the fix and the capability implementation plan exists.'
---

The engine-first releases keep their committed scope, v0.18.0 gains one narrow fix, and the session-capability and delegation arcs follow after the focus ends, capability first.

Nothing here adds to v0.17.0. v0.18.0 gains exactly one addition: a connection close never concludes a session, the narrow trigger removal from the auto-conclude gap (20260828-164140-s-tac-mss). v0.18.0 declares the engine the only flow, and shipping that declaration while a routine reconnect destroys a quiescent dialogue would undercut the trust the transition messaging asks users for. The fix is small and additive, and the capability directive's later map deletion subsumes it.

The capability rebuild itself stays out of v0.18.0. Its implementation plan does not exist yet, and binding unplanned subsystem work to a named release inflates the release, while the retirement plan's remaining work is bounded and gated only by the parity verdict it already declares (20260821-123832-d-tac-ip1). It should ship close on v0.17.0's heels, while the deprecation messaging users just saw is fresh.

After the engine-first focus ends (20260824-093559-d-tac-onl), the committed continuation is capability first, then delegation. The capability rebuild (20260828-165352-d-cpt-aen) deletes the connection map, the consent arguments, and the MCP listing, machinery the delegation work would otherwise have to build around, so planning starts with the capability slices, and the delegation pair, the engine-side fork and delivery (20260827-232430-d-cpt-voa) with the harness-side dispatch artifacts (20260829-122157-d-cpt-zpe), builds on the cleaned surface.

Fulfilled when v0.18.0 ships carrying the disconnect fix and the capability implementation plan exists.
