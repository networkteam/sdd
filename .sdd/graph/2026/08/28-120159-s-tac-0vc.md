---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260828-115512-s-tac-b0d
      kind: grounded-in
      desc: 'the live verification: this entry''s write on the rebuilt binary kept its backticked placeholder example literal'
closes:
    - 20260828-115857-s-tac-z0i
participants:
    - Christopher
confidence: high
topics:
    - implementation/cli
    - reliability/testing
    - evaluation/outer
summary: The attachment placeholder resolver was rewritten in commit 1e53118e to walk a masked copy of the entry body — the same mask the link checker uses — replacing only placeholders visible outside code regions, closing the blind-replacement gap (20260828-115857-s-tac-z0i). Three failing tests pinned survival of placeholders inside inline code spans, fenced blocks, and indented blocks before the fix. Live verification via the served-syntax done (20260828-115512-s-tac-b0d) confirmed the rebuilt binary preserves backticked placeholder examples literally at write.
---

The attachment placeholder resolver was rewritten, test-driven in commit 1e53118e, to read code regions as documentation, closing the blind-replacement gap 20260828-115857-s-tac-z0i. Three cases pinned survival of a placeholder inside an inline code span, a fenced block, and an indented block — all failing before the change — then the resolver was rewritten to walk the same masked copy of the body the link check uses, replacing only occurrences the mask leaves visible.

Verified live beyond the tests: the served-syntax done (20260828-115512-s-tac-b0d) was written through the rebuilt binary with a backticked placeholder example in its body, and the stored entry keeps it literal.
