---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260811-141038-d-tac-rvu
      kind: addresses
      desc: the rollout directive whose wiring half this delivers; the specimen sweeps stay open, so the directive is not closed
    - id: 20260810-141555-d-cpt-20r
      kind: grounded-in
      desc: the capture-guide architecture whose writing-guide half is now live; the graph-fit guide and mechanical write gate remain ahead
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - graph/entry-craft
summary: The capture writing guide is now live, judging every draft once between assemble and playback across five axes — stranding, dilution, conflation, pointing, form — returning reasoning-first findings with no gate on either severity level, while the legacy pre-flight narrows to graph-facing checks only; delivered in commits f358c5a3, 61ba857e, and b72c6e88. This completes the immediate wiring half of the rollout directive (20260811-141038-d-tac-rvu), which remains open as the per-kind specimen sweeps continue; the writing-guide half of the capture-guide architecture (20260810-141555-d-cpt-20r) is now live, with the graph-fit guide and mechanical write gate still ahead.
---

The capture writing guide is live: the first general guide now judges every capture once between assemble and playback, the legacy pre-flight keeps only graph-facing checks, and a seed eval pins the settled calibration invariants — the wiring the rollout directive (20260811-141038-d-tac-rvu) committed to do immediately, delivered in commits `f358c5a3`, `61ba857e`, and `b72c6e88`.

The delivered guide judges a draft in isolation against five axes — stranding, dilution, conflation, pointing, form — and returns findings that lead with reasoning, so the drafting agent receives the why behind each finding rather than a verdict to obey; severity carries two levels (substantive, minor) with nothing gating on either. The guide runs once per capture: the agent folds its findings in by judgment and the revised draft goes to playback without a second pass, because a re-run judges the repairs instead of the draft and drifts into stretching for findings an already-good draft does not warrant — re-running is an explicit agent choice reserved for a substantially reworked draft. The capture procedure's own text carries that flow — an agent junction, no gate, no override — and the mechanics beyond it live in the delivering commits. The pre-flight's writing-quality block is removed — the guide's form axis owns that ground — while every graph-facing check and the write gate's semantics stand unchanged until the graph-fit guide exists per the capture-guide architecture (20260810-141555-d-cpt-20r).

Verified three ways: the test suite covers parsing, prompt stability, and the procedure's clean-pass, findings-review, and recheck paths; a live seed eval passed every run of every case — a clean draft returned zero findings on all runs while stranded and conflated drafts fired their axes with reasoning argued in pull terms, the full run kept as this entry's attachment; and this entry is itself the first live capture through the guide — its first attempt looped three rounds of escalating findings, which is the observation that settled the once-per-capture rule now shipped. What stays open, and why this done closes nothing: the rollout directive's own calibration path — the per-kind specimen sweeps and the banking of what they teach, as that directive defines them — which continues against live capture traffic like this entry.
