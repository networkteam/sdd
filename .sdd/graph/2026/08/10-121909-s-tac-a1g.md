---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260703-200000-d-prc-evl
      kind: grounded-in
      desc: the evaluate procedure this two-lens run followed — scoped lenses, evidence beside each judgment, the evaluation recorded as work the graph can see
    - id: 20260731-083858-s-tac-dmk
      kind: builds-on
      desc: the shipped supersede-aware ranking this run evaluates on both lenses
    - id: 20260729-111657-d-cpt-x6z
      kind: grounded-in
      desc: the directive that deferred the hop factor to comparative findings on the live graph, and whose comment-duplication this run fixed; the sweep it asked for was produced here
    - id: 20260517-180149-d-tac-cpw
      kind: related
      desc: the sibling ranking directive whose closure still asks for comparative findings through sensitivity analysis — the precedent this run's methodology and findings attachment follow
    - id: 20260730-171311-d-cpt-0cv
      kind: related
      desc: the specimen read in the live lane during outer validation — the corrected delivery rule that now leads the guiding-directive lane
    - id: 20260716-155256-s-prc-686
      kind: grounded-in
      desc: 'names the limit this evaluation runs into: the outer pass was driven by the implementer, so it inherits the assumptions a foreign reader would not'
participants:
    - Christopher
confidence: high
topics:
    - evaluation/inner/code-review
    - evaluation/outer
    - cli/view
    - type-system/refs
summary: 'A two-lens evaluation of supersede-aware ranking (20260731-083858-s-tac-dmk) finds the work sound: inner verification confirms correct placement and negligible construction cost; outer validation confirms the live lane orders correctly, with (20260730-171311-d-cpt-0cv) now leading where a reader would look for it. A comment-duplication miss against (20260729-111657-d-cpt-x6z) was fixed in place; two consequential findings — on in-degree redefinition and hop-factor sensitivity — are carried out as separate entries. The outer pass inherits the implementer-assumption limit named by (20260716-155256-s-prc-686) and warrants a second reader.'
---

A two-lens evaluation of supersede-aware ranking (20260731-083858-s-tac-dmk) finds the work sound and serving its purpose, fixes a comment-hygiene miss in place, and carries two consequential findings out as their own entries. Inner covered guideline conformance plus three things the unit tests do not reach — the in-degree double-count, the supersede-fork tie-break, and graph-construction cost. Outer covered live-lane behaviour plus the sensitivity sweep the shipping directive deferred. Nothing here argues for reverting anything. Every measurement is a dated snapshot of a moving graph and lives in the attached findings record rather than in this text, which keeps only the verdicts that outlast the numbers.

**Inner — sound, and cheap.** The change sits entirely in `internal/model` with `internal/finders/ranking.go` untouched and the public layout grammar unchanged, which is the push-logic-down and CQRS placement `AGENTS.md` requires. The resolved index is built in the single graph constructor both production and tests reach, preserving the cross-repo exclusion of its sibling indexes. Construction cost was measured rather than assumed and is a non-issue: the overwhelming majority of inbound references point at a live entry and terminate the chain walk immediately, so the per-reference resolution is effectively free at this graph's size and degrades only with the density of supersession, not with the number of entries.

**Inner finding, fixed rather than recorded.** Two comments paraphrased calibration reasoning that 20260729-111657-d-cpt-x6z already owns — the hop factor's arithmetic and the heat doc's inheritance rationale — which the sharpened comment-hygiene rule in `AGENTS.md` names as duplication, and which was guaranteed to go stale on the first recalibration that same directive demands. Christopher judged a gap capture for this over-ceremony; it was fixed in commit dde7ac18 and merged to main as 68cec804, cutting the doc comments roughly in half by citing the entry instead of restating it. Vet, model tests, and lint clean after.

**Inner finding, carried out as its own entry: resolution silently redefined in-degree.** In-degree used to mean *how many entries point at me*; after resolution it means *how many references land on me*, and those diverge whenever one entry cites both a predecessor and its successor. Every instance found in the live graph was a signal describing a supersession — which naturally references both the retired entry and its replacement — so the shape recurs rather than being accidental. Someone has to rule on whether resolution deduplicates by source entry, which is a lifecycle this prose cannot carry, so it is recorded separately.

**Inner finding, kept here: the fork tie-break is incidental.** `ResolveRef` follows the first superseder, and that ordering comes from the loader's file-walk order rather than from anything chosen, so a forked entry's inherited attention lands on a branch by accident. The one fork in the live graph has both branches closed, making it settled rather than live, which is why `sdd lint` is correctly silent about it. Nothing observable is wrong — the finding is that the tie-break is unspecified, not that it misbehaves — so it obligates no one and stays in this record rather than becoming an entry someone would have to close.

**Outer — it works in use.** Read as someone deciding what to work on, the shipped lane is right: 20260730-171311-d-cpt-0cv, the corrected delivery rule, leads the guiding-directive lane where a reader would look for it, having been invisible there under the old behaviour. The entries it displaces each fall by a single rank and stay at the top, so nothing reads as wrongly demoted. The two lanes without supersede chains are untouched in practice and not merely in theory — active-and-hot returns identical scores at every hop factor, and open-loops is invariant by construction, because coldness divides by undamped in-degree and the factor cannot reach it.

**What the sweep settled, and what it could not.** The comparative findings 20260729-111657-d-cpt-x6z deferred were produced: binaries differing only in the hop factor, run against the live graph across three lanes. The evidence strongly supports inheriting at all — turning inheritance off drops the affected entry several ranks down its lane — and it does not discriminate the shipped factor from full undamped inheritance, which leave the lane ordering all but identical. The affected population is too small to be a sample. That verdict changes what should happen next rather than merely describing the present state, so it is carried out as its own entry rather than left in this prose. The methodology follows the precedent 20260517-180149-d-tac-cpw sets for defending a calibration number in this graph: build the variants, compare the live lanes, attach the findings.

**A limit on this evaluation itself.** The outer lens was run by the same agent that implemented the work, which is exactly the substitution 20260716-155256-s-prc-686 warns about: an implementer-run pass inherits the implementer's assumptions where a foreign one brings its own. The measurements are mechanical and reproducible by anyone from the attached record, but the judgment that no entry looks wrongly demoted is mine about my own change, and deserves a second reader before it is treated as settled.
