---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260824-093559-d-tac-onl
      kind: addresses
      desc: delivers the first of its two direct fixes; the config-messaging sweep stays open
    - id: 20260705-010751-d-tac-dbk
      kind: related
      desc: the serve-size decision whose header-only search default this act deliberately revises; its hit cap stays
closes:
    - 20260814-160042-s-tac-rst
participants:
    - Christopher
confidence: high
topics:
    - implementation/mcp
summary: Commit 438a88d6 ships a unified default of one citation per entry across both the MCP and CLI search surfaces, so MCP search now returns the highest-scoring chunk rather than collapsing to headers-only — closing the blind-surface gap (20260814-160042-s-tac-rst). This deliberately revises the header-only drill default set by the engine-completion directive (20260705-010751-d-tac-dbk), while preserving its hit cap of 8; an explicit 0 still suppresses citations. It delivers the first of the two direct fixes carried by the release focus (20260824-093559-d-tac-onl), with the config-messaging sweep still open.
---

MCP search now answers with match evidence by default: both surfaces share one default of a single citation per entry — the highest-scoring chunk — shipped in commit 438a88d6 and closing the blind-surface gap (20260814-160042-s-tac-rst).

`DefaultMaxCitationsPerEntry` drops from 3 to 1 and both surfaces substitute it when the caller leaves the cap unset; an explicit 0 keeps headers-only. Christopher chose the value in dialogue: one line shows why a hit matched without widening serves back to the old three-citation verbosity.

This deliberately revises the header-only drill default the engine-completion directive pinned for serve size (20260705-010751-d-tac-dbk); the closed gap records the live failure that made that default untenable. The hit cap of 8 stays, and the search-defaults test and tool-contract snapshot pin the new behavior.

Out of scope, as the gap itself scoped them: auto-mode and citation byte offsets for direct passage access stay where the closed gap points them. This delivers the first of the two direct fixes the release focus carries (20260824-093559-d-tac-onl); the config-messaging sweep remains.
