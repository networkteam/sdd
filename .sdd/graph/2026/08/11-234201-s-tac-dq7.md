---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260730-093011-d-tac-k4q
      kind: addresses
      desc: the rebuild plan this completes and retires, criterion by criterion
    - id: 20260730-105153-s-tac-tia
      kind: grounded-in
      desc: the slice-1 delivery holding the evidence for criteria 1, 3 and 4
    - id: 20260731-080621-s-tac-jkw
      kind: grounded-in
      desc: the slice-2/3 delivery holding the evidence for criteria 3 and 8 through 12
    - id: 20260810-145745-s-tac-sux
      kind: builds-on
      desc: the slice-4 delivery holding the evidence for criteria 2, 5, 6, 9 and 14, and the signal that named the two remainders this run closes
    - id: 20260712-194427-d-tac-wjq
      kind: addresses
      desc: realizes its parity clause — the same conformance suites exercising local and external adapters, not local ones alone
    - id: 20260728-004924-s-tac-gtx
      kind: addresses
      desc: the structural sweep gap whose repair this checks at the boundary where its original composition-side clause failed
    - id: 20260726-204112-s-prc-ogp
      kind: grounded-in
      desc: 'the bar criterion 7 is met against: observation in real use, never inspection over the artifact'
closes:
    - 20260730-093011-d-tac-k4q
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - portability/mcp
    - reliability/testing
summary: 'Commit `0c65f1ba` completes the session-subsystem rebuild by closing its final two open criteria: an external Go composition now passes the session and staged-blob conformance suites including collection and drives the collection pass through the exported API over storage it owns, and criterion 7 is met by Christopher''s repeated live observation of the unprompted wrap offer, corroborated by six such offers found in transcripts — the real-use standard (s-prc-ogp) — though not uniformly, since three session tails made no offer and one concluded without asking. This retires the rebuild plan on its full fourteen-criterion set (d-tac-k4q), with the evidence for twelve of them left in the prior slice deliveries (s-tac-tia, s-tac-jkw, s-tac-sux) rather than restated; the external run also delivers the binding form of the parity clause (d-tac-wjq) and verifies collection at the composition boundary where the structural sweep gap first went unbuilt (s-tac-gtx).'
---

The session-subsystem rebuild is complete and its plan retired: commit `0c65f1ba` closes the last two open criteria of 20260730-093011-d-tac-k4q, making an external Go composition pass the session and staged-blob conformance suites including collection and drive the collection pass through the exported API over storage it owns entirely.

The plan is retired on its full criterion set. Each criterion is listed with where its evidence lives, which for twelve of the fourteen is a delivery signal of this plan rather than anything restated here:

1. An earlier binary's session is listed and resumable with no migration step, and later appends stay in the log where it was found — 20260730-105153-s-tac-tia.
2. The binary is replaced under running sessions with no stop, acknowledgement or offline window — 20260810-145745-s-tac-sux, by a real swap over one store.
3. An unreadable log is skipped with every other session still listed, and is never removed for being unreadable — s-tac-tia for the listing half, which fixed a listing that aborted on the first undecodable log; 20260731-080621-s-tac-jkw for the pass rule.
4. No session-store flag or prompt, no relocation or pending-transition notice, no tombstone, and a session under a superseded identity simply absent — s-tac-tia, which deleted the relocation machinery outright.
5. Concluding ends the session naming each thread left behind, and its log stays readable until retention expires — s-tac-sux.
6. The door mints a new handle, no operation revives a concluded session, and a running session resumed by handle returns at its pending step — s-tac-sux.
7. A real dialogue where the agent offers to end and names what is open, and whoever simply stops still finds the session listed — this run, below.
8. A real accumulated store leaves the ended-and-expired gone with their blobs, the recently ended readable, the unended untouched, and nothing outstanding — s-tac-jkw against copies of this machine's store, extended by s-tac-sux, whose read-side recovery of unrecorded endings made fifty sessions collectable where one had been.
9. Retention is configurable through the existing configuration surface and takes effect on the next start — s-tac-jkw for `sessions.retention` on the existing overlay, s-tac-sux for the parsing fix that made a value expressed in days mean what it says.
10. A composition running the same collection through the exported API produces the same removals as a local start — s-tac-jkw established the exported API as the second caller over one command; this run proves an external composition drives it.
11. Two processes starting simultaneously both complete with nothing left half-removed — s-tac-jkw.
12. Nothing is removed that has not ended, is inside its window, or is still claimed; an orphaned staging area is removed; an unreadable payload stops that session rather than the run — s-tac-jkw.
13. An external Go composition passes the conformance suite including collection — this run, below.
14. The effective target branch is the branch the write lands on across every combination, with an explicit work branch honored on both sides — s-tac-sux.

**Criterion 13, and 10's external half.** The suites already demanded what collection reaches through, but only the local adapter ran them: the external example proved compilation alone on no-op stubs added by commit `b1881f30`. Real in-memory stores now replace those stubs, run both suites from outside the module, and back a test driving `CollectSessions` against composition-owned storage. This is the binding form of the parity clause in 20260712-194427-d-tac-wjq — the same suites exercising local and external adapters — and the direct check on 20260728-004924-s-tac-gtx, whose sweep was first assigned to composition-side work no composition ever built. Trust rests on mutation testing rather than a green run: a no-op `DeleteStaged`, a non-idempotent `Delete`, and a disabled CAS comparison each fail the suites. Both modules verify clean, and CI already runs the example module, so the conformance holds continuously.

**Criterion 7.** Met by observation, as 20260726-204112-s-prc-ogp requires of a criterion whose truth lives outside the artifact: Christopher confirms seeing the unprompted wrap offer repeatedly, and a transcript sweep finds six between 2026-07-31 and 2026-08-11, each naming what was still open in plain words. The verdict is met but not uniform — three session tails made no offer and one concluded without asking, an inconsistency that qualifies the verdict and warrants its own signal.
