---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260828-115512-s-tac-b0d
      kind: surfaced-by
      desc: the done whose drafting needed a literal placeholder in the body and so exposed the resolver's blind replacement
    - id: 20260827-232249-s-tac-pxn
      kind: grounded-in
      desc: 'the masked check that set the expectation: a placeholder inside code is documentation, not a live link'
    - id: 20260827-234805-s-tac-03e
      kind: grounded-in
      desc: the written entry carrying the observed damage — its backticked placeholder example left capture as a resolved path
participants:
    - Christopher
confidence: high
topics:
    - implementation/cli
    - graph/entry-craft
summary: The write path resolves attachment placeholders by plain string replacement across the entire entry body, mangling documented syntax examples in code spans — a discrepancy surfaced during drafting of the served-syntax done (20260828-115512-s-tac-b0d) and already visible as damage in the gap entry's own written body (20260827-234805-s-tac-03e). The masked attachment-link check (20260827-232249-s-tac-pxn) establishes the expected behaviour — placeholders inside code regions are documentation, not live links — but the write path applies no equivalent mask, silently overwriting at the one moment an immutable entry is committed.
---

The entry write path resolved attachment placeholders blindly, mangling documented examples of the syntax: the resolver replaced every occurrence in the body with the entry's attachment directory path by plain string replacement, code regions included. Observed by code inspection while drafting the done for the served-syntax gap (20260828-115512-s-tac-b0d), and visible in that gap's own written body (20260827-234805-s-tac-03e): a backticked placeholder example left capture as a resolved `./…` path.

Expected, from the masked attachment-link check (20260827-232249-s-tac-pxn): a placeholder inside a code span, fence, or indented block is documentation of the syntax, not a live link — the check reads past it, and the write path should read the body the same way. The deviation is the two surfaces disagreeing on one body: the check preserved as documentation what the resolver then rewrote, silently and at the only moment an immutable entry is written.
