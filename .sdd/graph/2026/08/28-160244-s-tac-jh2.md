---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260828-145055-d-tac-ruz
      kind: addresses
      desc: delivers the last of its three committed additions
    - id: 20260811-184407-d-cpt-x7a
      kind: grounded-in
      desc: 'the consumer pattern this migration follows: framework knowledge rendered from graph facts'
    - id: 20260828-155447-s-tac-ilv
      kind: builds-on
      desc: the finished act that shipped the fact this load resolves
closes:
    - 20260824-165218-s-tac-7w6
participants:
    - Christopher
confidence: high
topics:
    - implementation/cli
    - reliability/pre-flight
summary: Pre-flight now reads its ref-kind vocabulary from the graph fact shipped in the preceding act (20260828-155447-s-tac-ilv) and errors loudly on a missing or empty load, removing the last bundled-skills read and closing the silent-break gap (20260824-165218-s-tac-7w6). The migration follows the consumer pattern the writing guide established (20260811-184407-d-cpt-x7a), and a regression test pins both failure modes — missing fact fails loud, present fact renders correctly into the rubric. This delivers the third and final addition committed by the release amendment (20260828-145055-d-tac-ruz).
---

Pre-flight now renders its ref-kind vocabulary from the graph's ref-kind fact and blocks when the load fails, shipped in commit 0da9bc90 and closing the silent-break gap (20260824-165218-s-tac-7w6).

The load resolves the fact delivered by the previous act (20260828-155447-s-tac-ilv) to its live supersession head and errors on an unresolvable or empty body, so a graph without the fact stops the check instead of rendering the ref-meta rubric with an empty vocabulary. The read of `references/ref-kinds.md` from the bundled-skills store is deleted, removing pre-flight's last dependency on the legacy render before v0.18.0 removes it. This is the consumer pattern the writing guide established (20260811-184407-d-cpt-x7a), and the rubric's own prose now names the graph fact rather than the skill as the single source.

On the test side, unit tests whose graphs reach pre-flight now carry a stub of the fact, standing in for what every real graph gets automatically: base facts ship embedded in the binary and merge into each graph at load. A regression test pins both halves of the fix: a missing fact fails loud, and a present one renders into the rubric with its headings lowered to sit under the rubric's own section.

With this, all three additions of the release amendment (20260828-145055-d-tac-ruz) are delivered.
