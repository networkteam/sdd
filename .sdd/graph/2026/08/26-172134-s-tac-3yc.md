---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260826-120330-d-tac-8f8
      kind: addresses
      desc: partially realizes the plan — the static-serve dedup criteria delivered, the playback, patch, loudness, and correctability criteria left to slice two
closes:
    - 20260826-003425-d-tac-87o
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - portability/mcp
summary: 'Slice one of the capture-efficiency plan is shipped: static-serve repetition is eliminated at steady state, with step serves composing from named lanes that dedup independently, report schemas arriving as compact stubs after first receipt, and topics reduced to bare sorted labels that dedup like every other lane. This partially realizes the plan (20260826-120330-d-tac-8f8), with playback economy, patch reports, loudness boundaries, and correctability criteria remaining for slice two. All four consequences of the named-lanes directive are now in code — including the topics exception reversed to uniform dedup — retiring that directive (20260826-003425-d-tac-87o).'
---

Capture's static-serve repetition is gone at steady state: engine step serves now compose from named lanes the served-once memory dedups independently, a report schema already received arrives as a compact stub, and capture's topics serve as bare sorted labels. Shipped as slice one of the capture-efficiency plan (20260826-120330-d-tac-8f8) on branch worktree-serve-dedup-8f8, commits c7b35141, a76df51d, and e6a6639b — advancing the plan without closing it. Delivered of its criteria: the report schema deduplicates on canonical bytes, re-served in full only by resume or whole-position replay; step serves compose from declared lane markers with already-served lanes collapsed into one line naming them; every lane dedups by default with whole-position replay as the escape, no lane serving every time; and resume and replay serve full state, untouched. Playback's first-round economy and deltas, body and staged-attachment patch reports, the loud capture-path boundaries, and the widened correctable field set stay open for slice two.

The named-lanes directive (20260826-003425-d-tac-87o) retires because each of its four consequences now exists in the shipped code, as the plan revised them: inject calls carry stable identifiers — duplicates and declared-field shadowing refused at load, the shipped shell's six anonymous framing lanes named in the same cut; the schema is a derived, implicit lane; the per-lane policy landed as the plan's uniform dedup default, the topics exception replaced by bare labels that dedup like every lane; recovery stays whole-position replay, per-lane pulls deliberately unbuilt.

From the doing: base-junction mappings were found recording schema and vocabulary bytes they never deliver — the same silent-drop class the plan commits to eliminating — and were fixed in the first commit; the regression suite pins the guard's two observable halves and records why the strict poisoning flow is unreachable through the tool surface. The topics change deliberately adds no view-grammar terminator — a purpose-built query serves the labels — so the open aggregation-specifier question in the view layout stays unprejudiced. Tests landed at their owning layers: engine lane semantics over synthetic specs in an external test package, dedup regressions over the real MCP surface, the shipped capture's lane composition in proctest. This record was itself captured through the new serve path after a live binary swap mid-session: the capture opened with bare-label topics and the already-served overview pointer, the session's read ledger and both open moves intact across the restart.
