---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260812-134549-d-tac-9be
      kind: addresses
      desc: the kind-knowledge plan this delivers part of the first slice of — the done authoring fact and the minimal template layer; the plan stays open
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
summary: 'The done-kind authoring fact now ships embedded in the binary and serves from every graph, consolidated positive-side first from the graph''s banked discriminators, the skill references, and the served procedure bodies, then frozen after user review — delivering the done authoring fact and the minimal template layer as part of the first slice of the kind-knowledge plan (20260812-134549-d-tac-9be). The template layer derives the mechanics block from the declarations that enforce it, so served words cannot drift from validation. The plan stays open: the construction model, the overview fact, the remaining kinds, and the capture-lane pointers are still pending.'
---

The done-kind authoring fact now ships embedded in the binary and serves from every graph, delivered in commits bef3c891, 5c85b09c, and 5fd9ca27 — part of the first slice of the kind-knowledge plan (20260812-134549-d-tac-9be). It was consolidated positive-side first from knowledge already distributed across the graph's banked done-kind discriminators, the bundled skill references, and the served procedure bodies, with the pre-flight templates read afterwards as a completeness audit; the wording was instantiated in non-software acts as it was written, and frozen only after review by Christopher.

The minimal composition layer came with it: the fact renders from a whole-entry template file with a named placeholder where the mechanics block derives from the declarations that enforce it — the kind enumeration through new ordered accessors, the anchor rule as one constant shared with the write-path validator — so the served words cannot drift from enforcement. A table test renders and validates every base fact, and the fact's body is tested to stand alone: it cites no graph-local entries and names no host tools.

The doing reshaped the text beyond its sources, and the fact's own body is now the home of what was settled: the root-first move for an act no commitment holds, close targets widened to what the act genuinely resolved, the rule that a project's routine checks go without saying, and — from two review catches after the first landing — why a done is a signal, and that a done never restates its closed commitment's references. Still open on the plan: the construction model, the overview fact, the remaining kinds, and the served pointers that will make this fact reachable mid-draft.
