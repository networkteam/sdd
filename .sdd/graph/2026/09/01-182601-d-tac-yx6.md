---
type: decision
layer: tactical
kind: activity
refs:
    - id: 20260901-112834-d-cpt-75a
      kind: addresses
      desc: the summary directive this work renders into the template, the judge, and the regenerated stock
    - id: 20260901-133642-s-tac-p80
      kind: surfaced-by
      desc: the wild-sweep evaluation whose findings and implications shaped this dispatch
participants:
    - Christopher
confidence: high
topics:
    - cli/summaries
    - implementation/cli
summary: Commits to rendering the summary directive into the summary prompt template and eval judge, then repairing the stored summary stock via judged, batched regeneration — no figures or measurements in a summary unless the figure is the claim itself. It addresses the summary directive (20260901-112834-d-cpt-75a) and applies the wild-sweep evaluation's findings and regeneration implications (20260901-133642-s-tac-p80). Finished when the template and judge render the directive, cross-candidate verification is recorded, and the stored summaries have passed the judged batch loop with per-batch rates cited in the closing done.
---

Render the summary directive into its consumers and repair the summary stock through judged, batched regeneration.

The work, in order: rewrite the summary prompt template and the summarize eval judge so both derive from the summary directive's wording (20260901-112834-d-cpt-75a), including the one class the wild sweep found uncovered (20260901-133642-s-tac-p80) — no figures, commands, or measurements in a summary unless the figure is the claim itself. Verify the tuned template across the leading candidate models with the judged suites, and re-run the wild sweep as the post-tuning baseline. Then regenerate the graph's stored summaries in batches: each regenerated summary is judged against the full entry and the directive's rubric before it replaces the stored one — never blindly — with tuning between batches and the per-batch failure rate recorded, so declining rates measure the tuning on real data, per the evaluation's implications.

Finished when the template and judge render the directive, the cross-candidate verification is recorded, and the stored summaries have passed through the judged batch loop with per-batch rates cited in the closing done. It leaves behind the tuned template and judge in the codebase and a summary stock that passes the directive's judge.
