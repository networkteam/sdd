---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260821-123832-d-tac-ip1
      kind: addresses
      desc: delivers its removal, rename and surface-sweep criteria as the interim slice for v0.18.0-alpha.1; the parity verdict, kind-capture coverage and release notes stay open
    - id: 20260905-112554-s-tac-3ja
      kind: builds-on
      desc: the orphan cleanup this removal rests on, exercised live on this repo's own renders
    - id: 20260904-141736-d-cpt-xob
      kind: related
      desc: the vocabulary migration this slice deliberately does not wait for; the reference keeps its bundle address until that lands
participants:
    - Christopher
confidence: high
topics:
    - implementation/skill-text
    - implementation/cli
summary: The legacy skill family is removed from the bundle and the engine skill ships as `sdd`, letting v0.18.0-alpha.1 be tagged (commit b85557ac; renders refreshed by `sdd init`, commit 752e234b). This delivers the removal, rename and surface-sweep criteria of the interim slice for the skill retirement plan (20260821-123832-d-tac-ip1), while the parity verdict, kind-capture coverage and release notes stay open so the plan remains open. The init run exercised the orphan cleanup live on both agent renders (20260905-112554-s-tac-3ja), and the vocabulary reference file keeps its bundle address, deliberately the wrong home, until the vocabulary migration (20260904-141736-d-cpt-xob) lands before stable.
---

The legacy skill family is gone from the bundle and the engine skill ships as `sdd` (commit b85557ac); `sdd init` refreshed this repo's renders (commit 752e234b). This is the interim slice of the retirement plan (20260821-123832-d-tac-ip1) that lets v0.18.0-alpha.1 be tagged.

The engine template moved to `templates/sdd/SKILL.md.tmpl`. The four legacy skill templates and every reference file except `vocabulary-de.md` are deleted. The init deprecation notice, README, AGENTS.md, code comments and tests no longer point at the skill-driven flow.

The init run exercised the orphan cleanup (20260905-112554-s-tac-3ja) live on both agent renders. A `de` graph still receives its vocabulary, shown by the MCP server test over a German config.

Two departures, both the user's call for the prerelease: the removal ships before the parity verdict, since an alpha without the legacy skills is what produces that evidence, and the gate holds for stable; the vocabulary keeps its bundle address, knowingly the wrong home, until its migration (20260904-141736-d-cpt-xob) lands before stable. Also, `sdd init` auto-committed the renders before the source commit, against the AGENTS.md ordering rule.

Removal, rename and surface sweep are met. Parity verdict, kind-capture coverage and release notes stay open, so the plan stays open.
