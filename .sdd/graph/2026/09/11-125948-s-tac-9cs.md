---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260619-174104-s-tac-r8l
      kind: grounded-in
      desc: Existing participant filters and grouping depend on the stored participant list.
participants:
    - Christopher
confidence: high
topics:
    - collaboration/identity
    - implementation/engine
summary: 'High-confidence tactical gap: the capture engine cannot preserve a user-specified participant list — participants are absent from the declared draft state, revision inputs, and playback, and the write path fills the list with the local participant only, losing attribution to collaborators whose reasoning may live outside the graph. Grounded in the participant filter/grouping implementation (20260619-174104-s-tac-r8l), whose functionality cannot recover participants omitted from the stored list.'
---

Engine capture cannot preserve a user-specified participant list: participants are absent from its declared draft state, revision inputs, and playback, while the write path fills the missing list with the local participant. Session feedback reported an omitted collaborator that could not be added during capture; source inspection of the shipped capture procedure, state validation, and application write path confirmed the limitation.

Christopher's expectation is that entries retain the participants in the work and reasoning they record, including when one person later processes a meeting transcript. Those identities let later dialogue return to the people involved when an observation or decision is questioned. The current path records the local user alone without presenting that attribution for review. It loses the route back to collaborators who may hold reasoning absent from the graph, and makes it easier to reconsider their decisions without involving them. Participant filtering and grouping, delivered in 20260619-174104-s-tac-r8l, cannot recover participants omitted from the stored list; mentioning them in the body does not restore that attribution.
