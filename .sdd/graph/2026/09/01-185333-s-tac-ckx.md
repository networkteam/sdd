---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260901-100956-s-tac-83h
      kind: addresses
      desc: partial coverage of the routing divergence, one consumer of several; closes nothing
closes:
    - 20260901-182243-s-tac-uya
participants:
    - Christopher
confidence: high
topics:
    - implementation/cli
    - reliability/pre-flight
summary: 'This signal reports that the pre-flight config gap is fixed in commits 5d8ee3a8 and b91f0178: the write path now supplies the runtime''s language and dependencies to the pre-flight finder, whose config accessors fail loud on a missing config, with all consumers inheriting the check and the CLI''s read finder resolving absence to an explicit empty config. Regression tests pin both behaviors. This closes the pre-flight config gap (20260901-182243-s-tac-uya), and partially addresses the language-routing divergence (20260901-100956-s-tac-83h) by covering the pre-flight consumer, leaving the writing-guide surface and single-seam remedy open.'
---

The pre-flight config gap (20260901-182243-s-tac-uya) is fixed in commits 5d8ee3a8 and b91f0178. The application write path now hands the runtime's language and dependencies to the pre-flight finder, and the finder's three config accessors (declared dependencies, language, local participant) error on a missing config, with every consumer propagating: pre-flight, lint, the graph source, and info. A nil config on a finder now always means a mis-composed one; the single composition root where absence is a legitimate world state, the CLI's read finder, resolves it to an explicit empty config. Both candidate remedies the gap named landed, and the seam placement was settled in dialogue during review, the short loop: Christopher moved the fail-loud check from a single guard at the gate into the config accessors themselves, so every consumer of the config inherits it. Regression tests pin both behaviors: an engine-path capture with a declared, resolvable cross-repo ref draws no cross-repo finding, and a configless finder refuses pre-flight. Noticed in the doing: the configured language now reaches the engine path's pre-flight, covering one consumer of the language-routing divergence (20260901-100956-s-tac-83h); the writing-guide surface and the single-seam remedy it sketches stay open.
