---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260713-214251-d-prc-pop
      kind: grounded-in
      desc: the lifecycle whose landing order sets the expectation
    - id: 20260902-125432-s-tac-sv0
      kind: surfaced-by
      desc: the first run whose landing report hit the failure
    - id: 20260902-155452-s-tac-itj
      kind: surfaced-by
      desc: the second run whose landing report hit the failure
participants:
    - Christopher
confidence: high
topics:
    - engine/base-procedures
    - collaboration/concurrent-work
summary: 'Tactical gap signal: the implementation procedure''s landing report fails when the work branch is already gone — after merging into `main` and removing the worktree, reporting `landed` returned a "must have exactly one registered checkout (found 0)" error, reproduced on two runs. The failure stems from the landing junction resolving the graph on the work branch before accepting `landed`, making the report depend on a checkout landing itself makes disposable, contrary to the lifecycle''s expectation that the report authorize marker removal through the base authority alone (20260713-214251-d-prc-pop). It was surfaced by the two landing-report runs that hit the failure (20260902-125432-s-tac-sv0, 20260902-155452-s-tac-itj), with candidate remedies left unsettled.'
---

The implementation procedure's landing report fails once the work branch is gone: after merging into `main` and removing the worktree and branch, reporting `landed` returned `mutation target branch "worktree-llm-composition-surface" must have exactly one registered checkout (found 0)`. Seen twice in one session, on the runs closed by 20260902-125432-s-tac-sv0 and 20260902-155452-s-tac-itj; the report went through only after the branch was re-created at `main` in a worktree, then removed again.

Expected, per 20260713-214251-d-prc-pop: after host landing succeeds, the agent's report authorizes marker removal through the base authority. That report needs the base branch only. The served landing step does not say the work branch checkout must still exist, and "merge and remove the worktree" is the natural order for a user to ask for.

The deviation: the landing junction resolves the current graph on the work branch before it accepts `landed`, so the report depends on a checkout that landing makes disposable.

Candidate remedies, unsettled: resolve the landing step against the base branch, or have the step's instructions name the order, report landed before removing the worktree.
