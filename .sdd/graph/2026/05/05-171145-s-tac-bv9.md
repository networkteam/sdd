---
type: signal
layer: tactical
kind: done
refs:
    - 20260505-140419-s-prc-gu6
    - 20260505-111247-s-prc-nxy
    - 20260505-111057-s-tac-n8u
    - 20260505-111215-s-cpt-0db
closes:
    - 20260505-152402-d-tac-ab1
participants:
    - Christopher
    - Claude
confidence: high
summary: Pattern A modularization for `/sdd` has landed after evaluation—five playbooks extracted into on-demand references with load-gates, `/sdd-catchup` demoted to playbook reference, and SKILL.md reduced 47% on cold load (15,479 → 8,160 tokens) while preserving always-loaded invariants. All 9 walked routing-reliability scenarios pass with zero regressions and dialogue flow remains crisp; the decision closes the modularization plan (d-tac-ab1) and directly implements recommendations from the comparative bundle examination (s-prc-gu6) that address the skill size gap (s-prc-nxy) grounded in token-load baseline (s-tac-n8u) and instruction-density research (s-cpt-0db).
summary_hash: a77d29d864ad62362827c6ce1f44d37d
---

Lands Pattern A modularization for `/sdd` after eval — playbooks extracted into on-demand references with explicit load-gates, `/sdd-catchup` demoted from sub-skill to playbook reference, and `/sdd-bootstrap` hand-back updated. SKILL.md drops from 447 → 210 lines (~53%); cold-load reduction on `/sdd` invocation: 15,479 → 8,160 tokens (~47%). 9-of-9 walked scenarios pass with zero regressions, two qualitative observations surfaced and resolved, scenario #9 (bootstrap hand-back runtime) deferred to next bootstrap-state graph; structural change verifiable from source diff. Decision criterion holds on all three sub-criteria; change lands.

## AC evidence

- [x] **AC 1 — References extracted**: five `references/playbook-*.md` files in place — `playbook-catchup.md` (1,615 tokens), `playbook-explore.md` (875), `playbook-groom.md` (1,201), `playbook-augment-plan.md` (1,200), `playbook-implementation.md` (2,150). SKILL.md retains the dispatcher (gate table) with reference paths per mode. Commits: `24d82e8`, `bcc5b9f`.

- [x] **AC 2 — Always-loaded invariants preserved**: capture discipline, type/layer/kind rules, canonical-only participants, sync-check guidance, language handling, vocabulary register, and the modes-of-working dispatcher remain inline in SKILL.md. Capture flow scenario (#2) and decide flow scenario (#3) confirmed both pass without loading any reference, validating the always-loaded invariants are sufficient for capture and decision work.

- [x] **AC 3 — Load-gate format adopted**: SKILL.md's Modes of working section is now a gate table (one row per mode naming the trigger and the reference path). Adopted form includes load-first ordering in the preamble ("Load the listed reference first, then invoke any named sub-skill, then run any data-gathering CLI commands the playbook prescribes"), refined after eval surfaced an ordering inconsistency between catch-up/groom (data-first) and explore (playbook-first). Trim of redundant mode prose (commit `bcc5b9f`) eliminated the structural cause: prose paragraphs that pre-empted the playbook for referenced modes. Inline content retained only for the three modes without references (Capture, Reflect/Dialogue, Decide). Non-redundant guidance ("prefer reducing scope over building into the unknown") relocated to `playbook-implementation.md`.

- [x] **AC 4 — `/sdd-catchup` sub-skill removed and bootstrap hand-back updated**: sub-skill deleted from `internal/bundledskills/claude/sdd-catchup/` and `.claude/skills/sdd-catchup/`. Full-catch-up content moved to `references/playbook-catchup.md` (commit `24d82e8`). `/sdd-bootstrap`'s Move 5 hand-back (`internal/bundledskills/claude/sdd-bootstrap/SKILL.md`) now points at `../sdd/references/playbook-catchup.md` instead of `../sdd/SKILL.md`. Bundle and install tests updated (`bundledskills_test.go`, `handler_install_skills_test.go`).

- [x] **AC 5 — Token baseline captured** (per-file precision via Anthropic `count_tokens` API):
  - Pre-mod (commit `38b8260`): SKILL.md 447 lines, 15,479 tokens. `/sdd-catchup` SKILL.md 127 lines, 2,176 tokens.
  - Post-mod + trim (commit `bcc5b9f`): SKILL.md 210 lines, 8,160 tokens. References (loaded on demand): catchup 1,615 + explore 875 + groom 1,201 + augment-plan 1,200 + implementation 2,150 = 7,041 tokens combined, 234 lines.
  - **Cold-load reduction on `/sdd` invocation: 15,479 → 8,160 tokens (~47%).**
  - Trigger metadata surface drops by one sdd-family skill (~50 tokens) since `/sdd-catchup` is removed.
  - The `/usage` panel total-Messages reading vs s-tac-n8u baseline (51.3k tokens) was not captured during eval — deferred as follow-up evaluation if validation against the runtime baseline is wanted later.

- [x] **AC 6 — Routing-reliability scenario eval run**: 9 of 10 scenarios walked in fresh agent sessions against the candidate branch, all pass; #9 (bootstrap hand-back runtime) deferred to next bootstrap-state graph (structural change is verifiable from source diff in commit `24d82e8`). No scenario regressions vs baseline. Two qualitative observations: (a) load-order inconsistency across modes resolved by the post-eval trim; (b) groom output rendering (block-per-candidate vs table) — pre-existing, not Pattern A regression, captured as follow-up signal to consider after Round 1 lands. Full scenario detail in attached eval document.

- [x] **AC 7 — Decision criterion applied** (lands or reverts):
  1. **No scenario regressions vs baseline**: holds (zero regressions across 9 walked scenarios).
  2. **Measurable token reduction on cold start and per-mode loads**: confirmed at ~47% on cold load with per-mode deltas 875–2,150 tokens.
  3. **Qualitative read of dialogue flow**: positive — dispatch crisp, capture/decide flow fluently from always-loaded surface, false-positive prevention holds (#10), and the one ergonomic friction (load order) was structural and is now resolved.
  
  **Call: Land.**

- [x] **AC 8 — Mode differentiation deferred**: covered by `d-tac-kv5` (quick catch-up mode, contingent on `s-cpt-8tu` planning-activity proposal) and `d-tac-qom` (topic-drill catch-up via cluster traversal, no blocking dependencies). Both captured as separate directives that ref `d-tac-ab1`; this done signal does not close them — they remain active deferral commitments awaiting their own conditions.

## Follow-up signals (not blocking landing)

- Scenario #9 runtime test (bootstrap → catch-up hand-back) deferred until next bootstrap-state graph appears; structural correctness already verified.
- Groom-playbook output rendering: agent renders block-per-candidate where the playbook prescribes a markdown table. Pre-existing behavior, not Pattern A. Worth a separate signal if persistent.
- Capture-detail blocks (attachment assessment, infer participants, canonical names, language handling, sync-check) remain inline per Round 1 plan; Round 2 can re-evaluate moving them after this round's reliability data is in.

See [eval](./05-152402-d-tac-ab1/eval.md) for the full scenario walkthrough, per-scenario observations, and qualitative dialogue-flow read.
