---
type: signal
layer: tactical
kind: done
closes:
    - 20260507-233454-d-tac-k4l
    - 20260505-153647-d-tac-kv5
participants:
    - Christopher
    - Claude
confidence: high
summary: 'Shipped the `/sdd-catchup` sub-skill, always-on `/sdd` framing injections, and `sdd info` command with two fresh-session evaluations meeting cost targets (52–65s versus 41s–2m33s design envelope) and only minor voice/data-availability issues. Closes the quick-catch-up directive (d-tac-kv5) by delivering story-arc threading and focus-line surfacing that achieves the original goal via different structure than the proposed three-tier framing, and closes the tactical plan decision (d-tac-k4l) by promoting the sub-skill to inline deployment with dynamic context injection and operationalizing structural framing injection. Leaves the topic-drill directive (d-tac-qom) open: a third evaluation revealed drill verbs fall through to engage-mode instead of expanding topic clusters, with the expand-before-engage flow captured as a follow-up gap and topic-aggregation mechanics carved out as (s-cpt-sy4).'
summary_hash: 19a342b972ec1ed34e99c11df6a8563c
---

Shipped the inline `/sdd-catchup` sub-skill, the always-on `/sdd` framing injections, and the new `sdd info` command — two fresh-session evaluations produced colleague-style briefings within the design's cost envelope (1m 5s and 52s, comfortably under the 41s–2m33s target from the design phase) with one banned-word slip and one data-availability blur as the only voice/threading issues.

The build collapsed across four slices: a new `sdd info` command (participant + language + search) backing the always-on header, `/sdd` SKILL.md's structural injections for project framing, the `/sdd-catchup` sub-skill at `internal/bundledskills/claude/sdd-catchup/` carrying the voice block and format template ported verbatim from the converged design (`catchup-design.md`), and routing from `/sdd`'s Check-in mode to the sub-skill via the Skill tool. Plus a setup-side fix putting `bin/sdd` on PATH via `devbox.json` so injections call the dev build, and a mid-build refinement replacing `since("3d")` with `kind(done):rank(by(date)):n(10)` after the eval surfaced a window-too-narrow data-availability gap.

Closes the quick-catch-up directive (`d-tac-kv5`) — the sub-skill's story-arc threading and italic focus line achieve the original goal (surface what wants the user's next action) via a different shape than the original three-tier framing. The topic-drill directive (`d-tac-qom`) remains open: a third fresh-session evaluation on "drill into C" revealed the drill verb falls through to engage-mode behavior (chain reads + synthesis brief + decision-dimensions list) instead of expanding the topic cluster into more entries — the expand-before-engage flow is what `d-tac-qom` asks for and is captured as a follow-up gap signal. The mechanical topic-aggregation backing for the drill labels themselves also remains carved out as `s-cpt-sy4` per the plan. See [evaluation record](./17-170936-s-tac-7uy/evaluation.md) for the full evaluation across voice fidelity, format adherence, threading quality, and wall-clock cost — including the third (drill) session.
