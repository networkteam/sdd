---
type: decision
layer: process
kind: directive
refs:
    - id: 20260517-171007-s-cpt-kfl
      kind: addresses
      desc: completes the reader-facing half of the temporal-blur gap by surfacing inactive refs in catch-up
    - id: 20260529-212319-s-tac-vlg
      kind: depends-on
      desc: requires the expand(refs(inactive)) modifier shipped here
participants:
    - Christopher
    - Claude
confidence: high
summary: Integrate `expand(refs(inactive))` into the `/sdd-catchup` "Active and hot" block to surface closed or superseded dependencies as threading signals, helping readers spot now-unblocked plans. This completes the reader-facing resolution of the temporal-blur gap (s-cpt-kfl) by consuming the `expand(refs)` capability shipped in (s-tac-vlg), scoped narrowly to active plans where inactive refs carry clear unblocking meaning and keep catch-up costs lean.
summary_hash: 6773b64c1fd9c1bf4d9c23dc29a0640b
---

Wire `expand(refs(inactive))` into `/sdd-catchup`'s "Active and hot" fetch and add briefing guidance to read the resulting inactive-ref sub-lines as a threading signal — references that have closed or been superseded, e.g. spotting plans whose blocker just resolved and are now likely unblocked. Scoped to the Active-and-hot block only (active plans/activities), where an inactive dependency carries the clearest "now unblocked" meaning; the inactive filter keeps the added catch-up cost lean. Completes the reader-facing resolution of the temporal-blur gap (s-cpt-kfl) that shipping the `expand(refs)` capability (s-tac-vlg) began: the gap was framed around blur experienced in catch-up, so the capability alone did not remove the symptom until catch-up consumes it.
