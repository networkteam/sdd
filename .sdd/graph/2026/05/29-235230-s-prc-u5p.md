---
type: signal
layer: process
kind: done
closes:
    - 20260529-235130-d-prc-49b
participants:
    - Christopher
    - Claude
confidence: high
summary: Wired `expand(refs(inactive))` into the `/sdd-catchup` "Active and hot" block to surface closed or superseded dependencies as threading signals, enabling readers to spot now-unblocked work without rendering stale prose. This closes the process directive (20260529-235130-d-prc-49b) and completes the reader-facing resolution of the temporal-blur gap (s-cpt-kfl) by consuming the `expand(refs)` capability (s-tac-vlg), scoped narrowly to active plans to keep catch-up costs minimal.
summary_hash: bff0b4e978d12611f190841d43cf79b4
---

Wired `expand(refs(inactive))` into `/sdd-catchup`'s "Active and hot" fetch layout and added briefing guidance directing the agent to read the resulting inactive-ref sub-lines as a threading signal — references that have closed or been superseded under an active plan/activity, used to spot now-unblocked work — without rendering them verbatim. Scoped to the Active-and-hot block only; the inactive filter keeps the added catch-up cost minimal. This completes the reader-facing resolution of the temporal-blur gap (s-cpt-kfl): the `expand(refs)` capability (s-tac-vlg) shipped the render, and catch-up now consumes it, so a reader is shown when a referenced dependency is no longer live rather than reading stale summary prose. Source and installed sub-skill updated; change in commit fa6c42e.
