---
type: signal
layer: tactical
kind: done
closes:
    - 20260520-131326-d-tac-6tz
    - 20260507-233610-s-cpt-sy4
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - type-system/topics
summary: 'Shipped the topic system into active use across capture, CLI, display, and policy, delivering all four work shapes: capture-time research procedure, CLI filters and aggregation renders, annotation topic fix, and label-stability policy (d-tac-6tz). This closes the catch-up aggregation infrastructure gap (s-cpt-sy4), with topic backfill on existing entries deferred to the grooming directive (d-tac-nbp). Key implementations include `sdd view as-counts`, `untagged` and `id()` filters, `--max-citations 0` for headers-only search, and effective-topics folding that surfaces annotations under their assigned topics.'
summary_hash: 0a19deb86e05747227da73ec829a2d07
---

Shipped the topic system into active use across capture, CLI, display, and policy, closing the four-shape topic plan (d-tac-6tz) and the catch-up aggregation gap (s-cpt-sy4). AC2: `sdd view` gained `as-counts` (per-topic count + heat), an `untagged` filter, and an `id(…)` filter; `sdd search` gained `--max-citations 0` for headers-only output; cli-reference documents all plus the multi-ID `sdd show` form. AC3: `EffectiveTopics` now folds an annotation's own declared labels into its effective set, so annotations surface under the topics they assign (filter, show, as-list), fixed in one place. AC4: framework-concepts gained the label-stability policy. AC1: the /sdd capture playback gained a topic-research procedure (as-counts → id() on refs → max-citations 0 search) and a Topics line in the playback, validated by a fresh-session paste-test where a cold agent autonomously researched labels and proposed a new ux/cli cluster (s-cpt-idq). Four calls made in dialogue: `untagged` shipped as a dedicated primitive (not `not(topic())`); `--max-citations 0` means literal zero with the default resolved at the CLI via cmd.IsSet; `as-counts` aggregation lives on model.Graph.TopicCounts per push-logic-down; and `id()` requires quoting full IDs because the layout grammar reads leading-digit tokens as numbers (short IDs work bare), a candidate follow-up gap. Topic backfill on existing entries was deliberately left to the downstream grooming directive (d-tac-nbp): s-cpt-sy4's infrastructure ask is delivered, while the backfill is the grooming half of s-cpt-ghy that d-tac-nbp owns. Full per-AC evidence and the dialogued decisions are in the attached delivery notes.
