---
type: signal
layer: process
kind: done
refs:
    - 20260507-102919-s-prc-fc0
closes:
    - 20260507-103822-d-prc-kyz
    - 20260507-003530-s-prc-kaw
participants:
    - Christopher
    - Claude
confidence: high
summary: Shipped the engage refactor collapsing SDD's Explore and Act/Implement modes into a single Engage mode with a unified playbook that routes post-engage moves by entry kind, and rewrote `/sdd-explore` as a goal-tagged research compressor using `sdd search` as primary (s-prc-fc0). Resolves the stale explore prompts and load-order violation (s-prc-kaw) and closes the refactor plan (d-prc-kyz). Lens-flavor moves (evaluate, compliance, alignment checks) remain queued as a follow-up.
summary_hash: 288ab5e8751a7a9b60b548863b91c11d
---

Shipped the engage refactor for SDD's mode table, collapsing Explore and Act/Implement into a single **Engage** mode whose new playbook routes per-kind post-engage moves. The `/sdd-explore` sub-skill is rewritten as a goal-tagged compressed-brief generator using `sdd search` as the primary related-entry mechanism. Closes the originating gap (s-prc-kaw, stale Explore-mode prompts and load-order violation) and the plan (d-prc-kyz).

**Shipped**:

- New `internal/bundledskills/claude/sdd/references/playbook-engage.md`. Carries (a) the four-step universal entry move (anchor → read chain via `sdd show --downstream` → brief → offer moves), (b) post-engage move catalog keyed by entry kind with the acting-on / lens-flavor split, (c) brief-shape family with three concrete shapes worked out (AC-status table for plan + implementation; narrative for signal + explore; divergence list for contract + compliance-check) plus sketches for done+evaluate, aspiration+alignment-check, question+answer.
- Rewritten `internal/bundledskills/claude/sdd-explore/SKILL.md`. Inputs are now target + goal (refuses to compress without a goal). Step 4 uses `sdd search --query` as primary and `sdd search --term` for known identifiers; the bare-`sdd list`-then-read-everything LLM-as-semantic-matcher recipe is removed. Output is a goal-tagged compressed brief with an explicit "Compression note" section.

**Mode-table changes** in `internal/bundledskills/claude/sdd/SKILL.md`:

- Engage row replaces both Explore and Act/Implement, anchored on playbook-engage.md with an inline note that `/sdd-explore` is invoked from inside as a tool.
- Evaluate row no longer silently mis-points at playbook-catchup.md — temporarily routed at playbook-engage.md with an inline note that a dedicated `playbook-evaluate.md` is queued behind.
- Groom row reordered to put the playbook reference before the sub-skill invocation (load-first rule per s-prc-kaw).
- Bootstrap, Check-in, Capture, Reflect/Dialogue, Decide, Augment-plan rows unchanged.

**Removed**: bundle source `playbook-explore.md` and its installed copy; the orphan reference in `search.md` rewritten to describe Engage and the `/sdd-explore` compressor.

**Sequencing kept clean**: `playbook-implementation.md` stays as on-demand load (from inside playbook-engage.md when "implement" is the chosen move), no longer a top-level mode-table row. Same for `playbook-augment-plan.md` under the augment move.

**Build + reinstall**: `go build -o bin/sdd ./cmd/sdd`, `./bin/sdd init --scope project`. `go vet ./...` and `go test ./...` clean. Both bundle source and installed `.claude/skills/...` copies live in HEAD.

**Deviations**:

1. `meta-process.md` still names "Act/Implement" as a mode (line 24). Left unchanged — it's conceptual narrative ("how to work with the graph") rather than operational routing, predates the SKILL.md mode-table, and the plan's ACs don't require updating it. Minor inconsistency between conceptual narrative and operational mode-table; worth a follow-up signal if it bites in practice.
2. Commit `ae10591` mixes this engage refactor with unrelated `view` name-prefix work that was already pending in the working tree. Another session auto-committed both change sets under a message describing only the view work. Diffs intact in HEAD; readers tracing via git log won't find the engage work by commit message alone.
3. `sdd init` doesn't auto-prune installed files that no longer exist in the bundle — `.claude/skills/sdd/references/playbook-explore.md` was removed manually after reinstall. Worth surfacing as a separate fact/gap signal.

**Lens-flavor follow-up still queued**: per the synthesis (s-prc-fc0), `playbook-evaluate.md` for the lens-flavor moves (evaluate, compliance check, alignment check, focus-progress check) remains as a follow-up directive — out of scope here.
