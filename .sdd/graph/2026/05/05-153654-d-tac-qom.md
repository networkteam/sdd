---
type: decision
layer: tactical
kind: directive
refs:
    - 20260505-152402-d-tac-ab1
    - 20260504-235258-d-tac-lqr
participants:
    - Christopher
    - Claude
confidence: medium
summary: Decides to add a topic-drill catch-up mode to the playbook that loads entries within user-selected clusters via ref-graph traversal, with no blocking dependencies and available for immediate implementation. This deferred enhancement to the playbook extraction plan (d-tac-ab1) uses cluster identity from the catch-up clustering step as its foundation. Semantic-search ranking from the planned sdd search CLI (d-tac-lqr) is identified as a soft enhancement for improved cross-cluster surfacing but not a prerequisite for delivery.
summary_hash: 81c704815d5d0439e16b4d2b1e65ae4a
---

Add a topic-drill catch-up mode to the playbook (`references/playbook-catchup.md` per `d-tac-ab1`): when the user points at a thread/cluster identified during a full-mode catch-up, load entries within that cluster via ref-graph traversal. Basic implementation uses cluster identity from the catch-up clustering step plus ref-graph expansion; available today, no blocking dependency. Semantic-search ranking via `sdd search` (`d-tac-lqr`) is a soft enhancement — improves cross-cluster surfacing but isn't a prerequisite. The deferral reason is scope-boundary on `d-tac-ab1`, not an external blocker; can be picked up whenever capacity allows.
