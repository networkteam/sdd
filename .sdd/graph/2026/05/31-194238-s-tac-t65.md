---
type: signal
layer: tactical
kind: done
closes:
    - 20260531-164326-d-tac-6d4
    - 20260531-183508-d-tac-kxt
participants:
    - Christopher
    - Claude
confidence: high
summary: Shipped the eight-principle ref-kind vocabulary with single-sourced canonical definitions and status-aware pre-flight ref-meta checking, closing both the implementation plan (d-tac-6d4) and single-source directive (d-tac-kxt). All nine acceptance criteria met across model, display, pre-flight rubric, and skill surfaces, with live calibration validation confirming ref-kind scenario accuracy and resolving prior debatable medium findings.
summary_hash: c8d40160ed5e9e24bae4410ebd2f4821
---

Shipped the principle-based ref-kind vocabulary, single-sourced it, and made the pre-flight ref-meta check status-aware — closing the implementation plan (d-tac-6d4) and its augmenting single-source directive (d-tac-kxt). Code and bundle source landed in commit 8ded8f5; the re-stamped installed skills in commit 0e285c4.

Each acceptance criterion of d-tac-6d4:

1. Done — the capturable set is the eight principle kinds (grounded-in, builds-on, refines, addresses, surfaces, depends-on, required-by, related); grounds/evidence are rejected on capture (unit tests in internal/model and cmd/sdd).
2. Done — legacy on-disk grounds/evidence parse as grounded-in through a read-layer alias with no entry files rewritten; sdd lint is clean. Deviation: the alias flows into the summary-generation prompt, so 18 entries that cite grounds/evidence had their summary hashes re-stamped with byte-identical text (via yq into summarize --text, dialogued and chosen over carrying the raw kind) — ref data untouched, no relationship history rewritten.
3. Done — sdd show and sdd view expand(refs) render pre-existing grounds/evidence as grounded-in, and required-by renders for new entries (presenter tests plus a live show check).
4. Done — the pre-flight ref-meta rubric defines the vocabulary by principle, injected from the single fragment; no pre-flight template restates grounds/evidence (swept and confirmed — the remaining matches were ordinary English).
5. Done — pre-flight receives each referenced entry's derived status and uses it to separate grounded-in / builds-on / refines. Deviation: "summary hashes unchanged" holds for this status-threading path (the shared FormatEntryForPrompt is untouched and a guard test asserts the summary prompt carries no status); the only hash movement is the rename re-stamp recorded under AC 2.
6. Done — framework-concepts.md and SKILL.md document the principle vocabulary consistent with the selection-and-calibration directive (d-prc-2is); the full definitions live once in references/ref-kinds.md.
7. Done — installed skills rebuilt and re-stamped via sdd init (commit 0e285c4), with the bundle source committed alongside (8ded8f5).
8. Done — go vet ./..., go test ./..., and golangci-lint run ./... are clean.
9. Done — ref-kind calibration validated live against the runner (preflight_eval_test.go, -tags=eval): scenarios across the scenario-to-kind table plus the known-debatable cases. All ref-meta scenarios pass — addresses-realizing-a-decision and refines-an-active-plan (the two captures of this very plan that tripped debatable mediums) now produce no finding, and a status-sensitive builds-on-that-should-be-refines is caught as high. One inherited scenario was reworked: its body operationalized an aspiration, which the new vocabulary correctly reclassifies as addresses, so it was re-pointed at a fact target to probe the topical-drift band cleanly. Live eval has inherent nondeterminism on borderline cases.

Augmenting directive d-tac-kxt (single-source) delivered: one canonical references/ref-kinds.md, install-time include resolution (sdd init's render step resolves the marker and drift detection resolves it identically, so pristine/modified classification and the content-hash stay correct), and pre-flight injection of the same fragment. This answers the shared-canonical-reference question (s-tac-uer) for the ref-kind case; broader definitional grounding under s-tac-uer stays open.
