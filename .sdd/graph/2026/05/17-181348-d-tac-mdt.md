---
type: decision
layer: tactical
kind: focus
refs:
    - 20260506-190107-d-tac-0qn
    - 20260517-180646-s-cpt-c43
participants:
    - Christopher
    - Claude
confidence: medium
actors:
    - Christopher
    - Claude
involvement:
    - target: 20260517-175045-d-tac-cs0
    - target: 20260517-173733-d-tac-jqq
    - target: 20260517-180615-d-tac-yls
      actors: []
    - target: 20260517-180149-d-tac-cpw
      actors: []
summary: Commits to advancing the ref-metadata constellation by shipping the per-ref kind taxonomy (d-tac-cs0) as foundation, unblocking dependent render and ranking work (d-tac-yls, d-tac-cpw), with drill-flow planning (d-tac-jqq) running in parallel. Succeeds the prior focus (d-tac-0qn) by extending structural metadata into refs themselves, enabling derived-status rendering and label-aware heat weighting that resolve three observed gaps (s-cpt-kfl, s-cpt-zsd, s-prc-bxk). The compact-entry-representation signal (s-cpt-c43) is tracked but out of scope for this focus; closes when all four targets reach completion.
summary_hash: 6b651ce9a41ac7e6a044e8960c54b29d
---

Commits to driving the ref-metadata constellation as time permits, with no fixed timeline. Per-ref kind taxonomy (`d-tac-cs0`) ships first as the structural foundation, then unblocks the dependent render and ranking work (`d-tac-yls`, `d-tac-cpw`); the drill-flow plan (`d-tac-jqq`) runs in parallel since it touches the `sdd view` parser in a non-overlapping position (filter vs. transform). This focus succeeds `d-tac-0qn` (type-system + sdd view + playbook rewrite — all three targets closed) by carrying the structural-metadata thread forward into refs themselves: refs gain semantic `kind` and per-ref `desc`, which then enable derived-status rendering (resolves the temporal-blur gap `s-cpt-kfl`), label-aware heat weighting (resolves the heat-conflation gap `s-cpt-zsd`), and ref-graph drill expansion (resolves the drill fall-through gap `s-prc-bxk`).

Dependency chain: `d-tac-cs0` → {`d-tac-yls`, `d-tac-cpw`}. Both dependents have full plan/directive shape with acceptance criteria but can't start coding until cs0 ships the per-ref `kind`/`desc` model. Drive `d-tac-cs0` and `d-tac-jqq` (work can begin immediately); treat `d-tac-yls` and `d-tac-cpw` as pull-available pending the foundation. Closes when the four targets reach their done signals; supersede if priorities shift.

Compact-entry-representation direction (`s-cpt-c43`, short_summary) is tracked but out of scope here — it's the fourth primitive that would complete the per-ref information matrix when it lands, but doesn't need driving inside this focus. Small refinement directive `d-tac-tzw` is bundled into `d-tac-cs0`'s implementation (sharpens its pre-flight wiring) and doesn't appear as its own involvement target.
