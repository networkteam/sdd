---
type: signal
layer: tactical
kind: done
refs:
    - 20260507-130356-s-tac-m09
closes:
    - 20260506-151345-d-tac-uww
    - 20260507-101046-d-tac-3pq
    - 20260507-110226-d-tac-jgi
participants:
    - Christopher
    - Claude
confidence: high
summary: Shipped slice 8 of sdd view, closing all 23 acceptance criteria and both refinement directives (d-tac-3pq syntax, d-tac-jgi auto-headers) with a single commit that validates the CQRS decomposition mandate (d-cpt-ah1). Closes the parent plan (d-tac-uww) and its augmenting directives while delivering ranked-output defaults (heat, exp-14d) with comparative analysis and documented spec corrections from slice 2. Refs a newly surfaced CQRS leak (s-tac-m09) for follow-up and deliberately defers cross-command primitive reuse (AC 19) as a future opportunity rather than blocking closure.
summary_hash: 0d79704783d687a2a8c958c14d03b018
---

Shipped slice 8 of the `sdd view` plan (d-tac-uww), closing all 23 acceptance criteria with this slice landing source(wip) + as-wip-list, as-participants-block, the participants and wip macros, auto-derived section headers per d-tac-jgi, and the cli-reference.md skill update. The slice-1 CQRS seam (parser → query → finder → presenter → CLI) carried through every slice unchanged, validating the d-cpt-ah1 decomposition mandate.

This signal closes the parent plan along with both augmenting directives in a single commit: d-tac-3pq (`group(by(<field>))` nested form, slice 5) and d-tac-jgi (AC 14 scope, slice 7→8). The findings attachment compares three rank/decay combinations (heat with exp-7d/exp-14d/exp-30d) plus heat vs in-degree and the mult/add composites on the live 452-entry graph; documents chosen defaults (heat, exp-14d, top-N user-supplied, stalled threshold 1.0) with rationale; and notes that the live graph's current focus block doesn't discriminate stalled-threshold sensitivity (one target is pull-available, one scores 0.0 — both render identically across 0.5/1.0/2.0). Defaults remain revisable via a future directive once usage data accumulates.

Two slice-2 spec deviations were corrected in slice 6 and should be treated as canonical against d-tac-uww §2: multiple `kind()` filters now intersect rather than union, and `render` no longer needs to be the syntactically last function. The "render is the terminus" property holds semantically via canonical bucket order, not source position. Documented inline in the executor comments and called out in the findings attachment so future plan reviewers don't assume the original slice-2 shape.

AC 19 lands as partial-but-deliberate: `topic(L)` is the example primitive shared between `sdd view` and `sdd list --topic` (satisfying the AC's literal example), but other primitives (filters, rank, aggregation) are not yet exposed for cross-command reuse. Captured as a future opportunity rather than pushed into slice 8's scope. Slice 8 also surfaced a CQRS leak across read-side queries that carry pre-resolved graph data through the query slot — captured separately as s-tac-m09 for follow-up; not blocking d-tac-uww closure.

See [comparative findings](./07-133746-s-tac-z2o/findings.md) for the full comparison data, AC-by-AC coverage, and architectural observations.
