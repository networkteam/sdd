---
type: decision
layer: tactical
kind: focus
refs:
    - id: 20260506-191632-d-cpt-ni0
      kind: grounds
participants:
    - Christopher
    - Claude
confidence: medium
topics:
    - type-system/topics
actors:
    - Christopher
    - Claude
when:
    from: "2026-05-20"
involvement:
    - target: 20260520-131326-d-tac-6tz
    - target: 20260520-132400-d-tac-nbp
summary: This focus decision commits to operationalizing the topic system by ensuring consistent assignment and visibility across active work, grounding in the type-system contract (d-cpt-ni0) that formalized topics as a structural requirement. Execution splits between Plan-1 (d-tac-6tz) delivering capture infrastructure and view tooling, and a directive (d-tac-nbp) handling grooming and backfill, closing when both reach done and the graph shows functional topic coverage for clustering and discovery. It runs parallel to the ref-metadata focus (d-tac-mdt).
summary_hash: 018767b7ad8f5a56398571410fa49b0e
---

Commits attention to getting the topic system into use. Topics have been part of the type-system contract since May (`d-cpt-ni0`), but they generate no value until they're assigned consistently and surfacing where work actually happens.

Sequence:

- Plan-1 (`d-tac-6tz`) ships the foundation — capture-time procedure, view infrastructure (`as-counts`, untagged filter, `id(...)`, `--max-citations 0`), annotation display fix, label-stability policy.
- Directive (`d-tac-nbp`) delivers the grooming flow and runs the backfill exercise on active and open entries.

Closes when both targets reach their done signals — at which point the graph has consistent topic coverage on the active working set and the skill drives topics during capture. The goal is functional: topics visibly helpful in catch-up clustering, drill expansion, and discovery — not just present.

Runs in parallel with the ref-metadata focus (`d-tac-mdt`).
