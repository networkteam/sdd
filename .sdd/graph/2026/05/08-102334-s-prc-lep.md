---
type: signal
layer: process
kind: done
closes:
    - 20260508-101856-d-prc-h4u
participants:
    - Christopher
    - Claude
confidence: high
summary: Implemented the release skill at `.claude/skills/release/SKILL.md` with a six-step workflow (survey, version bump, draft notes, confirmation gate, ship, verify) plus a Recovery section documenting v0.5.1 anti-patterns and `gh run rerun` recovery steps. Smoke-tested `git log` for scope and `sdd show` with multi-ID + depth-0 for batch graph context, confirming the right detail level for release prose. The skill is project-scoped, gates all release commands behind user confirmation, and closes the release skill adoption directive (d-prc-h4u).
summary_hash: 96d6f44571de981c54fd82cdd3d56eef
---

Authored `.claude/skills/release/SKILL.md` per directive d-prc-h4u — six-step workflow (survey, version bump, draft notes, confirmation gate, ship via single command, verify) plus Recovery section codifying the v0.5.1 anti-patterns (no `--draft`, no delete-and-redo; `gh run rerun` for partial-completion recovery). CLI invocations smoke-tested in-session: `git log $(git describe --tags --abbrev=0)..HEAD --oneline` for authoritative scope and `sdd show <id1> <id2> --max-depth 0` for batch graph context (multi-ID + depth-0 confirmed showing entry bodies plus immediate closes/refs, deeper chain truncated — the right level for highlight prose). The skill is project-scoped (not bundled via `internal/bundledskills/claude/`), surfaces in the available-skills list as `release`, and gates explicitly on user confirmation before any release-creating command runs. Skill file committed in 853bf76.
