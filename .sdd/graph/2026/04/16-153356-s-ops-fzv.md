---
type: signal
layer: operational
kind: gap
refs:
    - 20260415-152324-s-prc-84r
participants:
    - Christopher
    - Claude
confidence: high
summary: Pre-flight and summary generation in sdd new run sequentially, adding 30–60s of unnecessary latency per entry despite being independent LLM calls that could execute concurrently. The summary is discarded if pre-flight rejects, but this cheap Haiku overhead is justified by saving 30–60s on most accepted entries. The fix launches both as goroutines after validation, waits for both, and discards the summary if pre-flight blocks, addressing latency introduced after the pre-flight format work (20260415-152324-s-prc-84r).
summary_hash: 59c04a277b4a8e64af7e0cedfb2f532f
---

Pre-flight and summary generation in sdd new run sequentially (handler_new_entry.go:82-135), adding 30-60s of unnecessary wall time per entry. Both are independent LLM calls that can run concurrently. The summary is wasted if pre-flight rejects, but that is a cheap Haiku call vs. saving 30-60s on every accepted entry — most entries pass. Implementation: launch both as goroutines after validation, wait for both, discard summary if pre-flight blocks.
