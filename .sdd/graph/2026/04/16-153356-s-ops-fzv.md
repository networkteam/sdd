---
type: signal
layer: operational
kind: gap
refs:
    - 20260415-152324-s-prc-84r
participants:
    - Christopher
    - Claude
confidence: high
summary: Pre-flight and summary generation in sdd new run sequentially, wasting 30–60s per entry since both are independent LLM calls that could execute concurrently. The proposed fix launches both as goroutines after validation and discards the summary if pre-flight rejects, trading a cheap Haiku call cost against consistent 30–60s savings on most accepted entries. This optimization follows completion of the confidence-scored pre-flight implementation (20260415-152324-s-prc-84r).
summary_hash: a03c1d607264b3b74d80496ccdaf50ec
---

Pre-flight and summary generation in sdd new run sequentially (handler_new_entry.go:82-135), adding 30-60s of unnecessary wall time per entry. Both are independent LLM calls that can run concurrently. The summary is wasted if pre-flight rejects, but that is a cheap Haiku call vs. saving 30-60s on every accepted entry — most entries pass. Implementation: launch both as goroutines after validation, wait for both, discard summary if pre-flight blocks.
