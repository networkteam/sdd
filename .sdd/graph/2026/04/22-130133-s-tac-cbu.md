---
type: signal
layer: tactical
kind: done
closes:
    - 20260417-091917-d-tac-q5p
participants:
    - Christopher
    - Claude
confidence: high
summary: Participant identity enforcement is now complete and live in commit d9165c5 — `.sdd/config.local.yaml` stores canonical names, CLI commands fall back to it when flags are absent, and pre-flight validation emits medium-severity drift findings for any name mismatches against the established graph set. This closes the tactical plan (d-tac-q5p) and unblocks participant profiles with authorship-based routing (s-cpt-5jk), making canonical names a load-bearing invariant for future work.
summary_hash: 7efe21febd0abaf1e5d08731c7cf5304
---

Lands d-tac-q5p end to end — participant identity is now explicit, enforced, and drift-detected. `.sdd/config.local.yaml` carries a `participant` key (written by `sdd init` interactively or via `--participant <name>`); `sdd new` and `sdd wip start` fall back to it when the flag is omitted and exit non-zero with "run `sdd init`" when both are missing; pre-flight emits a medium-severity `participant-drift` finding per unmatched name, listing the established set; the `/sdd` skill playbook gains a before-playback correlation rule. Implemented in commit d9165c5.

AC walkthrough:

1. `Config.Participant` field + yaml tag — `internal/model/config.go`.
2. Interactive `sdd init` prompt pre-filled with `git config user.name` — `participantPromptModel` in `cmd/sdd/main.go`, gated on terminal + no existing config value so re-runs stay silent.
3. `sdd init --participant <name>` non-interactive write via the new `InitCmd.Participant` field.
4. Idempotent re-init: handler writes only when bytes differ; `SetYAMLField` preserves unknown keys (the `llm:` block from d-tac-bes) and comments via `yaml.Node` tree manipulation; gitignore entries for `.sdd/tmp/` and `.sdd/config.local.yaml` already covered by `ensureGitignoreEntries`.
5. `sdd new` fallback with pure override — `resolveParticipantsFlag` splits the flag when present (no config merge), reads config when absent, errors when both are missing.
6. `sdd wip start` same rule via the singular `resolveParticipantFlag` variant.
7. `/sdd` skill playbook rewritten: the old "read from gitStatus" rule (the drift source) is replaced with CLI-fallback-aware guidance + a before-playback correlation step against `sdd status` participants.
8. `participant-drift` category at medium severity — deterministic Go-side check in `finders/preflight.go` appended after the LLM call.
9. Observation lists the established set via the new `Graph.AllParticipants()` method (sorted, unique, non-empty-only).
10. Per-name validation confirmed by a mixed-set test: a proposed `{Christopher, Bob}` pair with Bob unknown emits exactly one finding targeting Bob.

Design refinement during the session: the first pass used a one-off `SetLocalConfigParticipant` keyed to the participant field; review prompt pointed at networkteam/hosting/vignet's generic `yaml.Patcher`. Second pass ported the dotted-path core (no JSONPath — flat config doesn't need array filters) as `model.SetYAMLField(existing, path, value)` and switched to `yaml.Encoder.SetIndent(2)` so writes match the committed config convention. Future settings (`llm.provider`, etc.) plug in by path string — no per-key helper.

Verification: 11 `SetYAMLField` tests, 3 new init-handler tests, 4 drift-finding tests, 2 `Graph.AllParticipants` tests, 1 participant-merge test. `go vet`, full suite, `golangci-lint run ./...` all clean. Smoke-tested fresh init on a throwaway repo (preserves a commented `llm:` block with an API key), config-fallback on `sdd new`, explicit override verbatim, missing-both error path.

Forward thread: s-cpt-5jk (participant profiles + authorship-based routing) is unblocked — canonical names are now a load-bearing invariant rather than an aspiration.
