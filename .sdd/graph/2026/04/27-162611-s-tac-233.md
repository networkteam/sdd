---
type: signal
layer: tactical
kind: done
refs:
    - 20260427-151153-s-cpt-tdp
closes:
    - 20260427-161411-d-tac-06i
    - 20260427-151210-s-tac-dc3
participants:
    - Christopher
    - Claude
confidence: high
summary: 'Shipped complete summary fidelity tooling meeting all 11 acceptance criteria: explicit-text override with stdin support, multi-entry rejection guards, hash recomputation, manual-summary commit marking, capture-time display in `sdd new`, inline summary rendering in `sdd show`, updated docs and skill guidance, comprehensive test coverage, and end-to-end smoke testing. Fully closes the show-display gap (s-tac-dc3) and operationalizes explicit-summary-control from the drift-gap candidates (s-cpt-tdp), leaving prompt hardening, scope definition, and lint tooling open for future work.'
summary_hash: 4686f14a4b56cbe06a391c002d87aee5
---

Shipped summary fidelity tooling implementing all 11 acceptance criteria from d-tac-06i (commit f9e55ec, plus 2954f9c for the installed skill refresh). Each AC: (1) `sdd summarize <id> --text <value>` writes the supplied text bypassing the LLM, with `--text -` reading from stdin — smoke-tested both paths against a fresh project. (2) `--text` is rejected with a named-constraint error when combined with `--all` or multiple IDs — checked at the CLI layer for fast feedback and re-validated in the handler as defense-in-depth. (3) Hash recomputed from the current prompt input via `RenderSummaryPrompt + ComputePromptHash`, asserted by `TestSummarize_ExplicitText_HashMatchesPrompt`. (4) Commit message marks manual summaries via "(manual)" suffix, asserted by `TestSummarize_ExplicitText_WritesSummaryAndHash`. (5) `sdd new` output now prints the LLM-generated summary after the entry path — wiring extends `OnNewEntry` callback with the summary string so the CLI layer can render it. (6) `sdd show` renders a labeled `Summary:` block between metadata and body when present, omitted entirely when empty — covered by `TestWriteEntryFull_SummarySectionPresent` and `TestWriteEntryFull_SummarySectionOmittedWhenEmpty`. (7) Existing show behaviour preserved — the full presenter test suite passes with no snapshot diffs since prior tests use bodies without summaries on the depth-0 entry. (8) `/sdd` SKILL.md gained a "Verify the captured summary" section instructing agents to read the printed summary, judge fidelity, and offer `sdd summarize --text` correction when drift is spotted. (9) Tests cover all the named paths (handler explicit-text, multi-entry rejection, --all rejection, hash recomputation, show summary present/omitted). (10) CLI reference updated for `sdd new` summary output, `--text` flag, and `sdd show` summary section; the `sdd init` post-edit run scoped its commit cleanly to the two installed skill files. (11) `go vet`, `go test`, and `golangci-lint run` all clean. Smoke test against a fresh temp project exercised the workflow end-to-end: capture entry, run `sdd summarize <id> --text`, confirm `sdd list` and `sdd show` reflect the override, verify both rejection paths and stdin path. Closes the show-display gap (s-tac-dc3) fully and operationalizes the explicit-summary-control candidate in the broader summary-drift gap (s-cpt-tdp); the other three candidates (prompt hardening, scope definition, lint/diff tooling) remain open for separate dialogue, so s-cpt-tdp is referenced rather than closed.
