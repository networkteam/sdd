---
type: signal
layer: tactical
kind: done
closes:
    - 20260420-130323-d-tac-evg
participants:
    - Christopher Hlubek
    - Claude
confidence: high
---

Landed session 3 of the two-type redesign: `sdd rewrite` plumbing, `--missing-kind` filter, and `sdd status` vocabulary expansion for aspirations and activities. Covers CLI ACs #3, #4, #5 and Pre-flight AC #11 of the phased execution plan (d-cpt-e7i).

**CLI AC #3 (--missing-kind)**: `model.GraphFilter` grew a `MissingKind` bool; `Filter` short-circuits when set. CLI flag plumbed on `sdd list`. `TestFilterMissingKind` verifies composition with type filter. Live `./bin/sdd list --missing-kind` surfaces the 98 legacy actions and the kindless signals as expected.

**CLI AC #4 (sdd rewrite)**: New `RewriteEntryCmd`, handler, and CLI subcommand. Handler computes the new ID via `model.RewriteID`, resolves short forms, walks `graph.Downstream` as the inbound-reference set, moves the file (and attachment directory, if present) via an injected `Mover` interface (production `gitMover{}` shells to `git mv`), rewrites frontmatter on the target and each inbound entry, and commits atomically. Supports `--dry-run`, `--no-commit`, `--message`. Rejects no-op and new-ID collision cases with clear errors. Invalidates stale summaries on rewritten entries. Seven tests in `handler_rewrite_test.go` cover happy path, kind-only, dry-run, no-commit, attachment-dir, no-op, and collision.

**CLI AC #5 (status vocabulary)**: `query.StatusResult` grew `Aspirations`; finder populates via `graph.Aspirations()`; presenter renders Contracts → Aspirations → Plans → Active Decisions. `ActiveDecisions()` now excludes aspirations to avoid double-listing. Activities surface in Active Decisions with the kind qualifier visible (`EntryLine` already prints the kind alongside layer/type, so no new code). `TestRenderStatusVocabulary` and `TestActiveDecisionsExcludesAspirations` verify.

**Pre-flight AC #11 (rewrite bypasses pre-flight)**: The rewrite handler never invokes `h.reader.Preflight`; handler doc comment states the intent — mechanical fixes per `d-cpt-e1i` don't warrant LLM validation.

`go test ./...`, `go vet ./...`, and `golangci-lint run ./...` all clean. Code landed in commit 601cbe9.
