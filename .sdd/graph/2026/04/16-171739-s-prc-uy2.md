---
type: signal
layer: process
kind: gap
refs:
    - 20260416-171148-s-prc-8nl
participants:
    - Christopher
    - Claude
confidence: high
summary: Pre-flight validation is missing a check to flag refs that merely document rule-following rather than genuine build-on or dependency relationships (s-prc-8nl). The validator should catch unrelated refs as high-severity issues to prevent the graph from accumulating misleading connections, since explicit refs must only link entries that extend or depend on each other, not those that simply applied shared contracts.
summary_hash: 6dda171fa37397f664991c06274054ce
---

Pre-flight validation does not flag unrelated references. An entry that refs a contract it merely followed rather than builds on should be caught. Refs means builds-on or depends-on, not this rule applied during my work. Contracts apply automatically via the active contracts context. Explicit refs should only exist when the entry genuinely extends or depends on the referenced entry. Calibration should treat unrelated refs as high severity to prevent the graph from accumulating misleading relationships.
