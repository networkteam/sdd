---
type: action
layer: tactical
closes:
    - 20260417-173310-d-tac-lg1
participants:
    - Christopher
    - Claude
confidence: high
summary: 'This action shipped the release pipeline infrastructure at v0.1.0 with GoReleaser v2 config, CI/release workflows with native attestations, golangci-lint v2 baseline, binstaller install.sh, and documentation across 4 commits. It closes the release-pipeline decision (d-tac-lg1), confirmed through end-to-end validation: release workflow succeeded with 6 assets, Homebrew cask published to networkteam/homebrew-tap, curl installer verified on darwin/arm64, and SLSA v1 provenance attestations confirmed.'
summary_hash: 725479de90d576137f47777a896d7d82
---

Closes d-tac-lg1. Release pipeline infrastructure shipped at v0.1.0 (commit 69385f3): GoReleaser v2 config, CI + release workflows with native build-provenance attestations, golangci-lint v2 baseline, binstaller-generated install.sh, and README install docs. Landed across 4 commits (3ce37f3, 0f476a3, 865713d, 69385f3).

End-to-end validation:

- Release workflow green (run 24588939733), 6 assets published
- Cask pushed to `networkteam/homebrew-tap/Casks/sdd.rb`
- `curl | sh` install verified on darwin/arm64 — installs to ~/.local/bin/sdd, binary reports 0.1.0
- `gh attestation verify` passes on both install.sh and a tarball; SLSA v1 provenance binds to commit 69385f35a4f9099e4b6da08cea96ac258d4054d8
- `brew install networkteam/tap/sdd` is the canonical public path (user's machine had a local tap-alias collision with their GitLab mirror — not a project-level issue; alias `networkteam/github/sdd` resolves locally)

Deviations from plan AC wording, all validated safer/better in session:

- **Homebrew mechanism**: `homebrew_casks` instead of `brews` (brews deprecated in GoReleaser v2.10; casks support cross-platform macOS + Linuxbrew with per-OS/arch blocks)
- **Tap repo**: reused existing `networkteam/homebrew-tap` instead of creating a new `networkteam/homebrew-sdd` (standard single-tap-per-org pattern)
- **Attestation tool**: native `actions/attest-build-provenance@v1` instead of `actionutils/trusted-go-releaser` (idiomatic 2026 primitive, no wrapper indirection)
- **Linter shape**: `gosimple` merged into `staticcheck` in golangci-lint v2; `gofmt`/`goimports` moved to `formatters:` section

Process signals captured during execution: s-prc-w5r (dry-run reflex), s-prc-cgk (handoff-boundary drift).

Minor future item: `actions/attest-build-provenance@v1` uses Node 20 (GitHub deprecating June 2026). Non-blocking — version bump when a Node 24 major lands.

See [evidence](./18-101837-a-tac-r8l/validation-evidence.md) for the AC-by-AC matrix with commit SHAs, verification outputs, and deviation rationale.
