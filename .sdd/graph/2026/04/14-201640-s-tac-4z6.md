---
type: signal
layer: tactical
kind: done
refs:
    - 20260414-155925-d-tac-017
closes:
    - 20260414-155925-d-tac-017
    - 20260414-000402-s-ops-dd8
participants:
    - Christopher
    - Claude
confidence: high
preflight: skipped
summary: Completed implementation of depth-limited show command with summary-line rendering per d-tac-017, using DFS deduplication and --max-depth flag (default 4) to bound recursive traversal. Resolves the unbounded output problem (s-ops-dd8) and fulfills the directive (d-tac-017); refactored skills to use single show call and Entry.Summary directly, eliminating redundant reads and --width flag.
summary_hash: 6fbfc2b225383d20c01df022cc3f96bd
---

Implemented depth-limited show with summary lines per d-tac-017. Target entry renders full content; upstream at depth 1+ as indented summary lines with relation labels, kind, and full IDs. --max-depth flag (default 4) bounds expansion; --downstream opts into downstream entries. DFS with per-direction visited sets deduplicates entries (see above markers); truncation markers at max-depth list relation-qualified entry IDs. Tree traversal in model/show_tree.go (pure domain); finder is thin orchestration. sdd list/status/lint use Entry.Summary directly, removing --width flag. Skills (sdd-explore, sdd-catchup, sdd-groom) updated: single sdd show --downstream call, status summaries for catchup narrative, redundant reference file reads dropped. Architecture guideline added to framework/CLAUDE.md.
