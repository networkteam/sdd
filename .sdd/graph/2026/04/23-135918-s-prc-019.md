---
type: signal
layer: process
kind: done
closes:
    - 20260423-135539-s-prc-qxi
participants:
    - Christopher
    - Claude
confidence: high
summary: Both `sdd init` language prompt and README multilingual guidance were shipped in commit 801eea8, closing the user-facing discoverability gap (20260423-135539-s-prc-qxi). The init command now prompts for language on fresh setup with English default, supports a `--language` flag for automation, and writes the choice explicitly to `.sdd/config.yaml` while preserving the commented hint in non-interactive runs. A new README section explains locale effects, bundled options, and English-canonical surfaces, with Quickstart step 1 directing users there.
summary_hash: d959cd13b8fbf3fb731adbd4c2924ae4
---

Closes the multilingual discoverability gap with both fixes landed in commit 801eea8. `sdd init` now prompts for graph language on fresh init (default `en`) and carries a `--language` flag for non-interactive setup — the chosen value is written explicitly as `language: <code>` in `.sdd/config.yaml`, while unset (non-interactive) runs keep the commented hint so the option stays visible to later readers. README gains a `## Multilingual graphs` section covering what enabling a locale does (canonical capture, skill-side vocabulary rendering, English-stays-canonical surfaces) and how to add a new locale, with Quickstart step 1 linking to it. Smoke-verified: `sdd init --language de` writes the uncommented key; no-flag non-interactive init leaves the hint commented.
