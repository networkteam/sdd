---
type: signal
layer: tactical
kind: done
closes:
    - 20260417-123422-d-tac-v4f
participants:
    - Christopher
    - Claude
confidence: high
summary: 'SDD fork to `github.com/networkteam/sdd` complete with 368 commits, idiomatic Go layout, and `.sdd/graph/` dogfooding; all 17 acceptance criteria satisfied (d-tac-v4f). The implementation includes four structural commits plus a history scrub, with two process discoveries captured: CLI documentation drift (a-prc-k0m) and Devbox direnv requirements for GNU tools. Distribution (d-cpt-uu1), resonance path stripping, and two-type redesign (d-cpt-omm) remain pending.'
summary_hash: b1b55fd18d2f2a5c622b61a05872b7bd
---

Fork complete: SDD published to `github.com/networkteam/sdd` with `main` branch, 368 commits (365 from resonance's `future-framework` after `git filter-repo` + 3 reorg commits). The repo dogfoods `.sdd/graph/` as the default graph location, uses `internal/` for Go packages, and keeps `.claude/skills/sdd*` at the Claude-Code-standard path pending the distribution work (d-cpt-uu1). All 17 acceptance criteria from d-tac-v4f are satisfied (see [AC verification](./17-164222-a-tac-q94/ac-verification.md)).

Local clone ready; user handled repo creation on GitHub and `git push`. Implementation spanned four commits: wip-start (f30daf8), Go code reorg to cmd/+internal/ (b46faa0), graph+docs reorg to .sdd/graph/+docs/*.md (98a38b3), and infrastructure — CLAUDE.md/README/LICENSE/gitignore/devbox (8c999e9). A second `git filter-repo` pass scrubbed an accidentally-committed `bin/sdd` binary from history; the step 2 and step 3 hashes in the final repo differ from what was initially composed locally before that scrub.

Two process discoveries captured separately during execution: (1) CLI reference documentation had drifted from actual CLI behavior (a-prc-k0m, on resonance), and (2) fresh clones of Devbox-managed projects need `direnv allow` before GNU tools are in PATH — BSD vs GNU `sed -i` syntax differs (saved to agent memory). Out of scope and still pending: stripping SDD paths from resonance, distribution (d-cpt-uu1), and the two-type redesign (d-cpt-omm).
