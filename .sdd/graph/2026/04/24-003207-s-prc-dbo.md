---
type: signal
layer: process
kind: actor
participants:
    - Christopher
confidence: high
canonical: Claude
aliases:
    - Claude Code
summary: Claude is the AI agent collaborating with Christopher on the SDD graph via Claude Code CLI, participating in dialogue to shape signals and decisions while executing graph operations. The entry establishes `Claude` as the canonical participant name across all existing entries, with `Claude Code` reserved as a distribution alias that does not appear in participants fields per the canonical-only rule (d-cpt-979).
summary_hash: fd454886f9b7537896e358df89788f31
---

Claude is the AI agent Christopher collaborates with on SDD via Claude Code, Anthropic's CLI — participating in dialogue that shapes captured signals and decisions, and executing graph operations on Christopher's behalf. Canonical form `Claude` matches the spelling used on every existing entry's `participants` field. Alias `Claude Code` is the CLI distribution through which Claude currently participates; it never appears in `participants` per the canonical-only rule (d-cpt-979).
