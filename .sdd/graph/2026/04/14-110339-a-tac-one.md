---
type: action
layer: tactical
refs:
    - 20260414-002243-d-prc-22i
closes:
    - 20260414-101221-d-tac-h3o
participants:
    - Christopher
    - Claude
confidence: high
summary: This action implements the pre-flight template alignment decision, replacing rigid structural checks with dialogue-driven validation that respects human reasoning for entry decisions. It closes the alignment decision (d-tac-h3o) by incorporating action-durability requirements referenced from the directive (d-prc-22i) into the closing-action template. The implementation also fixed a missing Attachments field in the formatter that prevented the durability check from functioning.
summary_hash: 503f398ed175efc96592751c0f4405c6
---

Implemented pre-flight template alignment per d-tac-h3o, aligning validation with the principle that dialogue drives graph evolution. Five plan items, three changed by this session, two already present from concurrent session.

(1) supersedes.tmpl: replaced type-consistency check with dialogue-driven replacement — the validator now checks that entry text explains what happens to the superseded entry's concerns and why the replacement takes the form it does, rather than demanding structural form matches. Contracts can now be retired via directive, directives absorbed into plans, etc. (2) decision_refs.tmpl: narrowed ref completeness from "are related signals unreferenced?" to two concrete checks — does the entry duplicate information from existing entries it should reference, and does it logically build upon or advance existing entries without referencing them. Refs represent logical dependency, not topical association. (3) closing_action.tmpl durability section: already present from concurrent session (commit 743bd9e). Three-path durability check (prior commit, this-entry attachments, upstream-entry attachments). (4) SKILL.md line 60: already updated by concurrent session — two-class framing replacing code-changes-only framing. (5) entry_quality.tmpl: extended dialogue-context-is-grounding principle to cover structural departures — when entry text argues for a non-standard structural move, validator treats the reasoning as human-confirmed context.

Additional fix discovered during eval testing: formatEntryForPrompt did not include Attachments field, so the durability check couldn't see attachment metadata. Added Attachments output to the formatter. Eval test fixtures updated: FullCoverage switched from synthetic commit hashes to attachments (attachments are verifiable in graph context; synthetic commits are not), ValidSignal layer corrected from conceptual to tactical (validator correctly caught the mismatch).

All changes committed in 6c69be1. go vet clean, unit tests pass, all 6 eval tests pass.
