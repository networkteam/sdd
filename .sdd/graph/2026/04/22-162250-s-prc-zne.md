---
type: signal
layer: process
kind: done
refs:
    - 20260422-100926-s-cpt-fwk
closes:
    - 20260415-114317-s-prc-hpa
participants:
    - Christopher
    - Claude
confidence: high
summary: 'The pre-flight field-presence gap is now fully resolved with all three proposals implemented: Kind default-to-directive was already landed, missing-kind/missing-confidence checks removed from LLM templates via the operationalization plan (d-cpt-e7i, closed by s-cpt-fwk), and Confidence made required at CLI with no default in commit 2c9e9f3, preserving it as deliberate author self-assessment. This closes the structural validation gap (s-prc-hpa) and completes the two-type redesign foundation (s-cpt-fwk).'
summary_hash: c85100ad9f0099b9597f49e4cdf97bba
---

The pre-flight field-presence gap (s-prc-hpa) is now fully resolved across its three proposals. (b) Kind default-to-directive was already landed by s-ops-4zc before the gap was filed, and (c) removing missing-kind/missing-confidence presence checks from LLM templates landed via d-cpt-e7i (closed by s-cpt-fwk): typed errors in `internal/model/graph.go` enforce structural validation pre-LLM, and `decision_refs.tmpl` instructs the LLM that field-presence checks belong to CLI/lint. (a) The remaining "confidence required at CLI with no default" piece landed in commit 2c9e9f3 — `Required: true` on the confidence flag plus value validation rejecting empty strings and typos with clear error messages, preserving confidence as a deliberate author self-assessment rather than a silent default.
