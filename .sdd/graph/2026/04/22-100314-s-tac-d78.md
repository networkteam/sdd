---
type: signal
layer: tactical
kind: done
refs:
    - 20260422-095909-s-cpt-9qn
closes:
    - 20260420-130650-d-tac-pfi
participants:
    - Christopher Hlubek
    - Claude
confidence: high
summary: 'Session 6 of the two-type migration completed by removing TypeAction from code and intercepting `sdd new a` with a migration error, then sweeping CLI help, README, and skill documentation to reflect the two-type/five-kind vocabulary. Closes the session 6 activity (d-tac-pfi) by addressing all four teardown acceptance criteria plus CLI AC #2, with loader strictness tightened and done-signal lifecycle corrected per the conceptual insight (s-cpt-9qn).'
summary_hash: 041a0da5ef93c282054681676ea3a625
---

Completed session 6 of d-cpt-e7i by removing TypeAction from the codebase, intercepting `sdd new a` with a migration error, and sweeping CLI help, README, and skill documentation for the two-type / five-kind vocabulary. Code and docs committed in 1c6e83c.

**Acceptance criteria addressed:**

- **CLI AC #2** — `sdd new a` removed with clear migration error: Intercept added in `cmd/sdd/main.go` before type resolution; returns an error pointing to `sdd new s --kind done`. Smoke-tested for both `a` and `action` spellings.

- **Teardown AC #1** — No TypeAction constant remains: Removed from `internal/model/entry.go` (constant, `TypeAbbrev` entry, `TypeFromAbbrev` entry, `TypeLabel` case). Loader tightened via new `parseEntryType` that rejects unknown types at load time. Downstream call sites simplified: `validateCloses` dropped the "cannot close action" branch; `selectCheckType` collapsed `isCompletionRecord` to done-kind signals only. `go vet`, `go test`, and `golangci-lint` all clean.

- **Teardown AC #2** — No entry carries `type: action`: Already true pre-session (session 5 migrated all 98 entries). Verified by grep — remaining matches are prose inside migration-discussing entries. `sdd lint` clean.

- **Teardown AC #3** — CLI help, README, and skill documentation reflect the two-type / five-kind vocabulary: CLI help updated for `--type` filter and `new` error messages. README sweep: intro, screenplay (a-tac-q94 → s-tac-q94 kind: done), entry-type section expanded with five signal and five decision kinds, CLI cheat sheet. Skill corpus swept across SKILL.md, meta-process.md, and three sub-skills — entry-type references updated, English-sense "action" preserved.

- **Teardown AC #4** — Skill instructions describe retirement primitives, short-loop calibration, and directive/activity/plan distinguishing tests: Homed in `references/framework-concepts.md` as concept material the agent loads at session start. New sections: Retirement primitives (supersedes vs closes + per-kind table), Distinguishing tests (WHAT-vs-THAT, completion-criterion), Short-loop closure (principle + smell test). Full layer×scope-shape matrix stays in `short_loop.tmpl` where pre-flight applies it. SKILL.md additionally gained a short-loop smell-test bullet and a decision-before-done-signal checkpoint in the implementation-transition flow.

**Deviations with dialogue context:**

- Loader strictness: `parseEntryType` rejects unknown types at load time — a small strictness increase beyond pure teardown, in the spirit of the plan's "tighten at boundaries."
- Done-signal lifecycle: `DerivedStatus` was returning `StatusOpen` for done signals (inherited from the signal default). Fixed to StatusNone per the retirement table's terminal treatment. Not a named AC but surfaced during rendering and informed by `s-cpt-9qn`.
- Handler rewrite tests: retargeted from action-migration fixtures to signal↔decision flips rather than deleted, preserving coverage of rewrite mechanics (file move, attachment dir move, collision, no-commit, dry-run) for future uses.
- AC #4 interpretation: "Skill instructions describe..." taken to include `references/` (what the skill loads), not `SKILL.md` alone.
