---
type: decision
layer: tactical
kind: activity
refs:
    - 20260417-091917-d-tac-q5p
participants:
    - Christopher
    - Claude
confidence: high
summary: Commits to executing the tactical plan (d-tac-q5p) in a single build session by adding a top-level `participant` field to `.sdd/config.local.yaml`, making `sdd init` interactive with git user.name pre-fill, enforcing canonical names at capture, and adding a medium-severity pre-flight `participant-drift` category. Design locks fail-loud semantics (no silent fallback to git.name), schema merging with existing LLM config via idempotent init, and CQRS decomposition across handlers with a new config-read finder. This activity unblocks s-cpt-5jk (participant profiles and authorship-based routing) by establishing canonical-name enforcement so no further mechanical name-normalization sweeps are needed.
summary_hash: de90a2c8898aa04e46aa9bc4af256a5f
---

Commits to executing d-tac-q5p in a single build session, covering all ten acceptance criteria: `.sdd/config.local.yaml` gains a top-level `participant` field (merging with the existing `llm:` schema from d-tac-bes); `sdd init` prompts interactively with `git user.name` pre-filled as the default and writes the config; `sdd init --participant <name>` writes non-interactively; init is idempotent (gitignore entries, directory creation, schema additions) and preserves unknown fields; `sdd new` and `sdd wip start` read config when the flag is omitted and exit non-zero with "run `sdd init`" when both are missing; `/sdd` skill adds a before-playback correlation rule against names visible in `sdd status`; pre-flight gains a medium-severity `participant-drift` category that lists established graph names and validates per-participant (not as a concatenated string).

Dialogue confirmed three load-bearing design points:

- **Fail-loud at capture.** `--participants` is pure override; flag-omitted reads config; flag-omitted with no config errors out. No silent fallback to `git user.name` at capture — that implicit inference is the drift source. `git.name` appears only as the pre-filled default in the `sdd init` prompt.
- **Schema merges.** `.sdd/config.local.yaml` already exists (populated by d-tac-bes for LLM provider config). AC 1 adds a top-level `participant` field alongside `llm:`; AC 4's idempotent init must preserve the LLM section and any other unknown fields.
- **CQRS decomposition per d-cpt-ah1.** `InitHandler` extension for the prompt + config write; `NewEntryHandler` + `WipStartHandler` gain a config-read via a new finder over `.sdd/config.local.yaml`; new pre-flight template in `internal/llm/`; no presenter changes. Model gains a per-name drift-comparison helper.

Trigger: today's mechanical normalization (38 entries, "Christopher Hlubek" → "Christopher") confirmed drift continues to accumulate across sessions while the plan remains unimplemented. Activity ships canonical-name enforcement at capture so no further mechanical sweeps are needed.

Forward thread: s-cpt-5jk (participant profiles + authorship-based routing) builds on canonical names as its foundation; unblocked by this activity landing.
