---
type: signal
layer: tactical
kind: done
closes:
    - 20260417-112234-d-tac-3yi
participants:
    - Christopher
    - Claude
confidence: high
summary: Implemented computed-state notation across the SDD CLI and skill docs with `model.Status` and `Graph.DerivedStatus()`, rendering stored attributes in square brackets and derived status in curly braces (with labeled sections in `sdd show` to avoid nesting). This closes the plan to render entry state as a derived attribute across CLI surfaces (20260417-112234-d-tac-3yi), enabling agents to distinguish proposals from facts. Kind now renders as an identity qualifier rather than an attribute, and framework documentation clarifies that actions carry no status, only execution facts.
summary_hash: ab649eea07a6a5c1e37098107ff9b09d
---

Implemented computed-state notation across the SDD CLI and skill docs (commit 2b241c8). Added `model.Status` + `Graph.DerivedStatus()` computing state from reverse indexes; presenters render kind as an identity qualifier, `[confidence: X]` for stored attrs, `{status: Y}` for derived — except in `sdd show` where the header uses a labeled `Derived:` section with plain value, avoiding the redundant `Status: {status: ...}` wrapping.

AC coverage:
- Stored attrs in square brackets: done for `[confidence: X]`. Deviation: kind is no longer rendered as `[kind: X]` — dialogue during implementation established that with d-cpt-omm's two-type future, kind is identity (like layer/type), not attribute. Kind renders as a qualifier (`tactical plan decision`), matching the pre-d-tac-3yi format.
- `{status: active|open|closed-by <id>|superseded-by <id>}` across `sdd status`, `sdd list --all`, summary chains, grooming playbook: done.
- `sdd show` header: done via labeled `Derived:` section. Deviation from literal `{status: ...}` — curly-brace notation is reserved for flat inline contexts; a labeled block already makes the stored/derived split explicit.
- Summary chains carry kind + `{status}` next to each entry ID: done.
- Grooming: Status column added to outer-skill table; sub-skill block uses `{status: open}` / `{status: active}`.
- Space separator in compound values (`closed-by <full-id>`, `superseded-by <full-id>`): done.
- `framework-concepts.md` documents the stored/derived split and "actions don't carry status — they're facts of execution": done.

Spun off signal s-tac-93s about `sdd list` ordering / tail-access ergonomics — orthogonal to this plan.
