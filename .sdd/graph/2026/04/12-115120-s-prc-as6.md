---
type: signal
layer: process
kind: gap
refs:
    - 20260412-114647-d-cpt-omm
    - 20260412-001439-s-tac-umu
participants:
    - Christopher
    - Claude
confidence: high
summary: Pre-flight validation cannot distinguish entries that reshape graph patterns from those building on them, producing category-error rejections alongside substantive concerns during two-type design capture—notably 'ungrounded kinds' and 'alters without superseding'—with significant retry costs. This limitation affects the validator (s-tac-umu) when processing architecture-changing decisions like the two-type design (d-cpt-omm).
summary_hash: c739295f72511a565058ea5986e3a853
---

Pre-flight validation doesn't distinguish between entries that build on existing graph patterns and entries that intentionally reshape those patterns. During capture of the two-type design (d-cpt-omm), the validator rejected three times. First rejection (missing related entry s-cpt-s43) was genuinely helpful. Second rejection produced four concerns: two were valuable (missing gap signal pushed proper signal-before-decision flow; missing ref to s-prc-5ht caught a real connection) and two were category errors ('kinds introduced without grounding' — a directive introducing new concepts inherently contains ungrounded material; 'alters contracts without superseding' — the entry explicitly deferred superseding to post-validation, which the prompt couldn't assess). The re-assembly cost of a large heredoc command with attachment via stdin across three attempts was significant — each retry required re-sending the full attachment content.
