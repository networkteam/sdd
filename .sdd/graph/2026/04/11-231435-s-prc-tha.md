---
type: signal
layer: process
kind: gap
refs:
    - 20260411-194231-s-tac-9ec
    - 20260410-110413-s-prc-dk3
participants:
    - Christopher
    - Claude
confidence: medium
summary: Claude Code sessions remain bound to their project directory when sdd wip start --branch creates sibling worktrees, requiring users to manually start new agent sessions and blocking concurrent work in the creating session. This signals a session setup automation gap (20260411-194231-s-tac-9ec) and relates to broader coordination challenges in multi-participant graph work (20260410-110413-s-prc-dk3), suggesting the default should remain simple git branching without worktrees unless users explicitly opt into the overhead.
summary_hash: 9ccdddd02e083cef67c6455824a21d58
---

Claude Code sessions are bound to their project directory. When sdd wip start --branch creates a sibling worktree, the user must manually start a new agent session there — the creating session cannot continue in the worktree. Graph context transfers fine via git, but session setup is not automatic. For now, the default should be branching without worktrees (just git branch, no concurrent work on the same repo directory on the same system). Worktree creation should be an explicit opt-in for users who need true concurrent work and accept the session transition overhead. This keeps the common case simple and defers the tooling friction to when it's actually needed.
