---
type: signal
layer: tactical
kind: done
refs:
    - 20260420-164048-s-prc-y0e
closes:
    - 20260420-164048-s-prc-y0e
participants:
    - Christopher
    - Claude
confidence: high
summary: Fixed the missing-explicit-kind pre-flight false positive by removing the TypeDecision gate in summary.go:119, allowing signals to include kind in the prompt, and identified the root cause as a missing field in the prompt itself. Also corrected the compressed summary path to render the full layer-kind-type triple for proper kind-routing. This resolves the pre-flight validation gap (20260420-164048-s-prc-y0e) with regression test coverage and commit d957ed9.
summary_hash: 91b541f663e93cc16a25a4438741a1a3
---

Fixed the missing-explicit-kind pre-flight false positive at internal/llm/summary.go:119 by dropping the `e.Type == TypeDecision` gate — signals now carry their kind into the pre-flight prompt. Deterministic root cause: the LLM was truthful, the prompt genuinely omitted the field. Regression test `Test_FormatEntryForPrompt_ShowsKindForSignals` added.

Also fixed the parallel issue in the compressed summary path (summary.go:85), which rendered related entries as `[rel] Type (ID)` — now renders the "layer kind type" triple so kind-routing works across the summary generator too. Commit d957ed9.
