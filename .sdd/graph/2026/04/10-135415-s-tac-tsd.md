---
type: signal
layer: tactical
kind: done
refs:
    - 20260410-122858-d-tac-kfo
participants:
    - Christopher
    - Claude
summary: Implemented sdd lint command with validation checks for dangling references, malformed IDs, and type mismatches in closes and supersedes relationships, integrating warnings into the graph construction process for inline display. Testing against the live graph identified 4 issues across 3 entries. Partially fulfills decision d-tac-kfo, which also requires validation of broken and missing attachment references—that requirement remains unimplemented.
summary_hash: d56f88df0922dae8b37dce84bada3c6b
---

Built sdd lint command with checks for dangling refs (non-existent entries), malformed IDs (short suffixes), type mismatches in closes (signal can't close, action can't be closed, decision can't close decision), and type mismatches in supersedes (must be same type). Warnings are populated during graph construction on the Entry struct so sdd show displays them inline. Running against the live graph found 4 issues in 3 entries. Does NOT yet cover broken or missing attachment references — that requirement from d-tac-kfo remains unimplemented.
