---
type: signal
layer: tactical
kind: done
closes:
    - 20260414-232739-d-tac-gyk
participants:
    - Christopher
    - Claude
confidence: high
preflight: skipped
summary: 'Implemented LLM metadata surfacing with llm.Runner.Run returning *RunResult containing agent-neutral LLMMetadata, isolated Claude-specific handling in a new llm/claude subpackage, and added structured slog-based per-call logging with -v and -vv verbosity flags showing tokens, cost, and duration per model. This work closes the tactical plan decision (20260414-232739-d-tac-gyk) by fulfilling all acceptance criteria: metadata surfaces alongside response text, command and operation context appears in logs, existing functionality remains unchanged, and tests pass.'
summary_hash: ff0807a4e082cc1c0ae86151fcdecdda
---

Implemented LLM metadata surfacing: changed llm.Runner.Run to return *RunResult with agent-neutral LLMMetadata, created llm/claude subpackage with Claude CLI runner using --output-format json, removed claudeRunner from cmd/sdd/main.go. Preflight and Summarize log per-call metadata via internal logCallResult under slog.Group("llm") with per-model sub-groups. CLI accepts -v (info) and -vv (debug) flags via slogutils context-carried loggers, with command handlers setting "command" attr. All ACs met: metadata returned alongside text, claude code isolated in subpackage, -vv shows cost/tokens/duration per model, log lines show command and op, existing functionality unchanged, tests pass.
