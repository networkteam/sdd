---
type: decision
layer: tactical
kind: plan
refs:
    - 20260413-160358-d-stg-574
    - 20260413-142536-d-cpt-ah1
    - 20260413-161531-d-cpt-uu1
supersedes:
    - 20260413-094409-d-cpt-dlw
participants:
    - Christopher
    - Claude
confidence: high
summary: This tactical plan operationalizes SDD's fork into github.com/networkteam/sdd by extracting SDD-relevant paths from resonance's future-framework branch with preserved Git history, reorganizing to idiomatic Go layout, and establishing repo infrastructure. It supersedes the previous fork directive (d-cpt-dlw), preserves CQRS separation (d-cpt-ah1), and aligns with agent-platform-neutrality (d-stg-574) and distribution infrastructure choices (d-cpt-uu1).
summary_hash: 69132ae3da98dadd98e3f4f2e50cacda
---

Fork SDD into a standalone repo at `github.com/networkteam/sdd` by extracting SDD-relevant paths from resonance's `future-framework` branch with preserved Git history, reorganizing to idiomatic Go layout, and adding repo infrastructure (CLAUDE.md, README, MIT license). Primary branch named `main` in the new repo; resonance itself unchanged in this plan's scope.

Extraction via `git filter-repo` on: `framework/`, `.claude/skills/sdd*`, `.sdd/config.yaml`, `docs/framework/`, `.envrc`, `devbox.json`, `.gitignore`. Dropped: `.claude/settings.local.json`, `devbox.lock` (regenerated), `skills/`, `install.sh`, `package.json`, `docs/spec/`, resonance's `CLAUDE.md`/`README.md`. Reorg: `framework/cmd/sdd/` → `cmd/sdd/`; `framework/sdd/*` → `internal/*`; `docs/framework/graph/` → `.sdd/graph/` (dogfooding `model.DefaultGraphDir`); `docs/framework/*.md` → `docs/*.md` (flatten); root `go.mod` with module `github.com/networkteam/sdd`; all imports updated. Skills stay at `.claude/skills/sdd*` — relocation to `skills/` with `//go:embed` deferred to d-cpt-uu1. `bin/` gitignored. `devbox.json` drops `nodejs@24`. No git tag initially; v0.1.0 deferred to d-cpt-uu1. MIT license.

Supersedes d-cpt-dlw by operationalizing the fork directive. Informed by agent-platform-neutrality (d-stg-574) — CLI as neutral interface, so `internal/` for Go packages. Preserves CQRS separation (d-cpt-ah1) by keeping `finders/` and `presenters/` distinct. Graph at `.sdd/graph/` dogfoods SDD's own default convention. Resonance strip-out is out of scope — separate follow-up. See [full plan](./17-123422-d-tac-v4f/plan.md).

## Acceptance criteria

- [ ] Repository `github.com/networkteam/sdd` exists with `main` branch pushed
- [ ] New repo's default branch is `main` (no `future-framework` branch carried over)
- [ ] Git history preserved — earlier SDD commits traceable via `git log`
- [ ] File layout: `cmd/sdd/`, `internal/{command,finders,handlers,llm,meta,model,presenters,query}/`, `.claude/skills/sdd*`, `.sdd/graph/` with entries + `wip/`, `docs/*.md`, `bin/` (gitignored)
- [ ] Root `go.mod` declares module `github.com/networkteam/sdd`
- [ ] `go build -o bin/sdd ./cmd/sdd` produces a working binary
- [ ] `go test ./...` passes
- [ ] `./bin/sdd status` runs against `.sdd/graph/` showing all extracted entries
- [ ] CQRS separation preserved: `internal/finders/` and `internal/presenters/` remain distinct packages (d-cpt-ah1)
- [ ] Graph dogfoods SDD default: entries at `.sdd/graph/` per `model.DefaultGraphDir`
- [ ] `CLAUDE.md` covers Go stack, build/test commands, project conventions, binary location
- [ ] `README.md` covers project intro, install-from-source, basic usage
- [ ] `LICENSE` file contains MIT license
- [ ] `.gitignore` includes `bin/`
- [ ] `devbox.json` declares `go@1.26`, does not include `nodejs`
- [ ] No resonance-specific paths present (`skills/`, `docs/spec/`, `install.sh`, `package.json`, `.claude/settings.local.json`)
- [ ] No git version tag present (v0.1.0 deferred to d-cpt-uu1)
