---
type: signal
layer: tactical
kind: gap
refs:
    - 20260408-165659-s-prc-7xm
    - 20260408-163946-s-prc-k07
participants:
    - Christopher
    - Claude
summary: The `sdd new` command stores short ID suffixes in closes and refs fields, but the CLI cannot resolve them back to full IDs during queries, rendering closure captures ineffective. This bug was discovered during the first grooming session (s-prc-7xm) and exposed by sdd-groom (s-prc-k07), requiring either suffix expansion at write time or resolution at query time.
summary_hash: c1da077a3f3698ccae0ae95994bf8997
---

sdd new stores short ID suffixes in closes and refs fields (e.g. closes: [0un, k6r]) but the CLI cannot resolve short suffixes back to full IDs during graph queries. Closures captured today (oka closing 0un/k6r, k07 closing oka, 8g2 closing mqd) are all ineffective — the graph still shows these entries as open. The CLI needs to either expand short IDs to full IDs at write time, or resolve short suffixes at query time.
