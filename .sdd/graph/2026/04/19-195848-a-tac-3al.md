---
type: action
layer: tactical
closes:
    - 20260418-151212-d-tac-r5g
participants:
    - Christopher
    - Claude
confidence: high
summary: 'Shipped the unified sdd init flow per decision d-tac-r5g, implementing embedded skill extraction with versioning stamps and write-command gates. Two dialogue-validated deviations adjusted the plan: moving skills source-of-truth to internal/bundledskills/ while preserving the installed output in .claude/skills/, and adding a --force flag for non-interactive overwrite. Implementation verified in ac-verification.md with commits 57e1ebf and 5c42bd9.'
summary_hash: 31bd97be690d999cc064b7cac860d067
---

Shipped the unified `sdd init` flow per plan d-tac-r5g: skills embed via `go:embed` from `internal/bundledskills/claude/` and extract with `sdd-version` + `sdd-content-hash` stamps; `.sdd/meta.json` holds `graph_schema_version` and write-once `minimum_version`; write commands (`new`, `wip start|done`, `summarize`) gate on both fields via a dispatcher-level guard (dev builds bypass).

Implementation commit `57e1ebf`, dogfooded self-refresh commit `5c42bd9`.

Two dialogue-level deviations from the plan: (a) source-of-truth for skills moved to `internal/bundledskills/` while `.claude/skills/` became the installed output (user confirmed at decision point), preserving this session's live skill during implementation; (b) added a `--force` flag beyond the plan's original callback-only design for non-interactive overwrite.

See [ac-verification](./19-195848-a-tac-3al/ac-verification.md) for per-AC evidence.
