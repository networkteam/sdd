---
type: signal
layer: process
kind: question
refs:
    - 20260406-115547-d-cpt-o8t
participants:
    - Christopher
summary: Signals the question of whether each graph entry warrants its own Git commit, proposing that actions reference commit hashes to create traceability from decisions to code. This relates directly to the immutable Git-based decision graph architecture (d-cpt-o8t), which establishes the entry structure and how state reconstructs via graph traversal rather than caching.
summary_hash: d10b2fe23ac3b2bba9bc083cec5ac34e
---

When should graph entries be committed to Git? Should every captured entry be its own commit? Actions that record implementation work could reference commit hashes to link graph entries to actual code changes. This connects the decision graph to the Git history — traceability from decision to code.
