---
type: decision
layer: tactical
kind: directive
refs:
    - 20260420-113731-d-cpt-e7i
participants:
    - Christopher Hlubek
    - Claude
confidence: high
summary: Commits to session 1 of the three-phase type system migration (d-cpt-e7i) by landing model primitives and CLI surface for the new two-type system while maintaining backward compatibility with the legacy action path. Addresses architecture design, all five model and type system acceptance criteria, CLI kind selection with defaults, and structural validation as typed errors before LLM pre-flight checks.
summary_hash: dc69e7e869e9af6d477e3127c782e02b
---

Commits to session 1 of executing `d-cpt-e7i`: land the model primitives and CLI surface that accept the new type system alongside the legacy action path. Keeps `sdd new a` working so the graph stays operational. Covers: Architecture AC; all Model & type system ACs (5); CLI AC #1 (`sdd new s/d --kind` with defaults); Pre-flight & validation AC #1 (structural validation as typed errors before LLM pre-flight).
