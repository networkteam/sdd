---
type: decision
layer: tactical
kind: activity
refs:
    - 20260420-113731-d-cpt-e7i
participants:
    - Christopher
    - Claude
confidence: high
summary: 'Commits to phase 1 execution of the two-type system migration (d-cpt-e7i): implementing model primitives and CLI surface for the new type system while preserving backward compatibility with `sdd new a`. Covers architecture, all five model and type system acceptance criteria, CLI kind-selection with defaults, and structural pre-flight validation as typed errors. Directly executes the conceptual plan (d-cpt-e7i) to operationalize the signal-decision redesign.'
summary_hash: 4b3f3e287e7c1095b46c1122abaf6c96
---

Commits to session 1 of executing `d-cpt-e7i`: land the model primitives and CLI surface that accept the new type system alongside the legacy action path. Keeps `sdd new a` working so the graph stays operational. Covers: Architecture AC; all Model & type system ACs (5); CLI AC #1 (`sdd new s/d --kind` with defaults); Pre-flight & validation AC #1 (structural validation as typed errors before LLM pre-flight).
