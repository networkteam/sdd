---
type: decision
layer: tactical
kind: directive
refs:
    - 20260420-113731-d-cpt-e7i
participants:
    - Christopher Hlubek
    - Claude
confidence: high
summary: This decision commits to executing phase 1 of d-cpt-e7i by landing model primitives and CLI surface for the new type system while preserving backward compatibility with legacy actions. It establishes the architectural foundation and acceptance criteria for type system support, CLI `--kind` defaults, and structural validation before pre-flight, enabling the two-type redesign (d-cpt-e7i).
summary_hash: 402f1da247b43c40f0144e36f8dad050
---

Commits to session 1 of executing `d-cpt-e7i`: land the model primitives and CLI surface that accept the new type system alongside the legacy action path. Keeps `sdd new a` working so the graph stays operational. Covers: Architecture AC; all Model & type system ACs (5); CLI AC #1 (`sdd new s/d --kind` with defaults); Pre-flight & validation AC #1 (structural validation as typed errors before LLM pre-flight).
