---
type: signal
layer: tactical
kind: gap
refs:
    - 20260420-130432-d-tac-grr
    - 20260420-113731-d-cpt-e7i
participants:
    - Christopher Hlubek
    - Claude
confidence: high
---

Post-migration `sdd status` is unusable: Open Signals floods with terminal `kind: done` entries (100+ after session 4) and Recent Actions is empty. The two-type migration (d-tac-grr per d-cpt-e7i) swapped action semantics into `kind: done` signals, but the plan's CLI AC for `sdd status` only covers aspiration/activity distinguishability — it doesn't address the Open Signals filter or a recent-done replacement. Discovered post-migration on 2026-04-20: the converted done signals plus existing kindless-now-gap signals produce a catch-up view that's impossible to scan.

Dialogue-captured fix direction: filter `kind: done` out of `OpenSignals()` at the model level (so `sdd status` and `sdd list` default both benefit from the same semantic — done signals are terminal facts of execution, not attention items), and replace the Recent Actions section with a Recent Done Signals section returning the last N done signals chronologically (default raised 5 → 10 to surface a useful activity stream). Folded into session 4 implementation.
