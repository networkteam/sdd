---
type: signal
layer: tactical
kind: done
refs:
    - 20260420-130432-d-tac-grr
closes:
    - 20260420-152135-s-tac-vk7
participants:
    - Christopher
    - Claude
confidence: high
summary: 'Completed the `sdd status` usability fix by filtering `kind: done` signals from the Open Signals view and replacing Recent Actions with a Recent Done Signals section showing the last 10 captures. This resolves the post-migration flooding issue (s-tac-vk7) where terminal entries cluttered the attention view, and executes the session 4 migration plan commitment (d-tac-grr) with model-level changes verified end-to-end across 23 open gaps and converted entries.'
summary_hash: 9dd6e050db12ea7eacdd0648cdb47ab2
---

Folded the `sdd status` usability fix into session 4 implementation. Model-level changes: `OpenSignals` now excludes `kind: done` signals — terminal facts of execution don't belong in the attention-item view; `RecentActions(n)` renamed to `RecentDone(n)`, returning the last N `kind: done` signals chronologically. `StatusQuery.RecentActions` → `RecentDone` with default raised 5 → 10 for a more useful activity stream. Presenter's "Recent Actions" section replaced by "Recent Done Signals". Tests updated (model + finders). End-to-end verified via `sdd status`: Open Signals shows real open gaps only (23 entries), Recent Done Signals shows the last 10 captures including session 4's own converted entries. Shipped as commit 98da000.
