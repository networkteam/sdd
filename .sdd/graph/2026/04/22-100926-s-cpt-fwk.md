---
type: signal
layer: conceptual
kind: done
closes:
    - 20260420-113731-d-cpt-e7i
participants:
    - Christopher Hlubek
    - Claude
confidence: high
summary: Completes the two-type redesign and aspiration migration plan across six sessions, establishing exactly two entry types (signal and decision) with five kinds each, and addressing all architecture, model, CLI, pre-flight, migration, and teardown acceptance criteria. Closes the operationalization plan (d-cpt-e7i) by landing 98 action-to-done-signal rewrites, assigning kinds to 115 kindless signals, removing TypeAction scaffolding, and enforcing structural validation through CQRS-aligned architecture (d-cpt-ah1). Surface execution signals (s-tac-vk7, s-tac-2bw, s-prc-ze3, s-cpt-9qn) captured as follow-ups outside plan scope.
summary_hash: 7d8cc026d507c57809ddf2b18786db16
---

Completes the two-type redesign + aspiration migration plan across six sessions. The graph now recognizes exactly two entry types (signal and decision), five signal kinds (gap/fact/question/insight/done) and five decision kinds (directive/activity/plan/contract/aspiration). All architecture, model, CLI, pre-flight, migration, and teardown ACs addressed.

**ACs addressed, by section:**

- **Architecture (1)** — CQRS decomposition per `d-cpt-ah1` honored throughout: commands in `command/`, reads in `query/` + `finders/`, side effects in `handlers/`, pure domain in `model/`, pre-flight templates in `llm/`.

- **Model & type system (5)** — Landed session 1 (`d-tac-paz` / `s-tac-5jq`): type validation to signal/decision; kind fields with defaults (gap, directive); signal-kind validation; decision-kind validation; done-signal closes/refs structural requirement.

- **CLI (5)** — ACs #1, #3, #4, #5 in sessions 1 and 3 (`d-tac-paz`; `d-tac-evg` / `s-tac-uc1`) — `new --kind`, `list --missing-kind`, `rewrite`, `status` expanded vocabulary. AC #2 in session 6 (`d-tac-pfi` / `s-tac-d78`) — `sdd new a` migration error.

- **Pre-flight & validation (12)** — AC #1 (structural validation as typed errors) in session 1. ACs #2–#11 in session 2 (`d-tac-o1k` / `s-tac-020`) — template renames (closing_done, short_loop), new templates (dissolution, aspiration_capture), shared partials (unrelated_refs, unusual_close), retirement-rationale and directive-reads-aspiration checks, calibration discipline. AC #12 (rewrite bypasses pre-flight) in session 3.

- **Migration (6)** — ACs #1, #2, #3, #5, #6 in session 4 (`d-tac-grr` / `s-tac-hrn`): `d-cpt-ydf` contract captured before migration commits; 98 action entries rewritten to done signals with inbound refs updated; 115 kindless signals gained kinds; `sdd lint` clean; `sdd summarize --all` run. AC #4 (judgment-pass retagging) in session 5 (`d-tac-9zv` / `s-tac-23e`) — 12 activity retags across tac/ops layers.

- **Teardown (4)** — All in session 6 (`d-tac-pfi` / `s-tac-d78`): TypeAction removed; no `type: action` in the graph; CLI help / README / skill docs swept; framework-concepts.md gained retirement primitives, distinguishing tests, and short-loop principle.

**Execution-surfaced signals worth naming (outside plan scope, captured as follow-ups):**
- `s-tac-vk7` / `s-tac-2bw` / `s-prc-ze3` — presentation-surface restructuring emerged during migration; plan missed catch-up view consequences of expanded vocabulary. Fix folded into session 4.
- `s-prc-nck` — agent proposed ungrounded pre-flight template redesign during planning; caught and corrected.
- `s-cpt-9qn` — done-signal terminal semantics insight; open as future refinement.

Code landed across commits spanning 2026-04-20 through 2026-04-22.
