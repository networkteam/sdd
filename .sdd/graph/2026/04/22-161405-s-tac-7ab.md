---
type: signal
layer: tactical
kind: done
refs:
    - 20260422-143830-s-prc-qom
closes:
    - 20260422-144605-d-tac-6z1
participants:
    - Christopher
    - Claude
confidence: high
summary: Implemented LLM-judged pre-flight participant validation with config canonical as authoritative input, closing the tactical plan (d-tac-6z1). Config is now injected at finder construction rather than embedded in PreflightQuery, preserving query purity while strengthening layering; config parsing distinguishes "no repo" from hard errors across all write paths. Smoke testing revealed and corrected a rubric calibration gap where spelling variance was conflated with identity inference—refined rubric now judges form only, accepting distinct names co-listed (e.g., `[Christopher, Christian]`) but blocking spelling variants (e.g., `[Christopher, Christopher Hlubek]`), with all four paths verified via dry-run tests and canonical now surfaced in `sdd status` output per AC coverage.
summary_hash: 09444f7c63ee31d80b7bda27eb36c3e4
---

Extended pre-flight participant validation to an LLM-judged check with config canonical as authoritative input, closing d-tac-6z1. Plan AC 1's literal shape — `PreflightQuery.LocalCanonical` populated by the handler — was deviated from during implementation: config is now injected into the finder at construction (`finders.Options{Config: *model.Config}`) and `localParticipant()` is an internal helper. Same invariant (config canonical is authoritative), cleaner layering (query stays pure intent, handler doesn't know about preflight plumbing). Additionally, `loadConfig` now distinguishes "no repo" (silent nil) from "parse failure" (hard error) and every write-path call site propagates — the previous `cfg, _ := ...` anti-pattern is gone.

Smoke-testing surfaced a rubric calibration gap: the initial rubric conflated form-level spelling variance with person-identity inference, causing the LLM to block `[Christopher, Christian]` despite both being distinct names. The rubric was refined mid-implementation to judge on form only, with a co-listing defense (enumeration asserts distinctness) — `[Christopher, Christian]` now passes; `[Christopher Hlubek]` alone still blocks; `[Christopher, Christopher Hlubek]` still blocks because co-listing doesn't defend spelling variants. All four rubric paths verified via dry-run smoke tests.

AC coverage:

- AC 1 (deviated): config injected via Finder Options; PreflightQuery unchanged; malformed config fails hard.
- AC 2: `llm.Preflight(…, localCanonical string)`; `preflightContext` gains `LocalCanonical` + `EstablishedParticipants`; new `participants.tmpl` partial in every per-kind system template.
- AC 3: Six exemplars in the rubric — canonical match (pass), spelling variant alone (high), variant abbreviated alone (high), distinct name co-listed (pass), distinct name introduced (pass), distinct name mentioned-not-introduced (high), bootstrap (pass).
- AC 4: `participantDriftFindings` removed; LLM is single path.
- AC 5: `Local participant: <canonical>` header renders above `Graph:` counts; empty config shows `(not configured — run sdd init)`.
- AC 6: `/sdd` SKILL.md names the status header as canonical source; bundled source + installed copy stamped.
- AC 7: Three prompt-plumbing tests at the finder (canonical flows in, bootstrap framing, missing-config marker); presenter tests for the header. End-to-end rubric behaviour depends on LLM provider — tested via dry-run smoke suite, not asserted in unit tests (plan open question: mocking strategy for non-deterministic LLM-layer behavior remains deferred).
