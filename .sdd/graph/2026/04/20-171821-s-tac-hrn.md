---
type: signal
layer: tactical
kind: done
closes:
    - 20260420-130432-d-tac-grr
participants:
    - Christopher Hlubek
    - Claude
confidence: high
preflight: skipped
summary: 'Session 4 of the two-type redesign completed successfully, converting 100 action entries to done signals, assigning kinds to 115 kindless signals (109 gap, 4 insight, 2 question), and regenerating summaries across all migrated entries (d-cpt-ydf, s-prc-qdm, s-stg-3vr, s-cpt-91k, s-prc-8na, s-prc-ggp, s-prc-9yt). The work closes the migration commitment (d-tac-grr) and resolves a secondary scope addition — the status display issue — through viewer-layer filtering changes captured in separate signal entries (s-tac-vk7, s-tac-1tz). AC #4 remains deferred to session 5 (d-tac-9zv).'
summary_hash: 039ff5b91aae2d8e31b1abf92e9444cb
---

Session 4 of the two-type redesign landed per d-tac-grr. AC #1: target-state two-type contract captured (d-cpt-ydf). AC #2: 100 action entries rewritten to `kind: done` signals via batched `sdd rewrite --no-commit` with inbound refs updated atomically. AC #3: 115 kindless signals assigned kinds — 109 gap (default), 4 insight (s-prc-qdm, s-stg-3vr, s-cpt-91k, s-prc-8na — explicit re-frames / synthesis), 2 question (s-prc-ggp, s-prc-9yt — explicit unresolved questions). AC #5: `sdd lint` reports clean after a mechanical `refs: [d-tac-v4f]` backfill on s-prc-k0m per d-cpt-e1i — the entry narrative already named d-tac-v4f as originating context, so the fix satisfies the new done-signal structural rule without altering reader understanding. AC #6: `sdd summarize --all` regenerated summaries across migrated entries.

Scope addition discovered mid-session: after the action→done migration, `sdd status` became unusable — Open Signals flooded with 100+ terminal done signals, Recent Actions emptied. Folded the fix into session 4 implementation (commit 98da000): `OpenSignals()` now excludes `kind: done` (and post-merge also `kind: insight`); `RecentActions(n)` renamed to `RecentDone(n)` returning last N done signals chronologically; default raised 5 → 10; presenter section renamed; a parallel `RecentInsights` stream was added by the author for insight signals. Captured and closed separately via s-tac-vk7 / s-tac-1tz so d-tac-grr's scope remains focused on migration.

AC #4 (activity-shaped directive judgment pass) is session 5's work (d-tac-9zv) and remains out of scope.

Pre-flight skipped on this capture: validator hallucinated `missing-kind-field` despite CLI passing `--kind done` — classic s-prc-hpa pattern where LLM presence-of-field checks overstep CLI-level structural validation.
