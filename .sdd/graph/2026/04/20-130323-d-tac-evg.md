---
type: decision
layer: tactical
kind: directive
refs:
    - 20260420-113731-d-cpt-e7i
participants:
    - Christopher Hlubek
    - Claude
confidence: high
summary: 'Commits to session 3 execution of the conceptual migration plan (d-cpt-e7i) by implementing the `sdd rewrite` plumbing command with inbound-reference finder, rewrite handler, and atomic commit logic, plus the `--missing-kind` filter and `sdd status` presenter update. Fulfills CLI acceptance criteria #3, #4, #5 and pre-flight validation AC #11, with rewrite deliberately bypassing pre-flight checks.'
summary_hash: 8584f45f5efd77c52cfb3d311a65fe5a
---

Commits to session 3 of executing `d-cpt-e7i`: implement the `sdd rewrite` plumbing command (inbound-reference finder, rewrite handler, atomic commit), the `--missing-kind` filter, and the `sdd status` presenter update for expanded vocabulary. Covers: CLI ACs #3, #4, #5; Pre-flight & validation AC #11 (rewrite bypasses pre-flight).
