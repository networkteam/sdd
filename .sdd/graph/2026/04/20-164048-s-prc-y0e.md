---
type: signal
layer: process
kind: gap
refs:
    - 20260415-114317-s-prc-hpa
    - 20260420-150405-d-cpt-ydf
participants:
    - Christopher
    - Claude
confidence: high
summary: Pre-flight reports a false positive `missing-explicit-kind` finding even when entries carry an explicit `kind` field, confirmed across two sessions, indicating the LLM-based field-presence check cannot reliably detect metadata fields that are present. This confirms the structural validation gap identified in s-prc-hpa and demonstrates why deterministic field checks should move to CLI parse time rather than semantic validation. The finding directly supports the type system contract established in d-cpt-ydf by highlighting validation failures under the current architecture.
summary_hash: 3819e33226f85981cd2be9e6dfa90333
---

Pre-flight reports `missing-explicit-kind` as a medium finding even when the entry carries an explicit `kind` field — confirmed false positive in two sessions (s-stg-ljq and another). The LLM-based field-presence check (s-prc-hpa) cannot reliably detect metadata fields that are present, producing noise that erodes trust in pre-flight findings.
