---
type: signal
layer: process
kind: gap
refs:
    - 20260415-114317-s-prc-hpa
    - 20260420-150405-d-cpt-ydf
participants:
    - Christopher Hlubek
    - Claude
confidence: high
summary: A process gap signal documenting a confirmed false positive where pre-flight incorrectly reports missing explicit kind fields despite their presence, demonstrating that the LLM-based metadata checks cannot reliably detect present fields and introduce noise. This finding directly supports the earlier diagnosis (s-prc-hpa) that metadata presence validation should move to deterministic CLI parsing rather than remain in LLM templates, and aligns with the canonical SDD vocabulary contract (d-cpt-ydf) that formally defines the Kind field structure.
summary_hash: 038a0f51f9be2147c80c98ee234d22f3
---

Pre-flight reports `missing-explicit-kind` as a medium finding even when the entry carries an explicit `kind` field — confirmed false positive in two sessions (s-stg-ljq and another). The LLM-based field-presence check (s-prc-hpa) cannot reliably detect metadata fields that are present, producing noise that erodes trust in pre-flight findings.
