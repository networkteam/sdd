---
type: signal
layer: process
kind: gap
refs:
    - 20260415-114317-s-prc-hpa
    - 20260420-150405-d-cpt-ydf
participants:
    - Christopher Hlubek
    - Claude
confidence: high
summary: Pre-flight incorrectly reports 'missing-explicit-kind' as a finding even when entries carry the explicit kind field — a confirmed false positive from unreliable LLM-based metadata detection. The signal reinforces the structural limitation identified in (s-prc-hpa), which proposes moving field validation from LLM to deterministic CLI checks, and exemplifies the validation challenges addressed in the type-system contract (d-cpt-ydf).
summary_hash: 2f6d7e1f64e57f8d66b861bb6849d590
---

Pre-flight reports `missing-explicit-kind` as a medium finding even when the entry carries an explicit `kind` field — confirmed false positive in two sessions (s-stg-ljq and another). The LLM-based field-presence check (s-prc-hpa) cannot reliably detect metadata fields that are present, producing noise that erodes trust in pre-flight findings.
