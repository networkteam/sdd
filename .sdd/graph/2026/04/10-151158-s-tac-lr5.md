---
type: signal
layer: tactical
kind: gap
refs:
    - 20260410-150919-d-prc-4du
participants:
    - Christopher
    - Claude
confidence: high
summary: The `sdd new --attach` command accepts file paths but lacks filename mapping syntax, causing piped content and temporary files to receive meaningless names like 'stdin' or 'tmp.XXX'. This prevents interactive workflows and skills from producing descriptive attachment names without manual post-hoc cleanup. The signal references the plan-first implementation decision (d-prc-4du).
summary_hash: 1db599f4cdb9bd0adb82b9a5e8dadf18
---

sdd new --attach takes file paths but doesn't support renaming the attachment in the target directory. Piping content via /dev/stdin or using temp files results in meaningless filenames (stdin, tmp.XXX). The CLI needs a filename mapping syntax (e.g. --attach source:target or --attach /dev/stdin:plan-requirements.md) so that skill usage and piped content produce descriptive attachment names without manual git mv cleanup.
