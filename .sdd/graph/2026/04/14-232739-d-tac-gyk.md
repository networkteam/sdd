---
type: decision
layer: tactical
kind: plan
refs:
    - 20260414-162450-s-ops-5aw
    - 20260414-165211-s-ops-nms
    - 20260413-160358-d-stg-574
closes:
    - 20260414-162450-s-ops-5aw
    - 20260414-165211-s-ops-nms
participants:
    - Christopher
    - Claude
confidence: high
preflight: skipped
summary: Commits to surfacing LLM call metadata through an agent-neutral abstraction in llm.Runner, returning structured results with response text and platform-agnostic metadata alongside debug logging via slog. Directly resolves the metadata visibility gaps (s-ops-5aw, s-ops-nms) by isolating Claude-specific JSON parsing into a subpackage while keeping the core llm package provider-agnostic, aligning with the platform neutrality directive (d-stg-574). Implements structured logging with configurable verbosity tiers and internal helpers for Preflight and Summarize operations to enable cost tracking and performance diagnostics.
summary_hash: 61f6fdd63ef30eb0fb07a88e46242c96
---

Surface LLM call metadata through an agent-neutral abstraction in llm.Runner, per the agent-platform neutrality directive (d-stg-574) and explicit dialogue where the user identified that metadata must not be Claude-specific so other runners can populate the same struct. Change Runner.Run to return a result struct with response text and agent-neutral LLMMetadata. Move claudeRunner from cmd/sdd/main.go into llm/claude subpackage — claude-specific JSON parsing stays isolated, the llm package has no provider imports. The claude runner uses --output-format json, parses the full response, and maps it to the neutral metadata struct. Logging via log/slog with github.com/networkteam/slogutils for context-carried loggers — CLI command handlers set "command" attr, llm package emits one debug log call per LLM invocation under slog.Group("llm") with op, duration, and a sub-group per model containing tokens in/out and cost (when available). Internal helper called by Preflight and Summarize after Run returns. Verbosity tiers: default warn (no LLM output), -v info, -vv debug (LLM call details visible). See [plan](./14-232739-d-tac-gyk/plan.md).
