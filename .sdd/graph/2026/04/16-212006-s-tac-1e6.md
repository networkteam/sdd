---
type: signal
layer: tactical
kind: done
refs:
    - 20260416-190732-s-prc-omw
closes:
    - 20260416-203442-d-tac-955
    - 20260416-203327-s-prc-v69
participants:
    - Christopher
    - Claude
---

Closes d-tac-955 and s-prc-v69. Partially addresses s-prc-omw — the CSV-trim and catch-up-rendering fixes are in; the whoami-config (fix 3) and pre-flight-participant-validation (fix 4) fixes remain open on that signal.

Per commit 452e60f:

**AC 1–3** (format emission and grouping): `sdd status` and `sdd list` emit `<id> <layer> <kind>? <type> [confidence: <conf>]? (<participants>) <summary>`. Kind appears on decisions only (today); confidence on decisions + signals; participants always. Grouping preserved (Contracts / Plans / Active Decisions / Open Signals / Recent Actions). Verified against the live graph.

**AC 4** (CSV trim): `splitCSV` in `framework/cmd/sdd/main.go` now trims each element and drops empties. Regression test `TestSplitCSV_TrimsWhitespaceAndDropsEmpty` in `framework/cmd/sdd/main_test.go` covers `"Christopher, Claude"` → `["Christopher", "Claude"]` (the exact regression) plus extra-whitespace, stray-comma, and empty-input cases. Satisfies the d-prc-8vh bug-fix-requires-test contract.

**AC 5** (backfill): 162 entries normalized. Before: 132 × "Christopher Hlubek", 30 × " Claude" (leading-space CSV artifact), 112 × "Christopher", 200 × "Claude". After: 244 × "Christopher", 230 × "Claude", zero variants. Mechanical fix permitted under d-cpt-e1i — reader understanding unchanged. Repo-local cleanup, not a framework-wide first-name convention.

**AC 6–7** (sub-skill consumption + canonical forwarding): `/sdd-catchup/SKILL.md` now documents the enriched status line format, instructs the sub-skill to read kind/participants from status rather than per-entry `sdd show`, and requires forwarding kind, direct participants, and upstream voices as distinct structured lines per item plus a top-level `Active recently:` set.

**AC 8** (thread-level voices): sub-skill instructions specify full upstream ref chain, deduped, with direct set excluded for the voices set. Outer agent decides surfacing per playbook rules.

**AC 9** (Catch-up Playbook): `/sdd/SKILL.md` adds a "Participants — narrative, not metadata" section: active-recently header (optional, once), thread-level mention only when differentiating, per-item inline only in the surprise case. Never as per-item dashboard rows.

**AC 10** (lint): `sdd lint` reports "No issues found." Full Go test suite (`go test ./...`) passes.

Deferred scope tracked as open aspects of s-prc-omw: whoami config and pre-flight participant validation.
