---
type: signal
layer: operational
kind: done
refs:
    - 20260408-100259-s-ops-h63
closes:
    - 20260408-100259-s-ops-h63
participants:
    - Christopher
    - Claude
summary: Fixed explore sub-skill deduplication by changing sdd-explore to batch all related entry IDs into a single sdd show call instead of issuing separate calls per entry. This resolves the duplicate upstream context that appeared in the earlier explore trial (s-ops-h63), where repeated chains indicated further deduplication was needed.
summary_hash: bae7b94e58712a26840c0a1ca5ed17f4
---

Fixed explore sub-skill deduplication: changed sdd-explore to batch all related entry IDs into a single sdd show call instead of separate calls per entry. The CLI already deduplicates shared upstream chains across multiple IDs (prints 'shown above' for repeats).
