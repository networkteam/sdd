---
type: signal
layer: operational
kind: done
refs:
    - 20260408-100259-s-ops-h63
closes:
    - 20260408-100259-s-ops-h63
participants:
    - Christopher
    - Claude
summary: Fixed explore sub-skill deduplication by batching all related entry IDs into a single sdd show call instead of separate calls per entry, leveraging the CLI's built-in deduplication logic that prints 'shown above' for repeated upstream chains. This resolves the redundant context duplication that appeared when explore mode first surfaced semantic connections across multiple entries (20260408-100259-s-ops-h63).
summary_hash: 51d5002c4bad8f5f4b583062e9bbc5c5
---

Fixed explore sub-skill deduplication: changed sdd-explore to batch all related entry IDs into a single sdd show call instead of separate calls per entry. The CLI already deduplicates shared upstream chains across multiple IDs (prints 'shown above' for repeats).
