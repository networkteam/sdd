---
type: signal
layer: operational
kind: gap
participants:
    - Christopher
    - Claude
confidence: high
summary: CLI duration flags currently reject Go-style duration strings (e.g. 180s, 2m30s) and only accept raw integers, causing parse failures on formatted input. The --preflight-timeout flag and all other duration parameters should be standardized to use time.ParseDuration for consistent, human-friendly duration input across the CLI.
summary_hash: 97bc6355e852e3529e99300d07c757d2
---

CLI duration flags like --preflight-timeout accept raw integers (seconds) instead of Go-style duration strings (e.g. 180s, 2m30s). The flag errored on '180s' with a strconv.ParseInt parse failure. Duration flags should use time.ParseDuration for human-friendly input across all duration parameters, not just preflight-timeout.
