---
type: signal
layer: operational
kind: gap
participants:
    - Christopher
    - Claude
confidence: high
summary: The signal observes that CLI duration flags like --preflight-timeout accept only raw integer seconds and reject Go-style duration strings, causing strconv.ParseInt failures on inputs like '180s'. It identifies that all duration parameters across the CLI should adopt time.ParseDuration for consistent, human-friendly input support.
summary_hash: 0c9a6c1ff512f9132b6097f18fadb68d
---

CLI duration flags like --preflight-timeout accept raw integers (seconds) instead of Go-style duration strings (e.g. 180s, 2m30s). The flag errored on '180s' with a strconv.ParseInt parse failure. Duration flags should use time.ParseDuration for human-friendly input across all duration parameters, not just preflight-timeout.
