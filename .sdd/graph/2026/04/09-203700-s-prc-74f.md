---
type: signal
layer: process
kind: gap
refs:
    - 20260409-151757-s-tac-qa0
participants:
    - Christopher
    - Claude
summary: The SDD skill unnecessarily calls git config user.name to infer the participant when Claude Code's session context already provides this information via the gitStatus header. This identifies a gap in the participant inference playbook (s-tac-qa0), which should be updated to recommend reading from session context instead of invoking git config.
summary_hash: 5811dd01ccb669523c4dc52473f2c070
---

SDD skill calls git config user.name to infer the participant, but Claude Code's session context already provides the git user in the gitStatus header. The playbook guidance from qa0 should say 'read from session context' instead of 'call git config'.
