---
type: signal
layer: process
kind: gap
refs:
    - 20260409-151757-s-tac-qa0
participants:
    - Christopher
    - Claude
summary: The SDD skill's participant inference logic calls git config user.name when Claude Code's session context already provides the git user via gitStatus header, indicating redundant implementation. This gap (s-prc-74f) identifies that the playbook guidance documented in qa0 should be updated to direct reading from session context instead of calling git config, eliminating the unnecessary system call.
summary_hash: af87bb383f067a5f0a341962e9a5cc48
---

SDD skill calls git config user.name to infer the participant, but Claude Code's session context already provides the git user in the gitStatus header. The playbook guidance from qa0 should say 'read from session context' instead of 'call git config'.
