---
type: signal
layer: process
kind: done
closes:
    - 20260414-002243-d-prc-22i
participants:
    - Christopher
    - Claude
confidence: high
summary: Completed the migration of action-durability enforcement from contract d-prc-zch into pre-flight's closing_action.tmpl, implementing the three-path durability check (prior commit, --attach flag, or upstream attachments) and updating SKILL.md documentation accordingly. Closes the directive (d-prc-22i) and effectively supersedes the retired contract through the embedded validator guidance. All validation tests pass and code changes are committed at 104f225.
summary_hash: 0d361fc57b69fd18fe3d60e6c58924e0
---

Moved action-durability enforcement from the d-prc-zch contract into pre-flight's `closing_action.tmpl`, per the plan in d-prc-22i's design.md attachment.

Plan items:

1. **closing_action.tmpl** — added 'Durability' as Check 5 with the three-paths wording from the design: presence check accepting (a) a prior commit referenced by the action, (b) attachments on this entry via `--attach`, or (c) attachments on a referenced upstream entry already in the graph. If none apply, flag as a durability gap.
2. **SKILL.md line 60** — replaced the 'work must be committed to Git first' framing with the prior-commit-OR-`--attach` framing from the design.
3. **Supersede d-prc-zch** — already landed via d-prc-22i's `--supersedes` flag at decision time. No separate step needed here.

Validation: `go vet ./...` clean, `go test ./sdd/...` passes (templates compile; existing pre-flight tests still green). Binary rebuilt at `framework/bin/sdd` (not committed per framework convention — users rebuild locally). Code changes landed in commit 104f225.
