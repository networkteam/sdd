---
type: signal
layer: operational
kind: gap
refs:
    - 20260414-152107-s-ops-d1c
    - 20260414-152337-d-tac-h93
participants:
    - Christopher
    - Claude
confidence: high
summary: The llm.Runner discards per-call cost and token metrics from the claude CLI despite having access to total_cost_usd, token counts, and cache breakdowns, creating a gap in visibility for expensive entries and prompt optimization. Extending llm.Runner to surface this structured metadata would enable cost tracking and template tuning, directly supporting the token-bloat reduction strategy (d-tac-h93) and addressing the excessive token consumption problem (s-ops-d1c) by providing actionable data for identifying and optimizing high-cost entries during summarize and pre-flight operations.
summary_hash: c0b35b55a2bae13193fd6e3818b1e319
---

The claude CLI exposes per-call cost and token metrics via --output-format json, but the llm.Runner currently discards this data, losing visibility into expensive entries and prompt optimization opportunities. The JSON response includes total_cost_usd, per-model input/output token counts, and cache hit/creation breakdowns. Surfacing these per-call stats would enable spotting expensive entries (large descriptions, many refs), tracking migration cost, and tuning prompt templates for token efficiency. Natural next step: extend llm.Runner to return structured metadata alongside the response, log per-entry stats during sdd summarize and sdd new pre-flight.
