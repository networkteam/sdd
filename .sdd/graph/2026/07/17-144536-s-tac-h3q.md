---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260716-153108-s-tac-spx
      kind: builds-on
      desc: Completes the read-side target invariant exposed after the local mutation-target delivery.
closes:
    - 20260717-112556-s-tac-zle
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - reliability/testing
    - reliability/discipline
    - collaboration/concurrent-work
    - portability/runtime
summary: Commits `637d61d` and `334645a` make all artifact-dependent reads — summary injection, done-entry predicates, and resume/re-serve — resolve against the same explicit branch target used at write time, with the application selecting `captureBranch` or `workBranch`, normalizing before cache access, and acquiring through the same snapshot path writes use. This completes the read-side invariant left open after the mutation-target delivery (20260716-153108-s-tac-spx) and retires the branch-target read-consistency gap (20260717-112556-s-tac-zle), with full tracked regression, focused seam tests, independent review, and clean toolchain verification confirming every named surface now resolves correctly.
---

Commits `637d61d` and `334645a` make workflow reads follow the explicit branch authority of the artifact they consume and harden that seam against future extension, completing the branch-target read-consistency gap in 20260717-112556-s-tac-zle. The engine accepts an optional store-aware graph provider for function contexts while procedure loading remains on the session graph; the application owns branch semantics, selecting `captureBranch` or `workBranch`, resolving the canonical target before cache access, acquiring it briefly through the same snapshot path writes use, preserving dependency expansion, and making `generatedSummary` consume the resolved context. This builds on the mutation-target delivery in 20260716-153108-s-tac-spx without introducing a second authority mechanism or deriving authority from cwd.

The implementation covers capture's post-write summary injection, faithful or replacement summary paths through the same context, resume/re-serve at summary verification, and implementation's `doneEntry` predicate before landing. The full tracked regression uses distinct base and work stores: the WIP marker remains only on base; capture and its closing done exist only on work; generated-summary verification succeeds before merge and survives re-serve; reporting the work-only done advances implementation to the landing junction. Focused provider and predicate tests cover target selection, acquire/release discipline, cache reuse, and the shared engine seam.

An independent review judged the branch clean, well-scoped, landable, and free of blockers. Its two minor findings are resolved by `334645a`: target normalization now precedes both cache lookup and storage, and the application names the branch-bearing store fields as the authoritative read-target registry so a future procedure cannot add another field without encountering the extension warning. The read seam also documents its durable cost signal: local acquisition is a checkout lookup, while remote acquisition may clone once per cache miss and should use the existing read-cache acceleration without weakening explicit authority.

Verification after the follow-up is clean: `go test ./...`, `go vet ./...`, Devbox `golangci-lint` with zero issues, and `devbox run build`. The host lint binary was incompatible with the repository's Go 1.26 target, so the repository-owned Devbox toolchain supplied the authoritative lint result. This retires 20260717-112556-s-tac-zle because every artifact-dependent read, guard, and resume surface named by the gap now resolves against the work target through the tested tracked flow.
