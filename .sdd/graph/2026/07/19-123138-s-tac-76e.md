---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260703-200000-d-prc-evl
      kind: grounded-in
      desc: the evaluation procedure this outer-validation run followed
    - id: 20260719-015148-s-tac-khq
      kind: builds-on
      desc: the completed five-slice session-model shipment evaluated in genuine cross-client use
participants:
    - Christopher
confidence: high
topics:
    - evaluation/outer/user-feedback
    - portability/mcp
    - agent/ux
summary: 'A live outer evaluation of the completed session model finds its concurrency and recovery mechanics working, but identifies a concrete handoff failure: when a Codex agent attached mid-procedure, the MCP response omitted the persisted `anchor`, `plan`, and `widenReport` values, causing the new agent to re-search the anchor, treat both lenses as outstanding, and run inner verification tooling instead of the intended assisted outer validation. This mixed judgment is grounded in the evaluate procedure (20260703-200000-d-prc-evl) and directly evaluates the five-slice session-model shipment in genuine cross-client use (20260719-015148-s-tac-khq), establishing that durable intent-bearing state must not disappear silently at handover.'
---

A live outer evaluation of the completed session model finds its exercised concurrency and recovery mechanics working, while one assisted Codex handoff omitted the persisted evaluate-state values `anchor`, `plan`, and `widenReport` from the response shown to the new agent. Before attachment, the raw session log held `anchor: 20260719-015148-s-tac-khq`; a `plan` beginning “Outer lens only, as requested” and specifying an assisted second-client exercise; and the searches and prior evidence in `widenReport`. The 22.9 KB MCP response instead gave the evaluate instance only its procedure, step, goal, generic carry-out instructions, status, session handle, and `report_schema`. That schema named `anchor`, `plan`, `widenReport`, both lens evidence and judgment fields, and `selectedFindings`, but it carried none of their collected values. The generic instructions described both lenses and rendered diagnostics for both missing judgments; they did not render the anchor, the outer-only selection and assisted posture encoded in `plan`, or the prior grounding in `widenReport`. The new agent consequently searched for and showed the anchor again, described verification and validation as both outstanding, and ran `go vet`, `golangci-lint`, and `go test`, while Christopher observed that the intended assisted outer evaluation had shifted into autonomous inner verification. In the same run, discovery, structural consent, attachment, explicit takeover, typed stale-writer rejection, compact reorientation, and one-shot full replay held. This is a mixed outer judgment scoped to one concrete Codex handoff, not evidence that every procedure or cross-client handoff loses intent. It leads to the hypothesis that other procedures may show the same behavior when a current step depends on state collected earlier but its served instructions do not render those values. The evaluation does not settle whether handover should serve full current state alone or also a filtered trajectory of significant events and completed ancestors. It establishes the narrower boundary that the event log is a procedure trajectory rather than the full dialogue, and that durable intent-bearing state or trajectory must not disappear silently at handover.
