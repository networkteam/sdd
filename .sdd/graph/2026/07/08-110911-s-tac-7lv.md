---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260708-090449-s-tac-vyq
      kind: grounded-in
      desc: the review round whose findings this remediation pays off — its verdict held the merge on exactly this work
    - id: 20260708-010505-s-tac-94a
      kind: builds-on
      desc: the cross-repo implementation this remediation reshapes behavior-identically; its merge hold and outer evaluation remain
closes:
    - 20260708-092224-d-tac-fyd
participants:
    - Christopher
confidence: high
topics:
    - implementation/cli
    - cli/architecture
summary: 'All four architecture remediations committed in the directive are delivered, verified, and pushed on branch worktree-cross-repo-refs across five commits: cross-repo ID collectors unified into model.CrossRepoIDs, an internal/git adapter absorbing all raw git execution, internal/repos split into a pure Registry and side-effectful Manager behind explicit Locations, and index-lint computation moved into the finder — with the full toolchain clean and live two-repo fixture confirmed. This closes the directive (20260708-092224-d-tac-fyd) by satisfying every commitment it named, paying off the layering debt the review round identified (20260708-090449-s-tac-vyq). The merge hold and Christopher''s outer evaluation of the cross-repo implementation this reshapes (20260708-010505-s-tac-94a) remain open.'
---

The PR #2 architecture remediation is complete: all four commitments of the directive this done retires (20260708-092224-d-tac-fyd) are implemented, verified, and pushed on branch worktree-cross-repo-refs, with the merge still held for Christopher's re-review and outer evaluation. Five commits carry the work: 6458e34 unifies the duplicated cross-repo ID collectors as model.CrossRepoIDs beside SplitCrossRepoID; 090b010 introduces internal/git, the single adapter package for all git execution — git.CLI implements the consumer-defined interfaces (handlers.Committer/Brancher/Mover/Puller, finders.GitSyncer), git.RemovalCommitter carries the staged-deletion commit variant, the raw exec code in cmd/sdd/main.go and sync.go migrates in so cmd/sdd keeps wiring only, and the pathspec-scoping regression tests move with the code; b3e09bb splits internal/repos behind explicit Locations — repos.DefaultLocations() keeps the XDG convention in-package and is called only at composition roots, the pure Registry (connections, cache layout, lazy config reads so a long-lived MCP server sees new connections without restart) is injected into finders.Options, SearchFinderOptions, and mcpserver.Options, and the side-effectful Manager (EnsureCloned, CooldownPull, ForcePull, config writes) wraps the Registry plus the narrow repos.Git interface implemented by internal/git and is injected into handlers.Options with fail-loud guards — finder purity is now type-enforced, and every XDG t.Setenv left the finders tests (only DefaultLocations' own test touches the environment); 4281cba moves the index-lint computation into the finder as Finder.IndexLint over a new query.IndexLintQuery, the CLI keeping only flag-to-config resolution with the degrade-silently posture preserved; b5affdd lists internal/git and internal/repos in the AGENTS.md structure tree (pulled into this run in dialogue). Verification: go vet, the full test suite, gofmt, and golangci-lint clean after every slice; the rebuilt binary re-verified live against a two-repo fixture — repo add via local path exercising the declared-identity fallback, repo list/sync/remove, capture with a resolvable cross-repo ref driving freshen and fetch-on-miss through the Manager, cross-repo show resolving the cached remote as primary, merged search --repo with a repo-tagged remote hit, per-repo view --all-repos section, the lint degrade path, and an unresolvable cross-repo ref blocked when pre-flight runs (--skip-preflight still writes with the skipped annotation — the documented override, semantics unchanged). The directive retires because every commitment it named is delivered on the branch; behavior is intentionally identical throughout — this was layering remediation paying off the review round's findings (20260708-090449-s-tac-vyq), not feature work. Left open deliberately: the merge and Christopher's outer evaluation of the cross-repo feature itself (the hold recorded on the implementation done this remediation reshapes, 20260708-010505-s-tac-94a), and the legacy read-side query leak stays out of scope per the directive.
