---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260710-135610-d-cpt-z6h
      kind: grounded-in
      desc: the anchor-resolution decision this landing rests on for the bare-ID AC's move-anchor clause — satisfied by resolve-via-show, not an engine change
    - id: 20260710-144207-s-tac-v1y
      kind: surfaces
      desc: config get/set surface gap this work surfaced while documenting the config model
    - id: 20260710-144212-s-tac-z7j
      kind: surfaces
      desc: config-messaging-lag gap this work surfaced while documenting the config model
closes:
    - 20260709-170009-d-tac-rsf
participants:
    - Christopher
confidence: high
topics:
    - implementation/cli
    - implementation/skill-text
    - type-system/refs
    - cli/ux
    - portability/mcp
    - search/index
summary: 'Both batches of the cross-repo release plan are implemented, verified, and committed on branch worktree-cross-repo-release, closing every acceptance criterion and the plan itself (20260709-170009-d-tac-rsf). The bare-ID move-anchor criterion was satisfied via resolve-via-show rather than an engine change, grounded in the anchor-resolution directive (20260710-135610-d-cpt-z6h). Documentation work for Batch 2 surfaced two follow-up gaps deferred for a later sweep: the `sdd config` get/set surface shortfall (20260710-144207-s-tac-v1y) and stale config-messaging references (20260710-144212-s-tac-z7j).'
---

Cross-repo references reached release readiness: both batches of the plan (20260709-170009-d-tac-rsf) are implemented, verified, and committed on branch worktree-cross-repo-release, awaiting PR review before merge. This closes the plan — every acceptance criterion is met, one by design rather than code (below).

Batch 1 — code (commit 5d30e8c). `sdd repo remove` is now project-scoped and reference-safety-guarded (removes the .sdd/config.yaml dependency, auto-commits, refuses when a local entry still references the target, --force names the stranded refs); `sdd repo add` auto-commits its dependency-declaration write; the repo-add fast path skips the coordinator view when no clone runs (no escape-sequence leak); `sdd lint` flags cross-repo refs into undeclared dependencies; bare-ID resolution (short and unprefixed-full) resolves across the flat union of the local graph and its declared dependencies at `sdd show`, the show MCP tool, and written refs — which expand to full prefixed form — with framework-embedded entries deduped to the local instance and genuine collisions erroring; MCP cross-repo vector search resolves the same global-first embedder as the CLI so both share one (repo-id, fingerprint) store; `sdd index --repo/--all-repos --force` rebuilds connected member indexes; Ctrl-C during an embed exits cleanly with the SIGINT code rather than a raw context-canceled; and text-only `sdd search --repo` gives honest fetch feedback instead of a mislabeled indexing bar. Unit tests cover repo remove, the lint check, --force member rebuild, and the union resolver; build, vet, the full test suite, and lint are clean, and the CLI paths were runtime-verified end to end in a throwaway repo.

Batch 2 — docs (commits 5f9e4bb and acfebd6; installed skill renders refreshed in 455d767). The README gained a Connected repos onboarding section and a full config-overlay explanation — the user-global / committed-project / machine-local layers, their precedence, the by-whose-fact-it-is placement split, and the `sdd config` surface — closing the prior omission of the user-global layer; the cross-repo search note now recommends configuring the embedder at the global layer so all connected indexes share one vector space. The agent-neutral skill templates gained cross-repo command, ID-resolution, and authoring guidance, rendered for both Claude and Codex; and the host-neutral base-procedure specs (capture, dialogue orientation) reflect cross-repo resolve-or-block, the declared-dependency precondition, and bare-ID/anchor resolution. Stale index-location docs (an in-tree .sdd/index, a per-participant index) were corrected to the machine-global store as adjacent accuracy repairs.

One acceptance criterion was resolved by design rather than code: the bare-ID AC's move-anchor clause. Rather than loosen the engine's strict anchor field type, the anchor surface resolves bare IDs through the free show read — decided in 20260710-135610-d-cpt-z6h and reflected in the served guidance. Ctrl-C exits with the SIGINT code (a deliberate call over exit 0). Deliberately out of scope and unchanged: cutting the release tag, which follows via the release flow once this merges. The individual findings the plan folded in — config-commit consistency, the lint gap, embedder divergence, the --force gap, the footer mislabel, and the coordinator lifecycle edges — are resolved by this landing; their formal closure can follow the merge. Documenting the config model for Batch 2 also surfaced two follow-up gaps, captured for a later sweep rather than fixed here: the `sdd config` get/set surface falls short of the three-layer overlay (20260710-144207-s-tac-v1y), and user-facing config messaging still points only at `.sdd/config.local.yaml` (20260710-144212-s-tac-z7j).
