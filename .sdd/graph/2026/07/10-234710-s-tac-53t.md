---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260703-200000-d-prc-evl
      kind: grounded-in
      desc: the evaluation procedure this evidence-backed outer-validation run followed
    - id: 20260709-163921-s-tac-68a
      kind: builds-on
      desc: the shipped and inner-verified reorientation work whose outstanding live Codex validation this run completes
participants:
    - Christopher
confidence: high
topics:
    - evaluation/outer/user-feedback
    - portability/mcp
    - agent/ux
summary: 'Two genuine Codex compactions validate the shipped post-compaction engine reorientation in live use: in both runs, `resume_session` restored the live session and open dialogue and the agent continued work correctly, including recovery of dialogue continuity rather than merely server state. This completes the outer-validation follow-up left outstanding by the implementation done (20260709-163921-s-tac-68a), following the evidence-backed evaluation procedure (20260703-200000-d-prc-evl). Skill selection was imperfect in one of two runs but is recorded as a qualified observation rather than a separate finding, as removing the legacy `$sdd` skill may resolve the ambiguity.'
---

Two genuine Codex compactions successfully validate the shipped post-compaction engine reorientation in live use: in both runs, `resume_session` restored the live session and open dialogue and the agent continued the work correctly. This completes the outer-validation follow-up explicitly left outstanding by the implementation done (20260709-163921-s-tac-68a), following the evidence-backed evaluation procedure (20260703-200000-d-prc-evl); inner verification was not repeated because the implementation done already records clean vet, full tests, and MCP-surface regression coverage.

The first run exercised the strongest path. After compaction, Codex selected `$sdd-engine` itself, called `resume_session`, re-oriented from the returned live position, found the open dialogue, and continued so smoothly that Christopher did not initially notice the compaction. In the second run, Codex first selected the legacy `$sdd` skill; after Christopher corrected it to `$sdd-engine`, the agent again resumed the existing session, recovered the open work, and continued successfully.

The outer judgment is a successful validation: the durable recovery guidance and handle-free resume path work under the real condition they were designed for, including recovery of dialogue continuity rather than merely server state. Skill selection was imperfect in one of two runs, but it is recorded here as an observed qualification rather than a separate gap because removing the legacy `$sdd` skill may remove the ambiguity. No separate finding is surfaced from this evaluation.
