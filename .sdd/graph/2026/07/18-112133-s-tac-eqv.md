---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260703-224930-s-tac-4hh
      kind: builds-on
      desc: the earlier oversized-serve gap whose compose portion was deliberately left unchanged; this run supplies fresh live evidence
    - id: 20260704-000621-d-tac-bfc
      kind: grounded-in
      desc: the lifecycle contract that makes the door fresh and all other loop tools require an already-bound session
    - id: 20260705-010751-d-tac-dbk
      kind: grounded-in
      desc: the session-operations decision that kept the door as re-entry, connection-keyed served memory, and rich compose lanes
    - id: 20260704-201618-s-tac-w3v
      kind: builds-on
      desc: the earlier reconnect observation of rejected calls and duplicated orientation cost
    - id: 20260709-160621-d-tac-zm2
      kind: grounded-in
      desc: the reorientation plan that solved context loss only for a connection that remains bound
    - id: 20260712-194427-d-tac-wjq
      kind: grounded-in
      desc: the exclusive-holder and chooser-TTL contract exercised by the trace
    - id: 20260716-122358-s-tac-2sp
      kind: related
      desc: adjacent open evidence that agent-dialogue continuity can diverge from MCP connection ownership
participants:
    - Christopher
confidence: high
topics:
    - portability/mcp
    - agent/ux
    - reliability/discipline
summary: 'This signal records three concrete mismatches observed in a live Codex engine session: catch-up compose payloads reach 55.8 KB per reorientation because `resume_session` clears served-once memory before replay; `resume_session` and `list_sessions` silently require an already-bound session despite public descriptions implying otherwise; and an advance at the catch-up user junction applied the wrong gate lease. It builds on the open compose-serve oversize gap (20260703-224930-s-tac-4hh) and earlier rejected-call reconnect evidence (20260704-201618-s-tac-w3v), and confirms that the reorientation plan (20260709-160621-d-tac-zm2) and holder contract (20260712-194427-d-tac-wjq) do not cover the unbound-session failure mode observed here. The lifecycle contract (20260704-000621-d-tac-bfc) and session-operations decision (20260705-010751-d-tac-dbk) are the grounding commitments the trace exercises, and the branching continuity gap (20260716-122358-s-tac-2sp) sits as adjacent open evidence sharing the holder-divergence pattern.'
---

In a live Codex engine session, catch-up was re-served twice without advancing, and after the original holder was released both named resume and session listing were rejected until the door created a different durable session.

The observable combines three concrete mismatches. First, the reconstructed catch-up compose instructions are 43,086 bytes; `resume_session` clears served-once memory before replay, so the current catch-up plus framing alone exceed 55.8 KB on each reorientation. Second, the public description says a parked handle can be resumed, and the unbound error says `resume_session` picks one up, while the implementation rejects the call before inspecting that handle unless another session is already bound; `list_sessions` has the same hidden prerequisite. Third, the advance that landed on the catch-up user junction applied the pre-transition gate lease of two minutes rather than the user lease.

The trace does not establish why the agent invoked resume twice, whether its model-visible response was truncated, why the holder was released, whether TTL contributed to the unbound state, or whether these symptoms require one change. The attached observation preserves the transcript timeline, session-log evidence, payload reconstruction, implementation paths, lease evidence, and these uncertainty boundaries.
