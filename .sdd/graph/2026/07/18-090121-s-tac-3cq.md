---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260703-200000-d-prc-evl
      kind: grounded-in
      desc: the evaluate procedure this outer-validation run followed
    - id: 20260717-145856-s-tac-43y
      kind: builds-on
      desc: the shipped view usability and base-fact work evaluated in real use
    - id: 20260707-142811-s-prc-3kh
      kind: refines
      desc: the before-state discovery benchmark this run improves but does not close
    - id: 20260716-155256-s-prc-686
      kind: grounded-in
      desc: the standing requirement to validate agent-facing surfaces through a genuine unshaped agent session
participants:
    - Christopher
confidence: high
topics:
    - evaluation/outer/user-feedback
    - cli/view
    - engine/base-facts
summary: 'A real Codex engine session finds that the shipped view-layout grammar fact works once retrieved but fails outer validation because first-call discovery remains unsolved — an agent without prior knowledge of view syntax cannot bootstrap the call without deliberately searching for it. This run is the genuine unshaped-agent pass required by the real-agent validation principle (s-prc-686), conducted under the outer lens of the evaluate procedure (d-prc-evl), and it refines rather than closes the before-state discovery benchmark (s-prc-3kh). A second finding also emerges from evaluating the shipped work (s-tac-43y): the graph fact''s dense terminal-style formatting and absent examples make it less usable than the implementation-backed `sdd view --help` output.'
---

A real Codex engine session confirms that the shipped view-layout fact is effective after retrieval but does not solve first-call discovery, so the outer validation of the view usability work does not pass. This run covers only the outer user-feedback lens under the evaluate procedure (d-prc-evl); inner implementation verification is deliberately out of scope. The agent began with ordinary searches for Jonathan Philipp, recognized that an authorship view was needed, then found the grammar fact only by deliberately searching the graph for view syntax. Once retrieved, it produced `participant("Jonathan Philipp"):rank(by(date)):n(50):brief:as-list` correctly on the first attempt, without source inspection, graph-file grep, or layout trial-and-error. That is an improvement over the before-state benchmark (s-prc-3kh), but it sharpens rather than closes the gap: the first-view breadcrumb cannot bootstrap a call whose syntax the agent does not yet know. This is a genuine unshaped-agent run of the kind required by the real-agent validation principle (s-prc-686), and it builds on the shipped work (s-tac-43y). Christopher confirmed that discovery remains unsolved and identified a second usability finding: the graph fact's dense terminal-style formatting and missing examples are less usable than the implementation-backed `sdd view --help` output.
