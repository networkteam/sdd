---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260702-220449-d-tac-ry0
      kind: addresses
      desc: supplies slice 4 and live validation of the fifth acceptance criterion, without closing the plan
    - id: 20260703-002223-s-tac-kd2
      kind: builds-on
      desc: the shell now executes its first procedure
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - portability/mcp
    - skill/architecture
summary: 'Slice 4 of the v1 workflow engine plan shipped and was validated in live use: the base capture procedure is embedded (commit 4ee3bb5) with params for anchored, supersede, and close captures, chooser-collected evidence fields, and per-procedure table tests driving the shipped entry itself, alongside project-scope MCP setup and an embedded-entry integration test. This addresses the fifth acceptance criterion of the engine plan (20260702-220449-d-tac-ry0) for the plain-capture spine, with live dogfooding confirming capture-core parity and gaps clustering in session lifecycle rather than capture. It builds on the MCP shell (20260703-002223-s-tac-kd2), which now executes its first procedure end-to-end.'
summary_hash: 06b1ff7e88d71b44c09424b88461523d
---

Slice 4 of the v1 workflow engine plan is complete and validated in use: the base capture procedure ships embedded (commit 4ee3bb5) — authored against the surface-spec schema with params for anchored, supersede, and close captures, chooser-collected evidence fields in state, and instruction units carrying the authoritative capture discipline — with the per-procedure table tests driving the shipped entry itself rather than a parallel fixture, the MCP test fixture reshaped as a project override exercising the project-head-wins fork rule, and an embedded-entry integration test over MCP. Setup ships committed alongside: project-scope `.mcp.json` registration and the `mcp__sdd__*` permission allowance (commits ee49fb9, 9626dc6). Dogfooding began as the plan intended: a first live engine session drove the full spine — batch draft over real widen searches, adjust loop with verbatim relays, pre-flight write, genuine summary-fidelity judgment — and an outer evaluation of that session's transcript judged the capture core at parity in use, with the gaps clustered in session lifecycle rather than capture. The run's findings were captured as their own entries (a setup directive, two surface-widening augmenting directives — one already implemented and closed — four gaps and one question), so the fifth acceptance criterion is validated live for the plain-capture spine while its params, staged-attachment, and override variants rest on the integration tests; live validation of those variants accumulates as dogfooding continues.
