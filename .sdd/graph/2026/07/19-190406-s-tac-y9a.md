---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260719-124405-d-cpt-0tm
      kind: addresses
      desc: realizes the invariant's honest-surfacing half — resume-only projection with labeled compression; the projection-boundary design stays open under it
    - id: 20260719-135225-s-cpt-m8n
      kind: surfaces
      desc: implementing the slice exposed the per-field serve-threading pattern this question records for the future design
closes:
    - 20260719-123350-s-cpt-hkx
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - portability/mcp
    - agent/ux
summary: Resume-type serves now project each open instance's collected procedure state into `resume_session` responses, with per-value byte caps, labeled truncation and omission notices, and trust-machinery fields excluded — shipped in commit 7a243c4. This closes the state–serve asymmetry where durable values were absent from resumed MCP responses, causing agents to re-derive already-settled state (20260719-123350-s-cpt-hkx), and partially realizes the honest-surfacing half of the handover-fidelity invariant (20260719-124405-d-cpt-0tm), whose open projection-boundary questions carry forward through the serve-composition genericity problem this implementation exposed (20260719-135225-s-cpt-m8n).
---

Resume-type serves now project each open instance's collected state, shipped in commit 7a243c4 on the session-model worktree branch: the concrete state–serve asymmetry is fixed, so this closes the gap that recorded it (20260719-123350-s-cpt-hkx). A `collected` map rides every open instance in `resume_session` responses — param- and state-provenance values from the replayed store, with trust-machinery fields (playback confirmation, pre-flight override) excluded by provenance and by name. Size discipline is applied at the presentation boundary per the fidelity invariant this realizes in its honest-surfacing half (20260719-124405-d-cpt-0tm): each value is capped at 2,000 bytes with a labeled truncation notice, and each instance's projection is budgeted at 8,000 bytes with deterministic ordering and labeled omission notices — compressed or omitted material is always named, never silently dropped, and the budget keeps a multi-instance resume under the ~10K-token host ceiling documented for Codex. Door, ordinary advance, and base-junction serves are unchanged — the projection is deliberately resume-only. Verified by connection-level regression tests (state projected at a step whose prose does not render it; trust fields absent; truncation and omission notices pinned; ordinary next serves carry nothing) plus the full resume/reorientation suite; red/green demonstrated for both the projection and the budget; go vet, the full test suite, and golangci-lint clean; the tool-contract snapshot regenerated once for the new response field. An adversarial review approved with no blockers and drove the aggregate budget in a fix round. Closure rationale: the gap's observable — durable values absent from the resumed response — no longer occurs; the projection-boundary design it left open (filtered trajectory, completed ancestors, compression semantics) is carried by the standing invariant (20260719-124405-d-cpt-0tm) and the serve-composition genericity question this work surfaced (20260719-135225-s-cpt-m8n), which future design starts from. Implementation and review ran as delegated agents; sizing study attached to the question entry.
