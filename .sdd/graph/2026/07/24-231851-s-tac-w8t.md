---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260722-112853-d-tac-ln1
      kind: addresses
      desc: Delivers the plan's binding-consumption slice without closing the remaining procedure-prose slice.
participants:
    - Christopher
confidence: high
topics:
    - collaboration/concurrent-work
    - implementation/engine
    - implementation/mcp
    - reliability/testing
summary: 'Commit `7f307cb` delivers the binding-consumption slice of the session branch-binding and store-locality plan (20260722-112853-d-tac-ln1): ordinary capture resolves `captureBranch` before the durable session binding, summary replacement follows the actual successful default target, WIP start/done rejects empty `baseBranch`, procedure-context graph reads fall back through the binding, and attached free reads use the bound branch without affecting unbound or unattached behavior. Vector and hybrid reconciliation hash and chunk attachments through the selected target store, with binding-sourced acquisition errors retaining truthful provenance. The plan''s procedure-prose slice remains open, and two mutable-binding policy questions — definition selection and post-write rebind — are explicitly deferred as separate work.'
---

Commit `7f307cb` completes the binding-consumption slice of the session branch-binding and store-locality plan (20260722-112853-d-tac-ln1): ordinary capture now resolves explicit `captureBranch` before the durable session binding and configured default, summary replacement follows the actual successful default target, WIP start/done rejects an empty `baseBranch`, procedure-context graph reads fall back through the binding, and attached free view/show/search reads use the bound branch without changing unbound, unattached, or dependency behavior. Vector and hybrid reconciliation now hash and chunk attachments through the same selected target store while its acquisition remains live, and binding-sourced acquisition errors retain truthful provenance and recovery guidance.

Three independent review lenses drove fixes before commit. Architecture, feature, and adversarial passes found and closed selected-store attachment authority/lifetime, bound read provenance, overclaimed drift diagnosis, branch-exclusive ref and cache invalidation coverage, procedure-context routing, and a no-write retry that could pin the default before a later binding. The final retry regression proves a rejected unbound attempt leaves no stale target and a binding declared before retry wins. A first-pass attempt to select procedure definitions and listing from the binding was removed after the exact read decision was re-read: it covers `CurrentFor` and attached free reads, while definition selection under a mutable binding requires a separate replay-authority policy. The related post-write rebind question was likewise deferred rather than decided inside this slice.

Final architecture, feature, and adversarial re-reviews are clean. Uncached full tests, application/MCP race tests, vet, lint, and diff checks all pass. This slice deliberately records no acceptance-criterion deviation: the unchanged unbound/unattached read behavior is the plan's explicit Decision 4 boundary, and the two mutable-binding policy questions are separate open work rather than hidden expansion.
