---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260702-220449-d-tac-ry0
      kind: addresses
      desc: supplies slice 3 — ACs three and four plus the bootstrap-skill half of seven — without closing the plan
    - id: 20260702-235429-s-tac-8io
      kind: builds-on
      desc: registers the queries and commands the engine core deliberately left to the shell
    - id: 20260606-004059-d-tac-d21
      kind: addresses
      desc: read_attachment ships the MCP paged half of the read-side commitment
    - id: 20260527-212055-d-tac-6zt
      kind: addresses
      desc: stage_attachment ships the staging surface the plan re-homed this commitment into
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - portability/mcp
    - skill/architecture
summary: 'The MCP shell for the v1 workflow engine shipped in `internal/mcpserver` (commit 39ac582): the procedure loop (start_procedure, next, abandon) drives engine instances with verbatim chooser answers, sessions persist as append-only JSONL logs with replay across restarts, and graph writes remain reachable only as procedure transitions with no direct write tool. This progresses acceptance criteria three, four, and the bootstrap-skill half of seven (20260702-220449-d-tac-ry0), registers the graph-coupled queries and effectful commands the engine core left to the shell (20260702-235429-s-tac-8io), ships read_attachment as the MCP paged half of the read-side commitment (20260606-004059-d-tac-d21), and delivers stage_attachment as the re-homed staging surface (20260527-212055-d-tac-6zt).'
summary_hash: f2e7e18b9fdcbb1af1ca13099fde1e96
---

Slice 3 of the v1 workflow engine plan is complete: the MCP shell shipped in `internal/mcpserver` (commit 39ac582), replacing the spike grounding-gated server with the spec-exact surface — the loop (start_procedure, next, abandon) drives engine procedure instances with chooser answers carrying the user's words verbatim, sessions persist as append-only JSONL logs under `.sdd/sessions/` (gitignored via init housekeeping) with list_sessions deriving descriptors from the log alone and resume_session replaying across server restarts, stage_attachment fills session scratch that the newEntry write gate materializes, and the free reads (search, view, show, info, registry) are joined by read_attachment, the paged accessor realizing the read-side commitment (20260606-004059-d-tac-d21) so agents never derive storage paths. The shell registers the graph-coupled queries (sessionInfo, viewLayout, entryChains, generatedSummary) and effectful commands (newEntry with pre-flight inside and the pre-flight override reachable only through a user chooser, replaceSummary, wipStart, wipDone) that the engine core handed forward (20260702-235429-s-tac-8io), keeping graph writes reachable only as procedure transitions — there is no direct write tool. Framing injects once per bound agent consumer; served-instruction memory serves each unit full-text once and a one-line reminder after, resetting on resume (new consumer). The thin sdd-engine bootstrap skill ships next to the untouched /sdd for per-session choice. This progresses the plan's third and fourth acceptance criteria and the bootstrap-skill half of the seventh (20260702-220449-d-tac-ry0), and ships session staging as the re-homed upload-token commitment (20260527-212055-d-tac-6zt).

Integration tests drive the full capture fixture over an in-memory MCP transport — one-shot batch drafting, adjust/confirm playback with served-memory reminders, blocked-write routing to the user-only override, restart-resume with evidence intact and memory reset, staged-attachment materialization, chooser sequence validation, and attachment paging — and a live smoke run against this repo's graph confirmed the twelve-tool surface, real framing, and graceful no-procedure handling. Carried forward: the base capture procedure entry ships in slice 4, so start_procedure has nothing to execute until then; the wipStart/wipDone store contracts (reads anchor and wipDescription, writes wipMarker) await their first consuming procedure; the local `sdd show` absolute-path half of the attachment read commitment remains open; and whether the upload-token directive can close on the shipped staging surface is a grooming question.
