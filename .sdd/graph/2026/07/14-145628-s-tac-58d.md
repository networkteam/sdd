---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260714-140424-s-tac-fco
      kind: builds-on
      desc: completes the source-cleanup semantics omitted from the original migration delivery
    - id: 20260714-142231-s-tac-zdt
      kind: surfaced-by
      desc: the inner evaluation that isolated this cleanup finding
closes:
    - 20260714-142440-s-tac-19f
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - portability/runtime
    - reliability/testing
    - cli/init
summary: Commit 10e56f3 completes the staged-attachment migration as a true move by deleting each source `<session>-staging` directory after the current session log is durably installed, with cleanup failure propagating independently so retries handle only residue removal and the current-format path clears directories left by pre-fix migrations without reprocessing the session. This closes the copy-instead-of-move gap (20260714-142440-s-tac-19f), fulfills the move semantics omitted from the original delivery (20260714-140424-s-tac-fco), and resolves the sole finding from the code evaluation (20260714-142231-s-tac-zdt).
---

Commit 10e56f3 makes legacy staged-attachment migration a true move by removing each source `<session>-staging` directory after the current session log is durably installed, closing 20260714-142440-s-tac-19f. Cleanup failure now propagates without rolling back the successfully installed current log, so a retry reaches the current-format path and retries only the residue removal; that same path cleans staging directories left by migrations performed before this fix without rewriting the current session. Failures before replacement still preserve both the legacy log and its staging directory. Focused tests cover successful cleanup, pre-replacement preservation, and idempotent residue cleanup, while `go test ./...`, `go vet ./...`, `golangci-lint run ./...`, and the local build all pass. This completes the staged-file move semantics missed by 20260714-140424-s-tac-fco and resolves the sole finding surfaced by 20260714-142231-s-tac-zdt.
