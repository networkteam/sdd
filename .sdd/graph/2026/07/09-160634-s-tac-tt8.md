---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260703-200000-d-prc-evl
      kind: grounded-in
      desc: the evaluate procedure this outer-validation run followed
    - id: 20260709-141700-s-tac-jbs
      kind: builds-on
      desc: the shipped slice-4 / whole-plan work this outer validation judges
    - id: 20260709-152356-s-tac-6n3
      kind: related
      desc: the complementary concurrent inner code evaluation of the same slice-4 work; together both-lens coverage
participants:
    - Christopher
confidence: high
topics:
    - evaluation/outer/user-feedback
    - cli/ux
    - search/index
summary: 'Outer validation of the shipped slice-4 terminal-UX work (20260709-141700-s-tac-jbs) finds it serves in real use but not cleanly: progress rendering on the previously-silent connected-index build, cross-repo search ranking, and foreign-entry resolution by full ID all work, while escape-sequence leakage on `sdd repo add`, a raw `context canceled` error on Ctrl-C, an uncommitted config write, and short-ID non-resolution do not. This run covers the outer lens only per the evaluate procedure (20260703-200000-d-prc-evl), with the inner lens handled concurrently (20260709-152356-s-tac-6n3), together giving both-lens coverage; it surfaces five findings and grounds a follow-up directive on `sdd repo remove` scoping.'
---

Outer validation of the shipped config-and-state placement work — the whole of the plan closed by 20260709-141700-s-tac-jbs — finds it serves its purpose in real use, but not cleanly. Scope was the outer lens only (validation: does it serve in use); the inner lens is covered by a complementary concurrent run (20260709-152356-s-tac-6n3, an inner code evaluation of the same slice-4 work), so this run deliberately does not re-judge soundness — together they give both-lens coverage of the slice. This is the second-round outer validation the plan parked, following the first-round run on the original cross-repo implementation. Christopher drove the surfaces on a TTY. What works: `sdd index --all-repos` (warm) cleared its footer cleanly with a styled '+1 connected repo(s)' summary; `sdd index --all-repos --force` rendered an honest determinate bar (observed at 64/4687 chunks) with a live 'embedding N entries' note and per-entry indexed lines scrolling above a cleanly-redrawing footer — the previously-silent ~35-minute connected-index build is now visible; `sdd search --repo` returned cross-repo hits ranked inline with local ones; and `sdd show`/`sdd view` listed cross-repo refs and resolved a foreign repo's entry by full ID. What doesn't: `sdd repo add` on the fast already-connected path leaked terminal mode-query escape sequences into stdout and the next shell prompt (the config write itself was correct); Ctrl-C during a long embed tore down cleanly but printed a raw 'context canceled' error; the committed .sdd/config.yaml write was left uncommitted; and a foreign short ID did not resolve. Judgment: the terminal-UX slice ships and serves — its progress rendering achieves what the silent-hang finding demanded — but the surrounding cross-repo dependency lifecycle needs a follow-up design pass. Recorded surfaced-by this evaluation: five findings (a cross-repo dependency-lifecycle gap, a resolve-or-block lint check, transient-coordinator lifecycle edges, config-write auto-commit, and a short-ID-resolution question), complementary to the concurrent inner run's three reliability findings; it also confirms the still-open documentation/skills gap on cross-repo ref authoring guidance (folded there, no new entry) and grounds a follow-up directive redefining `sdd repo remove` to project-scoped, ref-safety-guarded removal.
