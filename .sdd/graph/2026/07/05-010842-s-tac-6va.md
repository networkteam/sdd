---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260702-204144-s-tac-j5a
      kind: refines
      desc: widens the multi-probe gap in place with the layout-unification half
supersedes:
    - 20260705-010815-s-tac-msm
participants:
    - Christopher
    - Claude
confidence: medium
topics:
    - cli/ux
summary: Search results should be rendered through the shared view-pipeline layout vocabulary — brief lines, render terminators, filters, caps — rather than a bespoke search output format, with search acting as a pipeline source so one call can fuse, de-duplicate, and shape-control multiple probes via existing layout options. This adds the layout-unification half to the existing multi-probe gap (20260702-204144-s-tac-j5a), which recorded only the N-probes-with-dedup requirement; together they form the complete gap. The supersession corrects a malformed ID in the first capture.
summary_hash: f1432bdf8a424d095edc68b1ddcc4885
---

The search surface should unify with the view pipeline: retrieval results rendered through the same layout vocabulary — brief lines, render terminators, filters, caps — instead of search's bespoke output format, with search acting as a source in the pipeline so one call can combine multiple probes into a single fused, de-duplicated, layout-controlled result. This widens the multi-probe search gap (20260702-204144-s-tac-j5a) in place: that entry records the N-probes-with-dedup half, while this adds the layout-unification half surfaced while deciding the drill-serve defaults — where search responses being model context makes size- and shape-control through the existing layout options the natural mechanism rather than search-specific flags. Supersedes its first capture, whose body cited the target gap under a malformed ID.
