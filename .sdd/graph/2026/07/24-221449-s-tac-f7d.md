---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260722-112853-d-tac-ln1
      kind: addresses
      desc: implements and verifies the plan's binding-core acceptance area without closing the remaining consumption and procedure slices
    - id: 20260719-124405-d-cpt-0tm
      kind: grounded-in
      desc: makes the durable binding agent-visible on re-entry and reorientation
participants:
    - Christopher
confidence: high
topics:
    - collaboration/concurrent-work
    - implementation/engine
    - portability/mcp
    - agent/ux
    - reliability/testing
summary: 'Slice 2 of the session branch-binding plan is implemented and committed as `b3bff10`: sessions now carry an explicit durable branch binding set or cleared via `bind_branch`, validated against exactly one live registered checkout, and projected into door, resume/reorientation, listing, and framing responses. This realizes the binding-core acceptance area of the branch-binding plan (20260722-112853-d-tac-ln1), with consumption, WIP targeting, procedure reads, and drift errors left to remaining slices. Durable branch state survives restart and appears in the session info block, satisfying the agent-visible continuity requirement (20260719-124405-d-cpt-0tm); two pre-commit defects — missing framing projection and stale metadata on reorientation — were caught, fixed, and verified clean.'
---

Slice 2 of the session branch-binding plan is implemented and committed as `b3bff10`: sessions now carry an explicit durable branch binding that can be set or cleared through the attached-session `bind_branch` capability, validated against exactly one live registered checkout without acquiring graph adapters, and projected into door, resume/reorientation, listing, and framing responses. This realizes the binding-core acceptance area of 20260722-112853-d-tac-ln1 while preserving hosted deployments through an optional validation port and a typed no-branch-capability failure; clear remains available without a branch concept, and absent metadata remains unbound for compatibility.

The transition is persisted by the existing session CAS append with `branchBound` / `branchCleared` events and branch payloads. The local Git adapter shares one resolution rule between declaration validation and later target acquisition: branch syntax, exactly-one-checkout resolution, and symbolic HEAD agreement are checked live, while the resolve-only path never opens graph or finalizer adapters. Durable branch state is visible after restart and in the session info block, satisfying the agent-visible continuity requirement of 20260719-124405-d-cpt-0tm.

Independent architecture, feature-correctness, and adversarial reliability reviews found two in-scope defects before commit: the first pass exposed the binding only as a response field but omitted it from framing, and a terminal second CAS conflict could leave same-session reorientation projecting an older cached branch/version. Both were fixed with deterministic regressions: bound framing now includes `Branch binding: <branch>` while unbound framing remains byte-identical, and the reorientation held-check refreshes authoritative metadata only after codec/identity validation and exact attachment confirmation. All three re-reviews returned clean. Final gates pass: `go test ./...`, `go vet ./...`, `golangci-lint run ./...` with zero issues, `git diff --check`, and focused race suites across the affected application, local, MCP, and CLI packages.

No plan-level policy was added or changed. This slice deliberately does not yet consume the binding for ordinary capture, summary replacement, procedure/free reads, WIP targeting, or binding-provenance drift errors, and it does not change procedure prose; those remain the next slices of 20260722-112853-d-tac-ln1.
