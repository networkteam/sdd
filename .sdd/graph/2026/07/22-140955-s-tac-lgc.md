---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260703-200000-d-prc-evl
      kind: grounded-in
      desc: the evaluate procedure this outer-validation run followed
    - id: 20260722-084742-s-tac-3gl
      kind: builds-on
      desc: the bootstrap-procedure-port closing done that this evaluation validates in use
participants:
    - Christopher
confidence: high
topics:
    - evaluation/outer/user-feedback
    - engine/base-procedures
    - collaboration/onboarding
summary: 'An outer validation of the bootstrap procedure port across two live local-agent runs — one greenfield, one brownfield — finds the procedure produces a coherent, useful orientation flow end to end but is not yet at parity: actor/role capture is the dominant gap, compounded by an illegible kind-conditional assemble gate and topic-landscape founding that mints provisional topics before user signoff. The evaluation builds on the bootstrap implementation closing done (20260722-084742-s-tac-3gl) and recommends holding legacy-skill retirement until those three areas are addressed; it followed the evaluate procedure (20260703-200000-d-prc-evl) and surfaces five findings as follow-up gaps.'
---

An outer (validation) evaluation of the bootstrap procedure port (20260722-084742-s-tac-3gl) across two live local-agent runs — one greenfield on an empty graph, one brownfield on a sparse graph over an existing repository with a connected dependency graph — finds the procedure works as an orientation flow and produces a coherent, useful foundation end to end on both, but is not yet at parity, with actor/role capture the dominant friction. Scope: outer lens only, driven by the local participant who ran both sessions and attested the transcripts; inner verification was already attested by the closing done and was not re-run here. What held: journalist-posture questioning, the meaning-level cluster playback (which absorbed heavy iterative reshaping without dropping into field-by-field review), dependency-ordered capture, correct gap/question discrimination, and the catch-up handoff — all end to end. Standout strength: brownfield host-side repository inspection and connected-graph grounding — the run read the repository locally, surfaced the decisive connected-graph decisions early, grounded its opener in them, kept inspection host-side, and referenced rather than duplicated a connected decision. Not yet at parity: actor/role capture reads primarily as an engine knowledge gap — the agent has no served guidance for composing identity entries (canonical matching the configured participant, alias collection, introduce-canonical-in-prose, role-to-active-actor-head reference, process layer) — and mechanical validator quirks around actor supersede compound it; the kind-conditional assemble gate is illegible, serving every branch's predicate flatly, which causes fabricated refs on near-root entries and leaked identity fields on non-identity entries; and topic-landscape founding mints provisional topics before user signoff and never applies the agreed vocabulary back. Recommendation: hold retirement of the legacy bootstrap skill until the actor/role composition path, assemble-gate legibility, and topic founding are addressed. Five findings are surfaced by this evaluation as their own follow-up gaps; three further notes (an unseeded materialize dispatch forcing restarts, recurring validator noise, and a session/door reconnect friction already resolved by since-merged session-model work) and one agent-fidelity observation (a scoped "just do it" instruction overshot into skipping the topic-landscape signoff) are carried in this evaluation. This run followed the evaluate procedure (20260703-200000-d-prc-evl); both runs executed on a pre-merge binary.
