---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260703-142447-d-tac-nqo
      kind: related
      desc: the open-threads directive whose junction-only, mid-serve-clean rule shaped the AC4 narrowing to junction and recovery serves
closes:
    - 20260709-160621-d-tac-zm2
    - 20260709-153648-d-cpt-xgq
    - 20260709-153017-s-cpt-h31
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - portability/mcp
    - agent/ux
summary: 'Post-compaction reorientation shipped and verified: resume_session now accepts an omitted session or the connection''s own ID and re-serves the bound session''s live position — framing plus every running move at its current step with its report schema — in one handle-free call, with recovery guidance placed in compaction-durable channels (tier-zero server instructions, tool description, junction and recovery serves). This closes the reorientation plan (d-tac-zm2), retires the reorientation-guarantee directive (d-cpt-xgq), and resolves the reach gap (s-cpt-h31) now that the surface exists. Recovery breadcrumbs were narrowed to junction and recovery serves rather than all mid-procedure serves, honoring the mid-serve-clean rule (d-tac-nqo); inner verification passes across 23 packages, with outer validation in live compacted conditions to follow.'
---

Post-compaction reorientation shipped: an engine agent that lost its handles can now reach its live session position in one handle-free call, closing the reach gap that surfaced in a live session on another project. Merged to main as commit d40510a (implementation commit 9dadb90). resume_session now accepts an omitted session (or the connection's own id) and re-serves the bound session's live position — framing plus every running move at its current step with its report schema — with no rebind or leave, forgetting served-block memory first so framing serves in full. Recovery guidance moved into compaction-durable channels: the tier-zero server instructions, the resume_session tool description (rewritten symptom-led — lost handles, or compacted/summarized context), the open-threads intro on junction serves, and the resume serve. Unbound connections still bootstrap via the parked-session list. AC4 was narrowed in dialogue to junction and recovery serves rather than every mid-procedure serve, honoring the open-threads directive (d-tac-nqo) that keeps mid-move serves clean; the tier-zero server-instructions line was added as the strongest durable channel the directive names. All five acceptance criteria are met and inner-verified: go vet ./... clean and go test ./... green across 23 packages, with new tests (TestResumeReorientsCurrentSession, TestResumeNoSessionUnboundListsParked) driving the no-session and current-id reach paths over the real MCP tool surface and asserting the running move's report schema returns. This closes the plan (d-tac-zm2, its acceptance criteria met), retires the reorientation-guarantee directive (d-cpt-xgq, whose own text set shipping as its close), and closes the reach gap (s-cpt-h31, now that the surface exists). Inner verification only — outer validation, whether a genuinely compacted agent recovers in one call in live use, will follow in a Codex session.
