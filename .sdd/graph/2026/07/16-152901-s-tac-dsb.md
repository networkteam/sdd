---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260716-133307-d-tac-9td
      kind: addresses
      desc: delivers slice 1 of the plan; slice 2 remains open
    - id: 20260716-142323-d-tac-js8
      kind: addresses
      desc: realizes the amendment's slice-1 delivery order including the embedded-entry index rules
closes:
    - 20260716-121435-s-tac-ex4
    - 20260716-152334-s-tac-wyu
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - implementation/mcp
    - portability/mcp
    - portability/runtime
    - search/index
    - reliability/testing
summary: 'Slice 1 of the persistent MCP vector index repair is implemented and merged: `sdd serve` now shares the machine-global chromem index with the CLI, using monotonic entry-presence reconciliation with no request-shaped deletes and all filtering at read time, cutting cold search from ~8 minutes to under 12 seconds on first access and 0.79s thereafter. This delivers slice 1 of the repair plan (d-tac-9td) and realizes the amendment''s slice-1 delivery order including embedded-entry index rules (d-tac-js8), while closing the cold re-embed regression (s-tac-ex4) and the silent type-filter no-op (s-tac-wyu); both referenced decisions remain open for slice 2.'
---

Slice 1 of the persistent MCP vector index repair is implemented, verified, and merged to main (merge f4bbf5f): `sdd serve` now reads and writes the same machine-global chromem index the CLI builds, reconciliation is monotonic — entry-presence based, graph revision ignored for freshness, no request-shaped deletes anywhere — and all filtering happens at read time. Commits a10f0b5 (shared chunk derivation in internal/chunking plus lock-before-load WriteStore/ReadStore), eda65da (monotonic reconciliation rewrite with entry-manifest presence and a chunk-identity fallback), 4fc5f74 (serve wiring: base key matching the CLI, connected stores keyed per repo ID with embedded entries excluded), d4de929 (conformance, counter-based application tests, and a subprocess production-path test over MCP stdio), and 971988f (normalize and validate MCP search type/layer/kind filters with loud typed errors).

Verified live against the real repository index: a fresh MCP server answered a phrase search in 11.3s (one-time incremental top-up of entries captured since the last CLI fill), a second fresh server in 0.79s with zero document embeds — against roughly 8 minutes of full re-embed before the fix. The filtered path failed in a real Codex session during validation (surfacing the type-normalization gap it closes alongside), and after the fix the originally failing call returns the expected gaps while a garbage kind errors loudly in 0.14s. All gates green on the branch and re-verified after merge: go vet, go test across 26 packages, golangci-lint, and the extendingsdd example module.

This closes the cold re-embed regression and the silent filter no-op; the plan (d-tac-9td) and its amendment (d-tac-js8) stay open for slice 2 — version-aware freshness, GC, the generation-checked snapshot cache, and the connected-store force repair. See the [verification note](./16-152901-s-tac-dsb/slice-1-verification.md) for the review findings, ratified judgment calls, and smoke measurements.
