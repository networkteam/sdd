---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260718-180550-d-tac-e3x
      kind: addresses
      desc: realizes slice 2 (handle-addressed tools and free discovery) of the five-slice plan without closing it — slices 3 through 5 remain
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - implementation/mcp
    - portability/mcp
    - agent/ux
summary: 'Slice 2 of the five-slice session-model plan has shipped (commits 9905913, eb03ed1): every work tool now requires an explicit session handle with typed errors for missing handles and attach-funnel mismatches, resume_session attaches named parked sessions on fresh unbound connections, and list_sessions exposes all open-work sessions with live-held ones labeled active and naming their holding client. Server instructions, tool descriptions, and the tool-contract snapshot test are rewritten to the handle-carried model; go vet, go test, and golangci-lint are clean. This advances the handle-required-tools slice of the realization plan (20260718-180550-d-tac-e3x) without closing it — slices 3 through 5 remain.'
---

Slice 2 of the session-model realization plan shipped in the session-model worktree (commits 9905913, eb03ed1): the session handle is now a required argument on every work tool (next, start_procedure, park, stage_attachment, abandon-by-instance) with a typed missing-handle error naming both doors and a typed attach-funnel error for a session this connection is not attached to, while resume_session keeps its optional handle as the compaction escape and — killing the continuity trace's unbound trap — now attaches a named parked session on a fresh unbound connection. list_sessions works unbound; listings show every session with open work, live-held ones labeled active with the holding client instead of hidden, and the in-use attach refusal names the holding client and its last activity. Server instructions and all tool descriptions are rewritten to the handle-carried, host-neutral model and pinned by the tool-contract snapshot test. Connection state is reduced to served-once dedup plus the attached-session record; no work tool resolves an omitted session from the connection. Holder machinery is untouched (Slice 3 deletes it); the active/idle label derives from holder liveness until the attachment stamp lands. Three independent reviews found no blockers; the fix round landed richer in-use refusals, honest prose on rejection paths, a table-driven funnel test across all five work tools, and the switch-path park assertion. go vet, go test ./..., and golangci-lint are clean on the worktree branch.
