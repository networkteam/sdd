---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260727-231512-d-tac-irt
      kind: builds-on
      desc: 'the plan whose recovery-projection fix this run validates, and whose AC #5 reserved this live-store run for Christopher'
    - id: 20260703-200000-d-prc-evl
      kind: grounded-in
      desc: the evaluate procedure this run followed, scoped to a single lens rather than stretched across both
    - id: 20260727-230506-d-cpt-msg
      kind: grounded-in
      desc: 'the two-durable-conditions contract supplying the standard the projection is measured against: delivered versus pending delivery'
    - id: 20260726-204112-s-prc-ogp
      kind: grounded-in
      desc: the run-it-once discipline that makes this live-store run a distinct requirement — and which the first observation shows applies one level up, to the plan's own boundary criterion
    - id: 20260727-102820-s-tac-9op
      kind: addresses
      desc: the stranded-notice gap whose thirty-four-item census this run measures as cleared; the gap closes on the plan's own done, not on this evaluation
participants:
    - Christopher
confidence: high
topics:
    - evaluation/outer
    - implementation/engine
    - reliability/testing
    - reliability/discipline
    - portability/runtime
summary: 'Live-store outer validation confirms all thirty-four stranded pending-write notices are cleared: `recover --history` returns exactly 80 previously recovered and 34 now `applied-finalized`, and no surface reports anything actionable, satisfying the Christopher-reserved acceptance criterion of the recovery-projection fix (20260727-231512-d-tac-irt) and measuring the stranded-notice census from (20260727-102820-s-tac-9op) as resolved against the two-durable-conditions standard (20260727-230506-d-cpt-msg). Two observations are recorded: the initial fix over-suppressed by treating absent finalizer records as success — a silent-loss shape worse than the original defect, corrected before merge, and a plan-level finding because the plan''s own acceptance criterion was satisfiable by the over-suppressing code, extending the run-it-once discipline (20260726-204112-s-prc-ogp) to boundary conditions in plans themselves; and the plan''s session count was wrong (three named, seven actual), harmless here because the fix reads recorded state rather than enumerating instances, but a reminder that a hand-audited list carries weaker evidence than the count it accompanies.'
---

Outer validation of the recovery-projection fix (20260727-231512-d-tac-irt), run against the real accumulated live session store, confirms all thirty-four stranded pending-write notices are cleared: `recover --history` returns 114 items in exactly two states — 80 previously recovered and exactly 34 now `applied-finalized` — and no surface reports anything actionable. This is the criterion the plan reserved for Christopher and forbade implementation and inner evaluation from claiming, and he drove it: a fresh engine session opened against the live store carried zero pending-write notices where one previously surfaced thirty-four, and `info` — whose framing populates recovery notices only when the render is non-empty (`application/application.go:49` and `:136`) — reports none, making the absence a positive zero rather than a silent surface. The census matching the plan's figure exactly matters: the intents read as delivered, not merely suppressed, and the store was left untouched, consistent with the read-side-only commitment and with the two durable conditions of 20260727-230506-d-cpt-msg. Attribution was checked rather than assumed, since a store that moved after the recent locality work would produce the same clean result for the wrong reason: the store holds all three sessions the plan named plus four further stranded-intent sessions, and because the engine serving this session predates the newest commit, both reads were re-run with the HEAD binary and reproduced identically — so the judgment holds at HEAD. The evaluation covers the outer lens only; the ordering and finalizer-boundary tests, fixture provenance, the no-store-write diff review, and the toolchain runs remain unjudged, with no inner evaluation of this fix in the graph.

Two observations are recorded here rather than as separate entries, at Christopher's direction.

The first is a gap in the plan, not only in the code. The initial fix over-suppressed: `finalizationOwed` treated zero recorded finalizer outcomes as success, so an applied mutation whose finalizers never reported projected as delivered — and because `ListRecoveries` filters non-actionable items and `selectRecoveryItem` searches only that list, the reconciliation path became unreachable, leaving a graph entry on disk with its git commit owed and no machine-readable trace that it was owed. That is a silent-loss shape, worse in kind than the noisy defect being fixed. It was caught and corrected before merge by requiring positive proof of delivery, with two tests pinning the boundary from the silent side, both derived from the real stranded fixture. What makes it a plan-level finding is that the plan named the boundary the fix must not cross as a recorded finalizer *failure* keeping the item actionable — while the actual breach was *silence*, no finalizer record at all. Its acceptance criterion was therefore fully satisfiable by the over-suppressing code. This is the discipline of 20260726-204112-s-prc-ogp applying one level up: conformance to an under-specified criterion cannot catch what the criterion failed to name, so a plan's boundary conditions deserve the same distrust as its implementation.

The second is smaller and carries no consequence here. The plan states that three sessions hold the stranded intents; the store holds seven. The total of thirty-four was right, its attribution to sessions was not — harmless because the fix teaches the projection to read recorded state rather than enumerating known instances, so its reach is class-wide. But a fix scoped to that enumeration would have missed four sessions, which is the reminder worth keeping: a hand-audited session list inside a plan body is evidence of a weaker grade than the count it accompanies.
