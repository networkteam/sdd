---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260718-112133-s-tac-eqv
      kind: refines
      desc: 'adds a second live reproduction: a fresh request was wrongly treated as consent to resume the parked session from the first trace'
    - id: 20260706-231416-s-tac-y2z
      kind: refines
      desc: sharpens the existing other-dialogue pickup ambiguity from liveness confusion to user-intent confusion
    - id: 20260703-142447-d-tac-nqo
      kind: builds-on
      desc: the open-threads directive requiring other dialogues to be presented as continuation options
    - id: 20260704-000621-d-tac-bfc
      kind: grounded-in
      desc: the lifecycle behavior exercised by opening a fresh shell and auto-concluding it when switching
    - id: 20260704-100000-d-prc-dlg
      kind: grounded-in
      desc: the active shell procedure whose routing text says recommend and never auto-run
participants:
    - Christopher
confidence: high
topics:
    - portability/mcp
    - agent/ux
    - reliability/discipline
summary: This signal records a live Codex failure in which an agent treated a request to find recent unfinished planned work as permission to resume a semantically matching parked dialogue, without offering that continuation or asking for confirmation, contrary to the served “present as options” and “recommend, never auto-run” instructions. The named resume switched away from the fresh shell, which the current lifecycle then auto-concluded, and replayed the older catch-up at its existing user junction rather than running a fresh catch-up; when asked, the agent said the exact label match led to its inference and acknowledged that the inference was wrong. The observation refines the earlier session trace and other-dialogue pickup gap (s-tac-eqv, s-tac-y2z), builds on the continuation-options directive (d-tac-nqo), and is grounded in the exercised lifecycle and active shell procedure (d-tac-bfc, d-prc-dlg).
---

In a fresh Codex engine session, the agent interpreted a user request as permission to resume a semantically matching parked dialogue, did not offer that continuation or ask for confirmation, and thereby failed to follow the served “present as options” and “recommend, never auto-run” instructions.

The trace is direct: `start_session` created and served a new shell; after the user asked to find recent unfinished planned work, the agent announced that it would “pick up the existing check-in” and called `resume_session` with the parked session labeled `Recent planned work not completely finished`. The switch auto-concluded the empty fresh shell and replayed the older catch-up at its existing user junction instead of running a fresh catch-up.

When asked why, the same agent stated that the exact label match led it to infer that the user meant “continue that work,” acknowledged that this inference was wrong, and cited the same served instructions it had failed to follow. The attached observation preserves the user transcript, agent retrospective, persisted timelines for both sessions, relevant instruction text, and the current session-switch behavior, without proposing a solution or asserting another cause.
