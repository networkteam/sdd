---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260703-200000-d-prc-evl
      kind: grounded-in
      desc: the evaluate procedure this evidence-backed outer-validation run followed
    - id: 20260712-234500-d-tac-x1m
      kind: builds-on
      desc: the active public API implementation whose external composition this run validates
confidence: high
topics:
    - evaluation/outer/user-feedback
    - portability/mcp
    - collaboration/identity
summary: 'Outer validation of the public project-aware SDD runtime confirms its shared application surface is genuinely consumable: an independent external module compiled without internal imports, mounted the Streamable HTTP application, passed authentication and free-read checks, and opened a durable dialogue, while this evaluation itself exercised the worktree''s local `sdd serve` composition. The core integration shape holds, but a release-significant hosted-use finding remains: participant identity is split between the resolved principal and project runtime, producing blank initial framing and inconsistent authority across open, persistence, resume, and mutation defaults. This run followed the evaluate procedure (20260703-200000-d-prc-evl) against the active implementation plan (20260712-234500-d-tac-x1m); inner verification remains uncovered.'
---

First external use of the public project-aware SDD runtime validates that its shared application surface is genuinely consumable, but finds participant identity split across the resolved caller and the project runtime. This outer-only run followed the evaluate procedure (20260703-200000-d-prc-evl) against the active implementation plan (20260712-234500-d-tac-x1m): the independent external module compiled without internal imports, mounted the shared Streamable HTTP application, accepted valid bearer authentication, rejected an invalid token with HTTP 401, served a free read, and opened a durable dialogue session. In parallel, this evaluation itself ran through the worktree's real `sdd serve` MCP composition: it opened the session, searched and showed graph entries, carried the evaluate procedure through its evidence-backed judgment, staged the evaluation attachment, and advanced the resulting capture to playback. Together these runs validate both the external HTTP composition and the local command's composition over the shared public application. The core integration shape therefore holds—external code supplies authentication and infrastructure while SDD retains application behavior—but the live external session exposed blank initial participant framing despite a resolved principal. Source tracing and Christopher's hosted-model attestation showed that initial framing and engine state use a runtime participant, durable session metadata and resume use the resolved principal, and mutation defaults mix both through a fallback. In the hosted model, request identity resolves the user and then a project-specific participant override or user-global canonical participant; the local analogue resolves the effective repository configuration into the local principal. Judgment: the public surface serves its intended composition, but the split participant authority is a release-significant hosted-use finding. Inner verification remains uncovered by this run; the full API trace and reasoning are attached.
