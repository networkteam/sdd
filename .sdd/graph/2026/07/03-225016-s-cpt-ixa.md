---
type: signal
layer: conceptual
kind: gap
refs:
    - id: 20260614-110729-d-cpt-chi
      kind: grounded-in
      desc: the single-source contract this run shows unmet for register guidance
    - id: 20260702-220449-d-tac-ry0
      kind: surfaced-by
      desc: observed in the second dogfooding run of the plan
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - skill/architecture
    - agent/ux
summary: Register guidance defined in the /sdd skill does not render into the sdd-engine skill or any served instructions, leaving the engine surface without vocabulary discipline. This violates the single-source contract (20260614-110729-d-cpt-chi), which requires instruction text to originate once and render into every surface that carries dialogue — evidenced by protocol vocabulary ("disposable sub-agent," "briefing lanes") bleeding directly into user-facing narration during the dogfooding run surfaced by the engine build plan (20260702-220449-d-tac-ry0). Candidate fixes include rendering the register section into the engine skill from the same source, embedding compact register reminders in served units, and grounding the rules in negative examples drawn from the engine's own design vocabulary.
summary_hash: 6daf6bcdd2e823c4384651acdcbee47f
---

The vocabulary-register discipline does not travel to the engine surface: the /sdd skill carries a full register-by-surface section (plain, outcome-focused narration; tool and protocol vocabulary stays out of dialogue), but none of it renders into the sdd-engine skill beyond a two-sentence note, and the served instructions carry no register guidance at all. The first dogfooding run leaked protocol vocabulary straight into user-facing narration — "dispatching the explore in a disposable sub-agent, keyed on s-stg-erj, with lineage back to this session", "six briefing lanes served", "drop the empty engage thread" — the procedure entries' own design words ("disposable", "lanes") bleeding into user prose. This is the single-source contract (20260614-110729-d-cpt-chi) unmet in practice: register guidance is instruction text that must originate once and render into every surface that carries dialogue, the engine surface included. Candidate directions: render the register section into the engine skill from the same source, carry a compact register reminder in user-facing served units (playback, junctions), and give the rules concrete negative examples using exactly the engine's design vocabulary.
