---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260719-190740-d-tac-ptn
      kind: addresses
      desc: realizes the base-fact index implementation plan and supplies its evaluation anchor
closes:
    - 20260719-190740-d-tac-ptn
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - implementation/cli
    - cli/view
    - engine/base-facts
    - reliability/testing
summary: The derived base-fact index implementation is complete, shipped across commits 60adf47, b7a31d8, and c5718d6, covering strict metadata, lifecycle-resolved reads, composable `indexed` filtering, opening-shell discovery, and a shared host-neutral view-layout reference. Reviews additionally generalized a partial-update defect into a Store ownership invariant enforced via recursive cloning, and all tests, lints, and smoke checks pass. This closes the implementation plan (20260719-190740-d-tac-ptn), with the prescribed fresh-agent outer evaluation remaining deliberately next to anchor on this signal.
---

The derived base-fact index implementation is complete across strict metadata, authoring and inspection, lifecycle-resolved reads, composable views, opening-shell discovery, and a shared host-neutral reference, shipped in commits 60adf47, b7a31d8, and c5718d6. Indexed facts now use one exact nested title/topic object propagated through capture, CLI, application, model, show, and playback; the pure current-project index respects embedded/project merge and lifecycle resolution without inheriting metadata; `indexed` composes as an ordinary zero-argument filter; and the shipped view-layout fact drives both Markdown guidance and terminal help from one semantic vocabulary and executable examples. Independent architecture, acceptance, and adversarial reviews also generalized a discovered partial-update defect into a Store ownership invariant: candidate validation and commit cover report batches, chooser collections, start params and inherited seeds, and command-produced state, while recursive cloning prevents mutable aliases from bypassing that boundary. Full tests, vet, lint, engine race checks, rebuild, and binary help/indexed/show smokes pass. The prescribed fresh-agent outer evaluation remains deliberately next because it must anchor on this closing signal; any behavioral failure will be captured rather than hidden.
