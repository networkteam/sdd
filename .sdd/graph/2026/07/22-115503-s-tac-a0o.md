---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260721-152753-s-tac-31o
      kind: builds-on
      desc: the evaluated work — the base-fact index implementation this evaluation verifies
    - id: 20260703-200000-d-prc-evl
      kind: grounded-in
      desc: the evaluate procedure this inner-verification run followed
    - id: 20260719-190740-d-tac-ptn
      kind: grounded-in
      desc: the closed plan whose acceptance criteria the verification checked one by one
    - id: 20260719-133529-d-cpt-qhn
      kind: grounded-in
      desc: the derived-index directive whose design contract the verification confirmed
participants:
    - Christopher
confidence: high
topics:
    - evaluation/inner/code-review
    - evaluation/inner/architecture
    - engine/base-facts
    - implementation/engine
    - reliability/testing
summary: 'Inner verification of the derived base-fact index implementation (20260721-152753-s-tac-31o) judges the work sound with qualifications: all ten acceptance criteria met, the design contract holding, and adversarial scrutiny cleared, but five findings recorded — most critically a validation-strictness split that lets a malformed index block prevent sessions from opening, and a latent store panic where recursive clone rejects `time.Time`. The evaluation followed the inner-lens scope of the evaluate procedure (20260703-200000-d-prc-evl), confirmed plan fidelity against (20260719-190740-d-tac-ptn) and design-contract integrity against (20260719-133529-d-cpt-qhn); the outer fresh-agent discovery benchmark remains uncovered and is the prescribed next run.'
---

An inner evaluation of the derived base-fact index implementation (20260721-152753-s-tac-31o) judges the work sound with qualifications: faithful to its plan and architecturally disciplined, with two confirmed medium-severity defects, one over-engineering hotspot, and two lesser findings recorded as separate entries. The run followed the evaluate procedure (20260703-200000-d-prc-evl) scoped to the inner lens only, carried out by four independent Opus review agents with distinct perspectives — architecture/CQRS and public-API soundness including the local/remote split, per-acceptance-criterion plan fidelity with a rebuilt binary and re-run smokes, an adversarial bug hunt with scratchpad repros and race checks, and testing strategy plus complexity — with agent disagreements resolved by direct code inspection before synthesis, alongside an independent vet, full-test, and lint run (all green).

Verification found all ten in-scope acceptance criteria of the implementation plan (20260719-190740-d-tac-ptn) met, and the design contract of the derived-index directive (20260719-133529-d-cpt-qhn) holding: enrollment by presence, derivation after lifecycle resolution, and no special rendering snuck into view, catch-up, or skill surfaces. Store transaction atomicity, supersession and override selection, ordering, the three metadata input paths, and the indexed filter mechanics all cleared adversarial scrutiny; the test strategy is behaviorally anchored at the right layers. The confirmed findings: a validation-strictness split where graph load records a malformed index block as a soft warning while the derived read hard-fails — and since the fact index is injected into the opening serve, one hand-edited malformed indexed fact prevents sessions from opening; a latent engine panic where the store's recursive clone rejects time.Time despite it being JSON-persistable; roughly 150 speculative lines of clone machinery around an otherwise-correct atomicity fix; a model type leaking through the application boundary in the factIndex query; and minor test gaps. Remediation directions were agreed in dialogue and are folded into the finding entries. The outer lens remains deliberately uncovered — the prescribed fresh-agent discovery benchmark still needs its own run anchored on the closing done. Full agent reports and evidence are attached.
