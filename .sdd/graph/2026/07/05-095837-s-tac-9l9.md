---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260705-010751-d-tac-dbk
      kind: addresses
      desc: realizes slice A of the four-slice completion directive, without closing it — the plan closes on its ACs
    - id: 20260704-235517-d-tac-tlo
      kind: addresses
      desc: implements the seeding contract, grounding handoff, task class, and anchor resolver this directive defines
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - portability/mcp
    - skill/architecture
summary: 'Slice A of the v1 workflow engine shipped in commit `a287683`: the unified seeding contract, parent→child grounding handoff carrying widenReport and anchor, the `task` procedure class with explore reclassified off the shell''s offered moves, and the uniform anchor/anchorHint resolver, with capture, engage, evaluate, and explore revised to use them. This realizes slice A of the four-slice completion directive (20260705-010751-d-tac-dbk) without closing it, and fully implements the seeding contract, grounding handoff, task class, and anchor mechanics specified in (20260704-235517-d-tac-tlo). Verified by engine table tests covering seeding, handoff, replay, task-class, and auto-advance-on-entry, plus an end-to-end MCP run confirming a dispatched capture inherits parent grounding.'
summary_hash: 823169b6cf00d5fdd94d61dcd081c530
---

Slice A of the v1 workflow engine shipped: the unified seeding contract (start inputs and move dispatch both seed declared state fields, gates evaluate on entry and auto-advance a satisfied step), the parent→child grounding handoff carrying widenReport and the anchor into a spawned move, the `task` procedure class (explore reclassified, kept off the shell's offered moves), and the uniform anchor/anchorHint resolver where a seeded anchor auto-advances and an unseeded one resolves from the hint — with capture, engage, evaluate, and explore revised to use them. In-slice wiring: a fixed grounding set {widenReport, anchor} rather than new spec grammar, anchor carried as state routed by SetStart (same-name param+state seeding was rejected because the store keys values by name), and seeds written as ordinary state provenance logged on the started event for resume fidelity. Verified by engine table tests (seeding, handoff, replay, task-class, auto-advance-on-entry contrast) and an end-to-end MCP run where an evaluation widens once and the dispatched capture inherits that grounding, completing in four calls. Shipped in commit `a287683`.
