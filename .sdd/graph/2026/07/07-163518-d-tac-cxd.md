---
type: decision
layer: tactical
kind: focus
refs:
    - id: 20260707-142811-s-prc-3kh
      kind: grounded-in
      desc: reproduced reach-failure instance and before-state benchmark whose closing done is the behavior-change metric
    - id: 20260707-142556-d-cpt-70z
      kind: grounded-in
      desc: the base-vs-project neutrality scope legitimizing the host-coupled release project procedure
    - id: 20260614-134122-d-tac-hza
      kind: related
      desc: the parallel tactical focus this stands alongside, not superseding
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - skill/architecture
    - portability/runtime
actors:
    - Christopher
    - Claude
when:
    from: "2026-07-07"
involvement:
    - target: 20260707-140544-d-cpt-dtv
    - target: 20260707-143547-s-prc-tso
      actors: []
summary: 'Commits to a parallel tactical focus on making the engine''s process-knowledge system real and dogfooding graph-local procedures: ship the base-facts pull layer first, then author the release skill as a graph-local project procedure, where the build itself evaluates whether the knowledge system works. The urgency is grounded in a confirmed, reproducing reach failure (s-prc-3kh), whose before-state benchmark — an agent grepping instead of pulling view grammar — is the concrete behavioral metric this focus must close. The release procedure''s host-coupling is legitimate under the base-vs-project neutrality scope (d-cpt-70z), and this thread runs live alongside the runtime-and-webapp focus (d-tac-hza) rather than displacing it.'
---

A distinct tactical focus on making the engine's process-knowledge system real and dogfooding graph-local procedures, held alongside the runtime-and-webapp focus rather than replacing it. The near-term arc: ship the base-facts pull layer first (d-cpt-dtv) — framework knowledge as embedded, pullable fact entries, plus the shell index and the indexer change that makes embedded entries searchable — then author the release skill as a graph-local project procedure (s-prc-tso), sequenced second, built using only what the graph serves and the facts that can be pulled, so the build doubles as the evaluation of whether the knowledge system works. The urgency is grounded in a reproducing failure, not a hypothesis: engine agents keep hitting reference knowledge they cannot reach — in s-prc-3kh an agent shaping a capture could not survey the topic vocabulary, guessed a rejected `sdd view` layout, and fell back to grep, recovering the working filter only by trial and error against parser errors. That gap is retained as the before-state benchmark, and its closing done — an agent in the same spot pulling the view-grammar fact instead of grepping — is the concrete metric that the pull layer changed behavior, not merely the served text. The release-procedure port is legitimate as a host-coupled project procedure under the base-vs-project neutrality scope (d-cpt-70z). This focus stands alongside the current tactical focus on the agent runtime and hosted webapp (d-tac-hza): both threads are live; this is the near-term engine-knowledge-and-dogfood push.
