---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260718-180550-d-tac-e3x
      kind: addresses
      desc: realizes slice 4 (structural consent and informative conflicts) of the five-slice plan without closing it — slice 5 remains
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - implementation/mcp
    - collaboration/concurrent-work
    - agent/ux
summary: 'Slice 4 of the five-slice session-model realization plan shipped (commits c7cb933, 6c6f285): attachment to an unowned dialogue now requires the user''s verbatim ask, takeover of a recently-attached session additionally requires an explicit flag, displacement errors are typed and informative rather than bare, teardown is restructured to prevent phantom holds, and reorientation is store-authoritative. All conflict paths — including the mwd regression — are structurally foreclosed and covered by tests. This advances the five-slice plan (20260718-180550-d-tac-e3x) without closing it, as slice 5 remains.'
---

Slice 4 of the session-model realization plan shipped in the session-model worktree (commits c7cb933, 6c6f285): attaching to a dialogue this connection does not hold now requires the user's verbatim ask, and attaching to one whose attachment is recent additionally requires an explicit takeover flag — the four-row consent decision table lives in one application-layer funnel, the mwd regression (unconsented resume of a matching parked dialogue) is structurally impossible and pinned by tests, and the consenting words ride the live attachment stamp durably for auditability. Displacement is now interpreted, not thrown bare: a displaced writer's append fails typed as session-displaced naming who took over and when with reorientation guidance (the 2sp resolution, tested end-to-end including the displaced connection itself re-attaching), an abandoned session's still-attached client is told actor, time, and reason, the benign same-writer version race retries invisibly, and the attach-path race re-runs consent against fresh state. The review round restructured teardown so abandon-by-handle never becomes the attachment (no phantom hold on mid-teardown failure, no wordless claim records) and — extending the plan by invariant I5's spirit, flagged for outer evaluation — refuses to tear down a recently-active session, so destruction is never cheaper than attachment. Reorientation is store-authoritative: a cached binding the store no longer lists is dropped and re-establishes through the consented attach path. go vet, go test ./..., and golangci-lint are clean on the worktree branch.
