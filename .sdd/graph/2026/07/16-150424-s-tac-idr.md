---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260716-145957-s-tac-5ua
      kind: surfaced-by
      desc: The inner evaluation run that produced these findings; its attachment carries the full record with file references.
    - id: 20260715-170623-d-tac-2ko
      kind: required-by
      desc: Resolving these findings is prerequisite to the plan's closing done — its acceptance criteria make the verification matrix a landing condition.
participants:
    - Christopher
confidence: high
topics:
    - reliability/discipline
    - reliability/testing
    - implementation/engine
    - implementation/cli
    - cli/ux
summary: This signal identifies two confirmed defects, a missing projection surface, and roughly half an unproven verification matrix blocking the mutation-target and recovery branch from landing. The defects — a silent target-release swallow in recovery paths and a structural dead-end in the interactive `sdd recover` flow for crash-mid-write scenarios — were surfaced by the inner evaluation (20260716-145957-s-tac-5ua), whose attachment carries the full record. Resolving all findings is prerequisite to closing the plan (20260715-170623-d-tac-2ko); the branch carries no data-corruption risk and is safe to continue on during remediation.
---

The local mutation-target and recovery branch carries two confirmed defects, a missing read-surface projection, and a verification shortfall that must be cleared before the work can land and close its plan 20260715-170623-d-tac-2ko. All findings were surfaced by the inner evaluation 20260716-145957-s-tac-5ua, which carries the full record with file references as an attachment; no data-corruption path exists, so the branch is safe to continue on while these are fixed.

The defects: first, a fail-loud violation — the recovery discard, abandon-unknown, and finalize-retry paths swallow the target-release error through a bare deferred Release in application/recovery.go, against the project's no-silent-fallback rule; it is masked today only because the local adapter's Release is a no-op, and becomes real the moment a connected adapter performs actual cleanup there. Second, the interactive `sdd recover` flow dead-ends on the primary crash-mid-write scenario it exists for: an intent persisted without an outcome projects as unknown, the verb menu offers only abandon-unknown, and reconciliation then forbids that verb because the mutation is definitively not applied — recovery succeeds only on a second run (the failed attempt's recorded reconciliation reclassifies the item) or via an explicit --verb bypass. The root cause is structural: verb selection guesses from a stale projection because no reconcile-first path exists. Third, recovery projections are wired into view alone, while the plan requires orientation and catch-up to carry actionable recovery notices as well.

Beyond the defects, roughly half the plan's verification matrix is unproven: the apply and bind-target recovery verbs and the entire recover CLI have no test coverage; legacy-v1 intents and attachment bytes crossing home staging into a target apply are unexercised; negative gating (discard after applied), multiple-checkout and changed-HEAD acquisition failures, and the same-directory and worktree workflow modes all lack proofs. Minors to weigh during the same pass: an orphaned staged-blob retention window with no GC, recovery-apply retrying CAS against the original expected revision (offered in the menu but unable to succeed once the graph has advanced), DeepEqual fragility in revalidation across the YAML/JSON round-trip, ordinary applies listing as recovered in recover --history, and view replaying every session log per call.
