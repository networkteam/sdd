---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260528-211910-s-cpt-r3l
      kind: addresses
      desc: the origin gap this implementation answers — cross-boundary references now exist; its formal closure can follow the merge
    - id: 20260507-130356-s-tac-m09
      kind: related
      desc: the open read-side query leak whose neighborhood the search widening touched without deepening — left to its own scoping
closes:
    - 20260707-175137-d-tac-i6f
participants:
    - Christopher
confidence: high
topics:
    - implementation/cli
    - type-system/refs
    - portability/mcp
summary: Cross-repo references are fully implemented end to end — reference foundation and cross-graph discovery — across nine commits on branch worktree-cross-repo-refs, satisfying every acceptance criterion of the plan it retires (20260707-175137-d-tac-i6f), with merge held pending Christopher's confirmation. The implementation answers the cross-boundary reasoning-re-derivation gap (20260528-211910-s-cpt-r3l), enabling resolution, status rendering, chain traversal, and discovery across repository boundaries; that signal's formal closure can follow the merge. The read-side query leak (20260507-130356-s-tac-m09) remains open as separately scoped — search widening touched its neighborhood without deepening it.
---

Cross-repo references are implemented end to end — the reference foundation and the cross-graph discovery layer — on branch worktree-cross-repo-refs, meeting every acceptance criterion of the plan this done retires (20260707-175137-d-tac-i6f); the merge to main is deliberately held for Christopher's confirmation. Nine commits carry the work (be8ed3a foundation primitives and capture acceptance, 9cfe8f8 rendering with the unresolved marker, 6dbe538 mechanical resolve-or-block with the forward-class exemption, 47bfb88 connected-repos config and managed caches, 1fd52b3 MultiGraph assembled inside the finder-owned GraphSource with cross-graph traversal, 590452f repo commands with init repo_id derivation and capture fetch-on-miss, ccf339c cross-graph search and view with MCP parameters and embedded-entry index exclusion, ef92664 repo-add declared-identity fallback plus sdd new --summary, 3bc92c3 explicit-summary test), with the full test suite, go vet, and gofmt clean after every slice and the flows verified live against the built binary in a two-repo fixture: repo add and list, capture with a resolvable cross-repo ref stored verbatim, show trees resolving the remote target with its owner-derived status, sdd show of a <repo-id>:<entry-id> primary, merged search with a repo-prefixed remote hit, per-repo view sections, and the high-severity resolve-or-block finding on an absent remote target, with pre-flight's ref-meta consistency check producing its verdict from the cached remote entry's content in the same run — that check resolves cross-repo refs through the cached member graph (1fd52b3), covering the plan's last acceptance criterion. This answers the reasoning-re-derivation gap (20260528-211910-s-cpt-r3l): entries can now reference across repository boundaries with resolution, status rendering, chain traversal, and discovery — the gap's own closure can follow once the branch merges and a real second graph exercises the mechanism.

Two deviations from the plan's letter were chosen during the run: a member index built under a mismatched embedder converges — re-embedding under the shared embedder on the next cross-graph search — instead of being excluded, with sdd lint flagging the pending drift per repo (self-healing with visible cost beats silent exclusion); and view --repo/--all-repos renders the layout per repo under repo headings rather than merging into one ranked list (score-comparable merging is search's semantics; view sections rank per graph). Two additions came from dialogue during the run: sdd repo add trusts the target's declared repo_id when the clone URL derives no identity, so local paths and offline mirrors connect while ssh/https URLs still cross-check the declared value; and sdd new --summary stores a caller-supplied summary verbatim, skipping LLM generation. Left open deliberately: the skill and reference-doc updates named in the plan's attached spec are follow-up work outside the acceptance criteria, and the read-side query leak (20260507-130356-s-tac-m09) stays open as separately scoped — the search widening touched its neighborhood without deepening it.
