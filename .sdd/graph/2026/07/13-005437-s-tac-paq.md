---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260712-234500-d-tac-x1m
      kind: addresses
      desc: completes Slice 5 durable intent/apply/outcome recovery, single-version replay, staged retention, and exclusive session-holder fencing
participants:
    - Christopher
confidence: high
topics:
    - portability/mcp
    - implementation/mcp
    - portability/runtime
    - reliability/discipline
    - reliability/testing
    - collaboration/concurrent-work
summary: 'Slice 5 of the public project-aware application plan shipped in commit 62ddc92, making prepared writes and exclusive session ownership durable: the root runtime now records versioned mutation intent before apply, reconciles unknown acknowledgements after restart, retries idempotent finalizers, and retains staged blobs until outcomes are safely resolved. Cross-instance file locks replace process-local memory for graph reconciliation and session CAS, and generation plus version checks fence displaced or stale callers. This completes the durable transition and session-holder slice of the seven-slice plan (20260712-234500-d-tac-x1m), with extracting the single shared MCP application remaining as Slice 6.'
---

Slice 5 of the public project-aware application plan shipped in commit 62ddc92, making prepared writes and exclusive session ownership durable before the MCP facade becomes public. The root runtime now records versioned mutation intent before canonical apply, records definitive outcomes afterward, reconciles unknown acknowledgements after restart, retries idempotent finalizers with their own durable outcomes, and retains staged blobs until an applied or not-applied result is safely resolved.

Filesystem graph reconciliation records and filesystem session CAS now use cross-instance file locks rather than process-local memory. Single-version session events and prepared transitions fail closed with typed migration-required errors. Chooser-kind TTL defaults back a user-visible holder model: live contention returns session-in-use metadata, explicit live and automatic expired takeovers are recorded in holder history, listings expose liveness, and generation plus version checks fence displaced or stale callers. Every mutation resolves current write access independently.

Failure injection covers stale graph revisions, orphaned intent/unknown apply recovery, finalizer retry, staged-blob retention, stale bindings, independent-store CAS, holder expiry and takeover, liveness listing, and unsupported codec versions. Full repository tests, the nested external-module tests, vet, and the repository-pinned linter pass. This completes the durable transition and session-holder slice of 20260712-234500-d-tac-x1m; extracting the single shared MCP application remains Slice 6.
