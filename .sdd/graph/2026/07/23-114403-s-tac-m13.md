---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260723-113402-s-tac-g4v
      kind: surfaced-by
      desc: the remediation round whose survey and review inventoried this residue
    - id: 20260723-113656-d-cpt-4y9
      kind: related
      desc: the architecture directive whose partial-read policy this residue completes — including its named WIP carve-out
participants:
    - Christopher
confidence: high
topics:
    - cli/architecture
    - reliability/discipline
    - portability/mcp
    - cli/ux
summary: This signal inventories the deliberately staged residue left after the graph-loading unification, covering two open policy questions — WIP-marker strictness and whether CLI read surfaces should display a health footer — plus substantive gaps including the MCP path swallowing genuine store failures, connected-repo LoadIssues reaching no surface, and a dangling-ref error that cannot distinguish unreadable from absent. It is surfaced by the remediation round's codebase survey and merge-readiness review (20260723-113402-s-tac-g4v), and completes the partial-read picture for the architecture directive (20260723-113656-d-cpt-4y9), including the WIP-carve-out that directive explicitly left open.
---

The graph-loading unification (20260723-113402-s-tac-g4v) leaves a deliberately staged residue of partial-read alignment work, inventoried by the codebase survey and the merge-readiness review (full inventory attached). The substantive items: the MCP dependency path still swallows genuine store failures into a generic dependencyUnavailable — now the only real fail-loud violation left on that path, since malformed entries load partially — and a connected repo's LoadIssues reach no surface at all: member-graph load problems appear in neither local lint nor health, and a cross-repo ref into an unreadable entry resolves as "not found" with no trace of why. Two policy calls are parked for explicit decision rather than silently diverging: WIP-marker strictness (one malformed marker still hard-fails reads that load markers, defensible for ephemeral files but undecided — the carve-out named in the architecture directive 20260723-113656-d-cpt-4y9) and whether CLI read surfaces (view, show, search, stats, index) should carry a one-line health footer instead of silently operating on readable entries only — today a user debugging a missing entry gets no pointer, and sdd index excludes unreadable entries from its own counts. Smaller code residue: WIPListQuery still carries GraphDir; ViewResult and ShowResult still carry the graph for presenter-side derived attributes, with a handful of model-level snapshot.graph pokes remaining in application; the structured-document path re-marshals frontmatter through the single ParseEntry gate (direct map decoding is a possible optimization behind the same seam); the dangling-ref rejection for an unreadable target reads "entry not found" without distinguishing unreadable from absent; and the capture playback refs line has no output-level test assertion. Also recorded for awareness, not action: write-time invariant checks run against a graph that may be missing unreadable entries — an accepted tradeoff of partial-read, mitigated by health visibility at session open and in lint.
