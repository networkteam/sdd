---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260718-180550-d-tac-e3x
      kind: addresses
      desc: realizes slice 1 (merge-clean graph applies) of the five-slice plan without closing it — slices 2 through 5 remain
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - implementation/mcp
    - reliability/discipline
    - collaboration/concurrent-work
summary: 'Slice 1 of the five-slice session-model realization plan shipped (commits b230e31, 190cffc): graph applies now CAS on a revalidated fresh revision with a three-attempt engine-internal retry, so concurrent unrelated appends merge cleanly, revision conflicts no longer reach the recovery queue, and exhausted contention fails with a structured graph-contention cause distinguishable from operator discards. This advances the merge-clean graph applies slice of the plan (20260718-180550-d-tac-e3x) without closing it — slices 2 through 5 remain outstanding.'
---

Slice 1 of the session-model realization plan shipped in the session-model worktree (commits b230e31, 190cffc): graph applies now CAS on the revalidated fresh revision with a three-attempt engine-internal mechanical retry, so concurrent unrelated appends merge cleanly and a revision conflict can no longer reach the recovery queue — exhausted contention fails typed as a graph conflict inviting a plain re-try, recorded with a structured graph-contention cause distinguishable from operator discards. The prepare-time revision pin stays in the durable intent as provenance only; the retry never re-runs pre-flight or summary; drafted attachments survive a discarded contended intent for re-submission. Coverage: interleaved captures with a slow LLM stage both land with pre-flight exactly once each and zero recovery entries; two writers colliding on the same WIP marker path still fail typed; the retry cap is pinned at exactly three; ReplaceSummary is driven through the same merge path; the sddtest graph-store conformance suite gains the merge-under-append case and the filesystem adapter passes it. Three independent reviews (architecture/layering, acceptance-criteria compliance, adversarial bug hunt) found no blockers; the review round's fixes — structured discard cause, shared release-and-terminal teardown helper, test fidelity and comment hygiene — landed in the follow-up commit. go vet, go test ./..., and golangci-lint are clean on the worktree branch.
