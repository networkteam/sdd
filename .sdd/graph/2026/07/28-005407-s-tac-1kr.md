---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260727-231512-d-tac-irt
      kind: addresses
      desc: the plan this delivers and retires — every acceptance criterion met, with its Christopher-reserved live-store criterion satisfied separately
    - id: 20260728-003033-s-tac-in2
      kind: builds-on
      desc: the outer validation that measured the census cleared on the live store; this completes the pair with the inner lens and closes the plan it deliberately left open
    - id: 20260727-230506-d-cpt-msg
      kind: grounded-in
      desc: the two-durable-conditions standard the projection now derives, and the source of the accepted-case reasoning behind the residual decision
    - id: 20260727-224047-d-cpt-u8o
      kind: grounded-in
      desc: 'the calibration that settled the residual: an unwritten terminal is scaffolding, so no verb is owed and the projection is honest as it stands'
    - id: 20260726-204112-s-prc-ogp
      kind: grounded-in
      desc: the run-it-once discipline that shaped the method — fixtures extracted from real logs, red-then-green split across separate agents, and the live-store run kept as a distinct requirement
    - id: 20260728-004924-s-tac-gtx
      kind: surfaces
      desc: the reclamation gap this work surfaced and deliberately did not answer — the staged-blob retention left owed by the residual
    - id: 20260728-004132-s-tac-cjt
      kind: surfaces
      desc: the branch-binding coverage gap this session hit while doing the work, unrelated to the fix itself
    - id: 20260721-151122-s-tac-spe
      kind: related
      desc: the write-path unification this work stayed independent of, as the plan intended
closes:
    - 20260727-231512-d-tac-irt
    - 20260727-102820-s-tac-9op
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - evaluation/inner/code-review
    - reliability/testing
    - reliability/discipline
    - portability/runtime
summary: 'The recovery-projection fix is delivered across four commits on `worktree-irt-recovery-projection`, with `recoveryItem` now deriving actionability from the recorded apply outcome before any version-based branching: positive proof of finalization means delivered and not actionable; absent or failed finalizer records keep the item actionable; this is the read-side expression of the two-durable-conditions standard (20260727-230506-d-cpt-msg), grounded in the calibration that an unwritten terminal is scaffolding rather than a recovery obligation (20260727-224047-d-cpt-u8o). This closes the stranded-notice gap (20260727-102820-s-tac-9op) and retires the plan (20260727-231512-d-tac-irt), with all thirty-four notices now `applied-finalized` as confirmed by the outer live-store run (20260728-003033-s-tac-in2). Two gaps were surfaced but not answered: the staged-blob reclamation left owed by the residual decision (20260728-004924-s-tac-gtx) and an unrelated branch-binding coverage hole encountered during the session (20260728-004132-s-tac-cjt).'
---

The recovery-projection fix is delivered on branch `worktree-irt-recovery-projection` across four commits — `ababca5`, `524d3f6`, `57e4741`, `6219a0b` — with `recoveryItem` now deriving actionability from the recorded apply outcome before any version-based branching, and both evaluation lenses covered. This retires the plan (20260727-231512-d-tac-irt) and closes the stranded-notice gap (20260727-102820-s-tac-9op): all thirty-four notices are cleared, measured on the live store by the outer run (20260728-003033-s-tac-in2) as exactly 80 previously recovered plus 34 now `applied-finalized`, with the store left untouched. The gap closes here rather than on that evaluation because the evaluation measured the census while this entry accounts for the change that cleared it.

The delivered rule: definitive applied with positive proof of finalization is delivered work and not actionable; a recorded finalizer failure, or no finalizer record at all, keeps the item actionable; definitive not-applied awaits a decision; unknown is pending delivery. `LegacyUnroutable` is set only where an item is still actionable and a target binding genuinely is the open question. This is the read-side expression of the two durable conditions in 20260727-230506-d-cpt-msg, and it is read-side only — no session-store write, no migration, no sweep, no new verb, nothing deleted — so it stayed independent of the write-path unification (20260721-151122-s-tac-spe) as the plan intended.

The method was chosen against 20260726-204112-s-prc-ogp and is the part worth reusing. Fixtures were extracted verbatim from three real live-store session logs rather than hand-authored, because the guard this work replaces was certified by proofs using constructed v1 intents carrying documents the v1 writer never produced. Red and green were given to separate agents, so the agent writing the failing tests could not make them pass. Three independent review lenses then ran, followed by a fourth verification pass over the corrected state that re-ran the pre-fix tree rather than trusting either implementer's report.

That method earned its cost. The first fix over-suppressed: treating zero recorded finalizer outcomes as success made reconcile-says-applied permanently non-actionable, and because the actionable list is the only list the CLI searches, `finalize-retry` became unreachable — a graph entry on disk with its commit owed and no machine-readable trace of it. Worse in kind than the noisy defect being fixed, found by an adversarial lens rather than by conformance, and corrected in `57e4741` by requiring positive proof, with two tests pinning the boundary from the silent side. Both were confirmed to fail without the fix. The plan's own boundary criterion had named a recorded *failure* while the actual breach was *silence*, so the over-suppressing code satisfied it — the plan-level finding recorded in 20260728-003033-s-tac-in2, which then recurred one level down when the verification pass found the same shape in the window after the finalizer record.

That last instance was decided rather than fixed. Under 20260727-224047-d-cpt-u8o an entry whose write landed and whose commit succeeded is delivered; the unwritten terminal is session bookkeeping, which warrants robustness rather than record-grade recovery, so `applied-finalized` is the honest projection and no version branch was added. What genuinely remains owed is the staged-blob retention, now recorded as 20260728-004924-s-tac-gtx rather than answered by re-arming a notice — which would have reimported the machinery this work exists to remove. A second gap, 20260728-004132-s-tac-cjt, was surfaced by the session's own worktree entry and is unrelated to the fix.

Verification: `go vet ./...`, `go test ./... -count=1` and `golangci-lint run ./...` clean in both the main module and the nested `examples/extendingsdd`, the latter having required a pre-existing lint fix the plan's final criterion had assumed already met. A 1200-row decision table against the pre-fix tree found no divergence beyond the intended one, plus 56 rows where legacy-unbound items now report their true outcome-derived state instead of a forced `unknown` — accurate, and behaviourally inert. The attached record carries the per-lens findings, the executed failure scenarios, and the reasoning behind the residual decision. Known and accepted: the fixture extractor lives under `testdata/`, invisible to build, vet and lint, so it will rot without notice — the repository's idiomatic answer is a build-tagged test, deferred to the next time the fixtures change.
