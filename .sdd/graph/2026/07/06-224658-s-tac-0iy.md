---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260705-010751-d-tac-dbk
      kind: addresses
      desc: realizes slice C of the four-slice completion directive, without closing it — slice D's remainder stays open
    - id: 20260706-093531-s-tac-xeq
      kind: builds-on
      desc: the park affordance and seeded drafts ride its explicit per-junction seed grammar
participants:
    - Christopher
confidence: high
topics:
    - implementation/engine
    - portability/mcp
    - reliability/discipline
summary: 'Slice C of the v1 workflow engine shipped (merge 83ced48): session read log with a mechanical refsInspected gate on capture, header-only hit-capped search defaults, connection-keyed content-hash dedup fixing a confirm-on-unseen fidelity hole, abandon by session handle in one unbound call, the park affordance, and a vocabulary data block for non-English graphs. This partially addresses the four-slice completion directive (20260705-010751-d-tac-dbk), with slice D''s freeze governance, parity pass, and user-scope registration still open. Park and seeded drafts build on the explicit per-junction seed grammar (20260706-093531-s-tac-xeq) with no side channel.'
summary_hash: 0d6ad8b8098136ff212e822d6325a4ff
---

Slice C of the v1 workflow engine shipped (merge 83ced48, commits 755c55a–5038afb): the session read log with the mechanical refsInspected gate on capture's assemble, header-only hit-capped search defaults, abandon by session handle, connection-keyed content-hash dedup of served blocks, unbound-call rejections inlining the parked-sessions list, the park affordance, and the vocabulary data block for non-English graphs. The read log records tool, entry IDs, and depth per serve — full means a body was served (a show primary, an injected chain's primary, or a same-session write), summary covers chain bullets, search headers, and snippets — metadata only, and the folded read set survives replay. The gate's rejection names exactly the un-inspected IDs while reads stay free and ungated; seeded parent evidence counts by construction since the read set is session-level, and the evaluate→capture handoff e2e passes with zero extra calls because the anchor injection alone satisfies the dispatched capture's gate. Served-once memory keys to the connection with a content hash over rendered block bytes — identical bytes stub (instruction units, the open-threads intro) or are omitted (framing), changed content always re-serves in full — which also fixes an old fidelity hole: an adjusted playback now re-serves fully, so a confirm can never bind to an unseen description. abandon(session) tears a parked session down in one unbound call — no resume, no framing — naming the label, discarded threads, and held WIP markers, and works on version-skewed logs since nothing replays. The park tool shelves a running move back to the shell junction with state kept, listed in open_threads and resumable across restarts; seeded drafts ride the explicit per-junction seed grammar (20260706-093531-s-tac-xeq), no side channel. Decisions taken in-slice: attachment reads log at summary depth under their own tool name (the gate stays body-strict), view sections stay unlogged in v1 (shape-tagged render data, summary-only), and same-session writes count as full-depth reads. Evidence: table tests plus e2e runs covering the completion interview's bar — a capture rejected for an un-inspected ref then passing after the show, teardown at one call per session, a reconnect paying orientation once — and a live stdio smoke of the built binary confirming gate, park, teardown, and defaults; go vet, the full test suite, and golangci-lint green. This realizes slice C of the completion directive (20260705-010751-d-tac-dbk) without closing it — slice D's freeze governance and side-by-side parity pass, plus user-scope registration, remain open.
