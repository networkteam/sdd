---
type: signal
layer: process
kind: gap
refs:
    - id: 20260718-090121-s-tac-3cq
      kind: surfaced-by
      desc: the real-agent outer evaluation that demonstrated the remaining discovery failure
    - id: 20260707-142811-s-prc-3kh
      kind: refines
      desc: the before-state reach-failure benchmark narrowed by this post-shipment observation
participants:
    - Christopher
confidence: high
topics:
    - agent/ux
    - cli/view
    - engine/base-facts
summary: 'The shipped view-layout fact works once an agent deliberately searches the graph for grammar, but first-call discovery in ordinary task flow remains unsolved: the agent had to infer that a view was needed and issue a targeted syntax search before the reference appeared, while the first-view breadcrumb arrives too late to bootstrap that call. This narrows the before-state reach-failure benchmark (s-prc-3kh) because source inspection, graph-file grep, and layout guessing were eliminated, but the relevant reference is still not apparent before first use. The finding was surfaced by the real-agent outer evaluation (s-tac-3cq).'
---

The shipped view-layout fact improves recovery once an agent deliberately searches for grammar, but it does not solve first-call discovery in ordinary task flow. In the real-agent outer evaluation (s-tac-3cq), the agent began with ordinary searches for Jonathan Philipp and only found the fact after inferring that a participant-filtered view was needed and issuing a targeted graph search for view syntax. The first-view breadcrumb could not help because the agent needed enough syntax knowledge to make that first call. This refines the earlier reach-failure benchmark (s-prc-3kh): source inspection, graph-file grep, and layout guessing were eliminated, but the reference still lacks a discovery surface available before first use. Resolution requires making the relevant reference apparent from the task or tool surface before a valid view call is already possible.
