---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260702-220449-d-tac-ry0
      kind: addresses
      desc: supplies slice 1 / the first acceptance criterion, without closing the plan
    - id: 20260702-222259-d-cpt-7iy
      kind: grounded-in
      desc: the type-system revision contract whose procedure rules this implementation conforms to
participants:
    - Christopher
    - Claude
confidence: high
topics:
    - type-system/kinds
    - portability/mcp
summary: 'Slice 1 of the v1 workflow engine plan shipped: the `procedure` decision kind is implemented end to end — model, validation, supersession chains with canonical resolution, embedded base-entry loading, fork lint, capture surfaces, and skill docs — conforming to the rules established by the type-system revision contract (20260702-222259-d-cpt-7iy). This satisfies the first acceptance criterion of the engine build plan (20260702-220449-d-tac-ry0) without closing it; typed spec parsing, LLM pre-flight routing, and the populated base procedure set are deliberately deferred to slices 2 and 4.'
summary_hash: 1a99186d3c989099d2aaec80d631a59e
---

Slice 1 of the v1 workflow engine plan is complete: the `procedure` decision kind shipped end to end — model, validation, supersession chains with canonical resolution, embedded base-entry loading, fork lint, capture surfaces, and skill docs — implementing the rules the type-system revision contract commits to (commit bf421c9; the revision itself was captured first as the 7+8 transitional contract). Procedure chains group by supersedes with write-once canonicals in their own namespace beside actors; ResolveProcedure resolves a canonical to its execution head under the project-head-wins rule; base procedures embed in the binary via internal/baseprocedures and join every graph load, disk winning on ID collision; lint flags forks on live heads as grooming candidates and catches write-once violations, with the generic ambiguous-fork warning exempted for procedures since their resolution is total; write-side surfaces (summarize, summary-hash lint, lint --fix, rewrite, participant coverage) skip or reject embedded entries; and a directive may now close a procedure, extending the retire-without-replacement carve-out. Verified via the full test suite, golangci-lint clean, the Codex skill render validating, and view/lint smoke tests.

Deliberate deferrals within plan scope, recorded here so later slices pick them up: typed `params`/`state`/`steps` spec parsing and structural validation land with the engine core (slice 2, per the contract's engine-load-time boundary); LLM pre-flight routing for procedure captures stays on the generic decision templates until the capture procedure makes user-facing procedure capture real (slice 4); the embedded base set ships empty until the capture procedure lands (slice 4). This fulfills the plan's first acceptance criterion without closing the plan — seven slices remain.
