---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260706-111022-s-tac-cam
      kind: grounded-in
      desc: the prior Codex capture failure mode this smoke test repeats as the regression case
    - id: 20260706-111102-d-tac-zhp
      kind: addresses
      desc: validates the code-side fail-fast and error-forwarding remedy by exercising the formerly hanging capture path
    - id: 20260706-113740-d-tac-ay1
      kind: addresses
      desc: validates the Codex-side SSH_AUTH_SOCK forwarding mitigation by completing an auto-committed capture through Codex
    - id: 20260706-122752-d-cpt-yum
      kind: grounded-in
      desc: uses the done-topic convention that inline done topics describe work-shape, here evaluation/outer/smoke-test
participants:
    - Christopher
confidence: high
topics:
    - evaluation/outer/smoke-test
preflight: skipped
summary: This done signal records that the Codex auto-commit capture path — previously the SIGTTIN hang site — completed successfully, with the entry's own existence in the graph serving as self-referential proof. It exercises the formerly hanging path against the regression case (s-tac-cam), validating both the fail-fast and error-forwarding code changes (d-tac-zhp) and the SSH_AUTH_SOCK forwarding mitigation (d-tac-ay1) as a single outer smoke test. The work-shape topic follows the inline done-topic convention (d-cpt-yum).
summary_hash: 5ad9f98df837d5eb200db82ed41ee807
---

The Codex capture path that previously wedged on SSH signing now completes a smoke-test capture, and this done signal is the test artifact.

We ran the same kind of SDD engine capture through Codex that previously failed when `sdd serve` could not reach the ssh-agent: playback confirmation led into graph writing and auto-commit, the path that had hung at `git commit` / `ssh-keygen -Y sign`. Since the Codex config now forwards `SSH_AUTH_SOCK`, and the commit path has been changed to fail fast and forward errors instead of waiting forever on an interactive prompt, a successful capture of this very done signal proves the outer behavior in use: Codex can record and commit an SDD entry through the workflow engine without wedging.

This is an outer smoke test, not a broad correctness evaluation. It validates the user-visible failure mode from the SIGTTIN hang: the operation that used to stall now returns by completing the capture. The evidence is intentionally self-referential: if this done signal exists in the graph as an auto-committed capture from Codex, then the capture path under test completed.
