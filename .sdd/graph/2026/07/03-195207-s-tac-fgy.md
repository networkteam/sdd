---
type: signal
layer: tactical
kind: gap
refs:
    - id: 20260702-204302-s-tac-4yf
      kind: grounded-in
      desc: the inventory the coverage check ran against
    - id: 20260702-220449-d-tac-ry0
      kind: related
      desc: the plan whose slice decomposition misses the item
participants:
    - Christopher
    - Claude
confidence: medium
topics:
    - portability/mcp
summary: 'This signal flags a coverage gap: locale-based language rendering — where vocabulary references become data per configured locale — is committed to in the parity inventory but has no assigned home in the v1 plan''s slice decomposition, leaving it absent from slice 3''s shell scope, the remaining slice names, and the deferred-with-triggers list. The gap was found by checking the plan (20260702-220449-d-tac-ry0) against the inventory it builds from (20260702-204302-s-tac-4yf). Candidate resolutions are folding it into a later shell slice after user-facing rendering is dogfooded, or adding it to the deferred list with an explicit trigger on the first non-English graph running engine mode.'
summary_hash: 251c36748608619cc75702c75276759c
---

The parity inventory's shell list commits to language rendering per configured locale — vocabulary references become data — but the v1 plan's slice decomposition gives that item no home: it is absent from slice 3's shipped shell scope, from the remaining slice names, and from the deferred-with-triggers list, so on an engine-only transition it would fall through silently for any non-English graph. Found during this session's coverage check of the plan against the inventory it builds from (20260702-204302-s-tac-4yf). Candidate homes: fold it into a later shell slice once catch-up-style user-facing rendering has been dogfooded, or add it to the plan's deferred list with an explicit trigger — the first non-English graph running engine mode.
