---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260708-010505-s-tac-94a
      kind: builds-on
      desc: the closing done this review round evaluates — architecture debt surfaced; functional claims not independently verified, outer evaluation pending
    - id: 20260413-142536-d-cpt-ah1
      kind: grounded-in
      desc: the CQRS planning contract supplying the review's yardstick — layer conflation is a structural gap
    - id: 20260413-142300-d-cpt-l3s
      kind: grounded-in
      desc: the CQRS layering definition whose concrete rules (purity, injected deps, package roles) the findings reason against
    - id: 20260507-130356-s-tac-m09
      kind: related
      desc: the open read-side dependency leak in the same drift family — ambient/couriered dependencies instead of composition-root ownership
participants:
    - Christopher
confidence: high
topics:
    - evaluation/inner/code-review
    - cli/architecture
summary: 'Christopher''s first code-review round on PR #2 (branch worktree-cross-repo-refs) surfaces four layering and encapsulation findings: a pure helper duplicated across CLI and MCP surfaces, ambient XDG environment resolution spread across eleven call sites inside the pure read side, ad-hoc git execution bypassing the established consumer-defined-interface pattern, and finder-shaped lint computation stranded in the CLI layer. The review builds on the implementation closing done (20260708-010505-s-tac-94a), leaving its functional claims unverified — no code was run — and holding the merge until the debt is paid; the ambient dependency pattern is the same drift family as the open read-side leak (20260507-130356-s-tac-m09), and all findings are reasoned against the CQRS layering rules (20260413-142300-d-cpt-l3s) and planning contract (20260413-142536-d-cpt-ah1).'
---

An inner evaluation of the cross-repo references implementation — Christopher's first code-review round on PR #2 (branch worktree-cross-repo-refs) — surfaces seven review comments that cluster into four architecture findings, all layering and encapsulation debt: a code-level review only, which leaves the closing done's (20260708-010505-s-tac-94a) functional claims standing on the implementing session's own record and Christopher's outer evaluation still pending. The review applied the standing architecture rules — the CQRS layering definition (20260413-142300-d-cpt-l3s), its planning contract (20260413-142536-d-cpt-ah1), and the push-logic-down, injected-dependency, and single-path disciplines codified in AGENTS.md. The findings: (1) a character-identical pure helper duplicated across the CLI and MCP surfaces (crossRepoIDsIn in cmd/sdd/repo.go, crossRepoIDsOf in internal/mcpserver/tools.go) — model-shaped computation living two layers too high, twice; (2) ambient environment resolution buried in internal/repos — CacheRoot and ConfigPath read XDG env vars as hidden globals and package-level LoadConfig/CacheDir are called from eleven sites across handlers, finders, and CLI, so the finder's member-graph loader silently reads env plus a user-global file inside the pure read side, and tests must resort to t.Setenv (the symptom the review comments caught); the same drift family as the open read-side dependency leak (20260507-130356-s-tac-m09) — dependencies consumed ambiently instead of owned at the composition root; (3) ad-hoc git execution — runGit in internal/repos/cache.go shells out directly, bypassing the established consumer-defined-interface pattern (handlers' Committer/Brancher/Mover/Puller, finders' GitSyncer), with the accumulated raw exec code in cmd/sdd/main.go and sync.go flagged as the same smell; (4) finder-shaped computation in the CLI layer — populateIndexLint/populateRepoIndexLint in cmd/sdd/search.go compute LintResult fields (config → manifest → fingerprint drift counts) that belong beside Finder.Lint. Verdict: at the code level no functional defects surfaced in reading, but this round neither ran nor exercised the feature — the closing done's acceptance verification stands on the implementing session's own record, outer evaluation by Christopher (validating the feature in use, against a real second graph) is still pending, and the merge is held until the layering debt is paid; the remediation architecture settled in this dialogue (a repos Registry/Manager purity split behind explicit locations, an internal/git adapter package, the model-level helper, the finder-side lint move) is recorded as its own directive on top of this evaluation. The full review record — comments verbatim, per-theme analysis, agreed remediation with reasons — is attached.
