---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260707-140544-d-cpt-dtv
      kind: grounded-in
      desc: the base-facts pull-layer commitment whose first slice this work delivers
    - id: 20260717-095714-s-tac-lwi
      kind: grounded-in
      desc: the view-tool usability gap whose four concrete tool-surface findings this work resolves
closes:
    - 20260717-102724-d-tac-n07
participants:
    - Christopher
confidence: high
topics:
    - implementation/mcp
    - implementation/cli
    - engine/base-facts
    - cli/view
summary: Across five commits, this entry ships the four MCP view-tool usability repairs and the first base fact — the view layout grammar rendered at load from the live executor vocabulary — with all eight acceptance criteria met and the full suite green. It closes the plan (d-tac-n07), delivers the first slice of the base-facts pull layer (d-cpt-dtv), and resolves all four agent-stranding tool-surface findings (s-tac-lwi); served-index coverage, alias resolution, and the reach-failure benchmark remain future work.
---

Shipped the view-tool usability fixes and the first base fact across five commits on the worktree branch, closing the plan (d-tac-n07) with all eight acceptance criteria met and verified. The base-facts mechanism (internal/basefacts, commit 3d28b65) merges embedded `fact` signals into every graph exactly as base procedures do, and ships the first fact — the view layout grammar at stable ID 20260717-110000-s-prc-vwg — whose body renders at load from the live executor vocabulary via viewlayout.ReferenceBody(finders.LiveViewVocabulary()), so a vocabulary change in code changes the fact with zero manual sync (ACs 5, 6, 7; resolves via show by full and short ID and surfaces in text search). A follow-up (d1147c8) collapsed the disk-wins embedded-entry merge to a single path — model.MergeEmbedded plus finders.BaseEntries, replacing four hand-rolled loops — and added LoadGraph/BuildSnapshot wiring tests with disk-wins shadowing. The four mechanical view fixes (2da2555) close the agent-stranding surfaces: macros now appear in the unknown-function error (AC1); bare multi-word, date, and duration arguments carry a quoting hint (AC2); an empty view result over MCP states "0 entries matched" and names known participants on a participant-filter miss, with emptiness made a read-side fact via SectionData.Count() and query.ViewResult.MatchedCount() (AC3); and MCP view output is capped with an n()-paging notice (AC4). The first-view breadcrumb (9a55994) points at the fact via a single viewHint producer, connection-keyed through the existing servedBefore memory so it also reaches the unbound free-reader cohort, joining the door breadcrumb only in the unbound-first-view cell (AC8). A defect in the AC3 work — Counts.Count() returning row count, so an untagged as-counts set falsely read as empty — was caught and fixed (46bee33): Counts now carries Matched and Count() returns it, keeping the contract honest independent of rendering. This realizes the first slice of the base-facts pull layer committed in d-cpt-dtv and resolves the tool-surface findings of the usability gap s-tac-lwi. Deliberately left open: the reach-failure benchmark s-prc-3kh closes only on observed behavior change (an agent pulling the grammar mid-dialogue), which is an evaluation step, not shipping; and d-cpt-dtv's served facts-index, participant alias resolution, and the CLI viewHelpText regeneration (landed separately on main) remain future work. Full suite green: go vet, go test ./..., golangci-lint on the touched packages.
