---
type: signal
layer: tactical
kind: done
refs:
    - id: 20260708-185833-d-tac-nhx
      kind: addresses
      desc: the plan this work realizes — slices 1–3 of four delivered; the plan stays open for the terminal-UX slice
closes:
    - 20260708-153545-s-cpt-m4j
    - 20260708-164349-s-cpt-9ql
    - 20260708-164528-s-tac-eql
participants:
    - Christopher
confidence: high
topics:
    - cli/config
    - search/index
summary: 'Slices 1–3 of the config-and-state placement plan (20260708-185833-d-tac-nhx) are implemented, verified, and committed on main, satisfying acceptance criteria 1–11: a unified BaseConfig overlay with strict decoding and `sdd config` get/set/effective, committed `dependencies` declarations with a cross-repo-dep-undeclared precondition, and a machine-global content-addressed index store with centralized flock-based locking. This retires the silent global schema truncation (20260708-153545-s-cpt-m4j), the uncommitted-connections portability gap (20260708-164349-s-cpt-9ql), and the duplicate index embedding problem (20260708-164528-s-tac-eql); the terminal-UX slice remains open under the plan.'
---

Slices 1–3 of the config-and-state placement plan (20260708-185833-d-tac-nhx) are implemented, verified, and committed on main across commits a3a3e73, e0258fd, 99d6d69, and 5038f85 (plus init auto-commit 4b37279 refreshing the skill renders), satisfying acceptance criteria 1–11 and leaving only the terminal-UX slice (ACs 12–13) for a follow-up session. Slice 1 (a3a3e73, e0258fd): the shared user/machine settings form one BaseConfig embedded inline by PerRepoConfig and the user-global GlobalConfig; effective resolution overlays global under committed under local under CLI flags; all three files decode strictly with errors naming the file and key; config load failures propagate through every resolver instead of degrading to defaults; and `sdd config` provides get/set (comment-preserving upserts, validated against the target layer's schema, global by default) plus the effective view with per-value provenance and masked api_keys. This retires the config finding (20260708-153545-s-cpt-m4j): its root cause — the narrow cross-repo-only global schema silently dropping participant, llm, and embedding — is gone, live-verified by the inverted repro (a participant set only globally is honored by lint and the capture fallbacks). Slice 2 (99d6d69): `sdd repo add` performs the dual write — a committed `dependencies` declaration (go.mod-style, comment-preserving sequence upsert) plus the per-user global resolution — with an idempotent upgrade path for pre-existing connections and a self-dependency guard; pre-flight gains the cross-repo-dep-undeclared precondition covering forward and backward ref classes alike; and `sdd init` names each declared-but-unconnected dependency with the exact `sdd repo add <clone-url>` instruction. This retires the connections finding (20260708-164349-s-cpt-9ql): a fresh clone now carries what it needs connected, and the one-way direction holds by construction, exactly as proposed. Slice 3 (5038f85): the vector index lives in one machine-global store at <cacheRoot>/index/<repo-key>/<fp-hash>, keyed by the committed repo_id (else a repo-root hash under local/), shared by a repo's checkout, its worktrees, and its connected cache; all locking is centralized in internal/index — a shared flock during Open's load, an exclusive WriteSession around every mutation including its manifest reads — with a concurrent writer/reader scenario test under -race and one small new dependency (github.com/gofrs/flock); `sdd init` migrates legacy indexes move-if-absent and never clobbers, the .sdd/index gitignore entry is no longer emitted, and the .worktreeinclude recipe drops the index carry-over. This retires the index finding (20260708-164528-s-tac-eql): live-verified on this repository by migrating the real 130 MB local index into the store, detecting the connected-cache copy as the removable duplicate (the double-embedding case dissolved), and searching from the store with no .sdd/index in the working tree. The terminal-UX finding stays open for slice 4, the full vet/test/lint gate was green at every commit, and the implementation run is parked with pickup notes.
