package mcpapp

const openThreadsIntro = `Open work, in continuation order — this dialogue's other threads first, then
other open dialogues (resume_session picks one up). Present it to the user in
their language as options to continue, never as an obligation. A move listed
here as "(this dialogue)" resumes through next on its instance id; if you have
lost that handle, resume_session with no session re-serves these moves with
their current step and schema:`

const openThreadsReminder = "(open work, in continuation order — offer continuations as before)"
