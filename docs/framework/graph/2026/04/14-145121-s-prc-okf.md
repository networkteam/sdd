---
type: signal
layer: process
refs:
    - 20260413-120558-d-prc-31v
participants:
    - Christopher Hlubek
    - Claude
confidence: high
preflight: skipped
summary: Pre-flight applies the regression test contract (d-prc-31v) to template refinements and definition improvements, even though these are improvements rather than code bugs and lack regression-testable paths. The contract requires regression tests for every bug fix without distinguishing bug fixes from refinements, causing over-application to non-code changes. This reveals that d-prc-31v needs clarification to properly scope what constitutes a bug fix versus an improvement.
summary_hash: 63466d96f175c00ca14b5a21a82c632d
---

Pre-flight applies d-prc-31v (regression test contract) to all changes that affect validation behavior, including prompt template refinements and definition improvements — not just code bug fixes. The contract says 'every bug fix requires a regression test' without scoping what counts as a bug fix. A template wording refinement (improving a definition, sharpening a distinction) is not a bug fix — it's an improvement with no code path to regression-test. The contract needs to distinguish bug fixes (broken behavior that needs correction) from refinements (correct but imprecise behavior that gets sharper).
