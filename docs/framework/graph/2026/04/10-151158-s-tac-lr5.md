---
type: signal
layer: tactical
refs:
    - 20260410-150919-d-prc-4du
participants:
    - Christopher Hlubek
    - Claude
confidence: high
summary: The sdd new --attach command doesn't support filename mapping for attachments, leaving piped content and temporary files with meaningless names like 'stdin' or 'tmp.XXX'. This friction point complicates the capture of planning artifacts in the graph required by the pre-flight gate decision (d-prc-4du), which mandates recording plan decisions before implementation begins.
summary_hash: 4778e2418e3ff463ec83a62cc81b1dde
---

sdd new --attach takes file paths but doesn't support renaming the attachment in the target directory. Piping content via /dev/stdin or using temp files results in meaningless filenames (stdin, tmp.XXX). The CLI needs a filename mapping syntax (e.g. --attach source:target or --attach /dev/stdin:plan-requirements.md) so that skill usage and piped content produce descriptive attachment names without manual git mv cleanup.
