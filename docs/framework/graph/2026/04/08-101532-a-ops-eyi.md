---
type: action
layer: operational
refs:
    - 20260408-100259-s-ops-h63
closes:
    - 20260408-100259-s-ops-h63
participants:
    - Christopher
    - Claude
summary: Fixed duplication in the explore sub-skill by batching all related entry IDs into a single sdd show call instead of making separate calls per entry, leveraging the CLI's existing deduplication mechanism that prints 'shown above' for repeats. This action resolves the duplication issue that was exposed when explore mode first revealed contextual connections the ref graph had missed (s-ops-h63).
summary_hash: 9ca8ddcf26955bb3afbc3915f8fb2d44
---

Fixed explore sub-skill deduplication: changed sdd-explore to batch all related entry IDs into a single sdd show call instead of separate calls per entry. The CLI already deduplicates shared upstream chains across multiple IDs (prints 'shown above' for repeats).
