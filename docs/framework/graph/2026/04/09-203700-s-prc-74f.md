---
type: signal
layer: process
refs:
    - 20260409-151757-a-tac-qa0
participants:
    - Christopher
    - Claude
summary: The SDD skill currently invokes git config user.name to infer the participant, but Claude Code's session context already provides the git user in the gitStatus header. The playbook guidance (qa0) should be updated to read from session context instead of calling git config, improving accuracy by leveraging already available session data.
summary_hash: d26fdd17dd8abd528489ce13602c4d01
---

SDD skill calls git config user.name to infer the participant, but Claude Code's session context already provides the git user in the gitStatus header. The playbook guidance from qa0 should say 'read from session context' instead of 'call git config'.
